# PAUL Handoff

**Date:** 2026-05-29
**Status:** paused — context limit approaching

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** SIMKA — Sistem Manajemen Keuangan Al-Hasaniyyah
**Core value:** Operator cari siswa → pilih tagihan → input bayar → cetak kwitansi. Semua tercatat, tidak ada yang bisa dihapus, audit aman.

---

## Current State

**Milestone:** v1.0 Production Ready
**Phase:** 4 of 4 — Reporting (In Progress)
**Plan:** 04-02 complete — 2/4 plans done

**Loop Position:**
```
Phase 04 — Reporting
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [04-02 closed, ready for 04-03]
```

---

## What Was Done This Session

- **Plan 04-01** — ReportsModule dibuat: GET /reports/dashboard, /harian, /bulanan, /tahunan — semua live dan verified
- **Plan 04-02** — GET /reports/tunggakan (10 siswa, filter kelas/unit/tahun) dan GET /reports/rekap-pos (breakdown per POS) — live dan verified
- **Phase 03 UNIFY** — 03-04 PDF Kwitansi diclose, Phase 03 ditransisi complete (100%)
- **PROJECT.md + ROADMAP.md** — Phase 03 decisions #22-28 tercatat, Phase 04 status In Progress

## What's In Progress

Tidak ada yang setengah jalan — loop 04-02 sudah closed bersih.

## What's Next

**Immediate:** `/paul:plan` untuk Plan 04-03 — Export Excel (ExcelJS)

**Scope 04-03:**
- Install ExcelJS (`pnpm add exceljs --filter backend`)
- ExcelService injectable di ReportsModule
- Endpoint export per laporan: GET /reports/harian/export/excel, /bulanan/export/excel, /tunggakan/export/excel, /rekap-pos/export/excel
- Response: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet binary

**After that:** Plan 04-04 — Export PDF (Puppeteer laporan — reuse PdfService dari Phase 03)

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview |
| `.paul/phases/04-reporting/04-02-SUMMARY.md` | Last completed plan |
| `apps/backend/src/reports/reports.service.ts` | 6 method: dashboard, harian, bulanan, tahunan, tunggakan, rekap-pos |
| `apps/backend/src/reports/reports.controller.ts` | 6 endpoint aktif |

## Tech Context Penting

- **Jalankan server:** `cd apps/backend && pnpm dev` (BUKAN dari root — turbo missing packageManager)
- **Docker harus jalan dulu:** `docker start simka-postgres simka-redis`
- **Login:** POST http://localhost:3000/api/auth/login `{"email":"admin@alhasaniyyah.sch.id","password":"Admin123!"}`
- **Pattern inject DB:** `@Inject(DRIZZLE) private db: DB` — import DB type dari `'../db/db'`
- **sum() result** selalu wrap dengan `Number(result ?? 0)` — Drizzle return string dari PostgreSQL
- **PdfService** sudah ada di `apps/backend/src/transactions/pdf/pdf.service.ts` — reuse untuk Plan 04-04

---

## Resume Instructions

1. Jalankan `/paul:resume`
2. STATE.md akan tunjukkan next action: `/paul:plan` untuk Plan 04-03
3. Konfirmasi "yes" → plan akan dibuat → "1" → apply

---

*Handoff created: 2026-05-29*
