---
phase: 01-core-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - .npmrc
  - .gitignore
  - apps/backend/package.json
  - apps/backend/tsconfig.json
  - apps/backend/nest-cli.json
  - apps/backend/.env.example
  - apps/backend/drizzle.config.ts
  - apps/backend/src/main.ts
  - apps/backend/src/app.module.ts
  - apps/backend/src/db/db.module.ts
  - apps/backend/src/db/db.ts
  - apps/backend/src/db/schema/auth.schema.ts
  - apps/backend/src/db/schema/academic.schema.ts
  - apps/backend/src/db/schema/financial.schema.ts
  - apps/backend/src/db/schema/audit.schema.ts
  - apps/backend/src/db/schema/index.ts
  - apps/backend/src/db/seed.ts
  - apps/frontend/package.json
  - apps/frontend/tsconfig.json
  - apps/frontend/tsconfig.node.json
  - apps/frontend/vite.config.ts
  - apps/frontend/index.html
  - apps/frontend/src/main.tsx
  - apps/frontend/src/App.tsx
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
autonomous: true
---

<objective>
## Goal
Setup monorepo SIMKA dengan NestJS backend, React+Vite frontend, shared package — lengkap dengan Drizzle schema semua tabel dan seed dummy data siap pakai.

## Purpose
Ini adalah fondasi dari seluruh sistem. Tanpa plan ini, tidak ada yang bisa dikerjakan. Setelah plan ini selesai, plan 01-02 (auth) dan 01-03 (master API) bisa langsung dikerjakan secara paralel.

## Output
- Monorepo berjalan: `pnpm install` dan `pnpm dev` sukses
- Semua tabel PostgreSQL terbuat via Drizzle migration
- Seed data tersedia: admin bisa login, data dummy siap ditest
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/phases/01-core-system/CONTEXT.md
@plan-simka-v1.md
</context>

<acceptance_criteria>

## AC-1: Monorepo Berjalan
```gherkin
Given repo di-clone dan PostgreSQL + Redis sudah running
When developer menjalankan `pnpm install` di root
Then semua dependencies terinstall tanpa error
  And `pnpm --filter backend start:dev` menjalankan NestJS di port 3000
  And `pnpm --filter frontend dev` menjalankan Vite di port 5173
```

## AC-2: Database Schema Terbuat
```gherkin
Given file .env sudah dikonfigurasi dengan DATABASE_URL yang valid
When developer menjalankan `pnpm --filter backend db:generate` lalu `db:migrate`
Then semua 11 tabel terbuat di PostgreSQL:
  users, roles, permissions, role_permissions,
  school_units, school_years, classes,
  students, student_classes,
  payment_posts, audit_logs
```

## AC-3: Seed Data Tersedia
```gherkin
Given migration sudah berhasil
When developer menjalankan `pnpm --filter backend db:seed`
Then data berikut tersedia di database:
  - 4 roles (Super Admin, Admin, Bendahara, Operator)
  - 1 user Super Admin (admin@alhasaniyyah.sch.id / Admin123!)
  - 4 school_units (SD, SMP, SMA, Pondok)
  - 1 school_year aktif (2025/2026)
  - minimal 8 classes (2 per unit)
  - 40 students dummy (10 per unit) dengan NIS unik
  - 4 payment_posts (SPP, Uang Gedung, Seragam, Buku)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Monorepo Structure</name>
  <files>
    package.json,
    pnpm-workspace.yaml,
    turbo.json,
    .npmrc,
    .gitignore,
    apps/backend/package.json,
    apps/backend/tsconfig.json,
    apps/backend/nest-cli.json,
    apps/backend/.env.example,
    apps/backend/src/main.ts,
    apps/backend/src/app.module.ts,
    apps/frontend/package.json,
    apps/frontend/tsconfig.json,
    apps/frontend/tsconfig.node.json,
    apps/frontend/vite.config.ts,
    apps/frontend/index.html,
    apps/frontend/src/main.tsx,
    apps/frontend/src/App.tsx,
    packages/shared/package.json,
    packages/shared/tsconfig.json,
    packages/shared/src/index.ts
  </files>
  <action>
    Buat struktur monorepo dengan pnpm workspaces + Turborepo.

    **Root package.json:**
    ```json
    {
      "name": "simka",
      "private": true,
      "scripts": {
        "dev": "turbo dev",
        "build": "turbo build",
        "lint": "turbo lint"
      },
      "devDependencies": {
        "turbo": "latest",
        "typescript": "^5.4.0"
      }
    }
    ```

    **pnpm-workspace.yaml:**
    ```yaml
    packages:
      - "apps/*"
      - "packages/*"
    ```

    **turbo.json:**
    ```json
    {
      "$schema": "https://turbo.build/schema.json",
      "tasks": {
        "dev": { "persistent": true, "cache": false },
        "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] }
      }
    }
    ```

    **.npmrc:**
    ```
    shamefully-hoist=true
    ```

    **apps/backend** — NestJS app:
    - Framework: @nestjs/common @nestjs/core @nestjs/platform-express @nestjs/config
    - Database: drizzle-orm pg
    - Dev: drizzle-kit @types/pg ts-node
    - Config: bcryptjs @types/bcryptjs
    - package.json scripts:
      - `dev`: `nest start --watch`
      - `build`: `nest build`
      - `db:generate`: `drizzle-kit generate`
      - `db:migrate`: `drizzle-kit migrate`
      - `db:seed`: `ts-node src/db/seed.ts`
      - `db:studio`: `drizzle-kit studio`

    **apps/backend/.env.example:**
    ```
    # Database
    DATABASE_URL=postgresql://user:password@localhost:5432/simka_db

    # Redis
    REDIS_HOST=localhost
    REDIS_PORT=6379

    # JWT
    JWT_ACCESS_SECRET=your-access-secret-here
    JWT_REFRESH_SECRET=your-refresh-secret-here
    JWT_ACCESS_EXPIRES_IN=15m
    JWT_REFRESH_EXPIRES_IN=7d

    # App
    PORT=3000
    NODE_ENV=development
    ```

    **apps/backend/src/main.ts:**
    - NestJS bootstrap dengan port dari env (default 3000)
    - Enable CORS untuk origin http://localhost:5173
    - Global prefix `/api`

    **apps/backend/src/app.module.ts:**
    - Import ConfigModule.forRoot({ isGlobal: true })
    - Import DatabaseModule (dibuat di Task 2)

    **apps/backend/nest-cli.json:** standard NestJS config

    **apps/backend/tsconfig.json:**
    - strict: true
    - target: ES2021
    - moduleResolution: node

    **apps/frontend** — React + Vite:
    - Dependencies: react react-dom
    - Dev: @vitejs/plugin-react typescript @types/react @types/react-dom vite
    - package.json scripts: dev, build, preview
    - vite.config.ts: server port 5173, proxy `/api` ke `http://localhost:3000`
    - src/main.tsx: ReactDOM.createRoot standard
    - src/App.tsx: placeholder `<div>SIMKA</div>`

    **packages/shared:**
    - Dependencies: zod
    - src/index.ts: export placeholder (schemas akan ditambah di plan berikutnya)
    - tsconfig: strict, module ESNext

    **.gitignore:** node_modules, dist, .env, *.local, .turbo, drizzle/
  </action>
  <verify>
    1. `pnpm install` dari root berjalan tanpa error
    2. `pnpm --filter backend start:dev` → NestJS running di port 3000, log "Application is running on: http://localhost:3000/api"
    3. `pnpm --filter frontend dev` → Vite running di port 5173
  </verify>
  <done>AC-1 satisfied: Monorepo berjalan, backend dan frontend up</done>
</task>

<task type="auto">
  <name>Task 2: Drizzle Schema — Semua Tabel</name>
  <files>
    apps/backend/drizzle.config.ts,
    apps/backend/src/db/db.ts,
    apps/backend/src/db/db.module.ts,
    apps/backend/src/db/schema/auth.schema.ts,
    apps/backend/src/db/schema/academic.schema.ts,
    apps/backend/src/db/schema/financial.schema.ts,
    apps/backend/src/db/schema/audit.schema.ts,
    apps/backend/src/db/schema/index.ts
  </files>
  <action>
    Buat Drizzle ORM schema lengkap sesuai ERD SIMKA.

    **drizzle.config.ts:**
    ```typescript
    import { defineConfig } from 'drizzle-kit';
    export default defineConfig({
      schema: './src/db/schema/index.ts',
      out: './drizzle',
      dialect: 'postgresql',
      dbCredentials: { url: process.env.DATABASE_URL! },
    });
    ```

    **src/db/db.ts:**
    ```typescript
    import { drizzle } from 'drizzle-orm/node-postgres';
    import { Pool } from 'pg';
    import * as schema from './schema';

    const pool = new Pool({ connectionString: process.env.DATABASE_URL });
    export const db = drizzle(pool, { schema });
    export type DB = typeof db;
    ```

    **src/db/db.module.ts:**
    - NestJS Global Module yang provide `db` sebagai DrizzleDB token
    - Export sehingga bisa di-inject di service lain

    **src/db/schema/auth.schema.ts:**
    ```typescript
    import { pgTable, bigserial, varchar, boolean, timestamp, bigint } from 'drizzle-orm/pg-core';
    import { relations } from 'drizzle-orm';

    export const roles = pgTable('roles', {
      id: bigserial('id', { mode: 'number' }).primaryKey(),
      name: varchar('name', { length: 100 }).notNull().unique(),
      createdAt: timestamp('created_at').defaultNow().notNull(),
    });

    export const permissions = pgTable('permissions', {
      id: bigserial('id', { mode: 'number' }).primaryKey(),
      code: varchar('code', { length: 100 }).notNull().unique(),
      name: varchar('name', { length: 100 }).notNull(),
      createdAt: timestamp('created_at').defaultNow().notNull(),
    });

    export const rolePermissions = pgTable('role_permissions', {
      roleId: bigint('role_id', { mode: 'number' }).notNull().references(() => roles.id),
      permissionId: bigint('permission_id', { mode: 'number' }).notNull().references(() => permissions.id),
    }, (t) => [{ pk: primaryKey({ columns: [t.roleId, t.permissionId] }) }]);

    export const users = pgTable('users', {
      id: bigserial('id', { mode: 'number' }).primaryKey(),
      name: varchar('name', { length: 255 }).notNull(),
      email: varchar('email', { length: 255 }).notNull().unique(),
      password: varchar('password', { length: 255 }).notNull(),
      roleId: bigint('role_id', { mode: 'number' }).notNull().references(() => roles.id),
      isActive: boolean('is_active').default(true).notNull(),
      lastLogin: timestamp('last_login'),
      createdAt: timestamp('created_at').defaultNow().notNull(),
      updatedAt: timestamp('updated_at').defaultNow().notNull(),
    });
    ```

    **src/db/schema/academic.schema.ts:**
    ```typescript
    // school_units: id, name, code, created_at, updated_at
    // school_years: id, name, start_year (smallint), end_year (smallint), is_active, created_at, updated_at
    // classes: id, school_unit_id (fk), name, level (smallint), created_at, updated_at

    // students:
    //   id, nis (unique), nisn (nullable, unique), name
    //   gender: pgEnum('gender_enum', ['L', 'P'])
    //   birth_place, birth_date (date)
    //   parent_name, phone, address (text)
    //   registration_status: pgEnum('reg_status_enum', ['baru', 'pindahan', 'mengulang'])
    //   student_status: pgEnum('student_status_enum', ['aktif', 'lulus', 'keluar', 'pindah'])
    //   entry_year (smallint), created_at, updated_at

    // student_classes:
    //   id, student_id (fk), class_id (fk), school_year_id (fk)
    //   is_active (boolean), created_at
    //   CONSTRAINT: unique(student_id, school_year_id) — satu siswa satu kelas per tahun ajaran
    ```

    **src/db/schema/financial.schema.ts:**
    ```typescript
    // payment_posts:
    //   id, code (unique), name
    //   type: pgEnum('payment_type_enum', ['bulanan', 'bebas'])
    //   description (text nullable)
    //   created_at, updated_at
    ```

    **src/db/schema/audit.schema.ts:**
    ```typescript
    // audit_logs:
    //   id, user_id (fk users nullable — system actions),
    //   action (varchar: CREATE|UPDATE|DELETE|VOID)
    //   table_name (varchar), record_id (bigint)
    //   old_data (jsonb nullable), new_data (jsonb nullable)
    //   created_at
    ```

    **src/db/schema/index.ts:**
    - Re-export semua schema dari 4 file di atas

    Catatan penting:
    - Gunakan `bigserial` untuk semua primary key
    - Gunakan `bigint` untuk semua foreign key dengan `{ mode: 'number' }`
    - Enum dibuat dengan `pgEnum` di luar tabel (reusable)
    - Tambahkan `relations()` untuk semua relasi (dibutuhkan Drizzle query API)
    - Jangan simpan class_id di students (sesuai constraint)
  </action>
  <verify>
    1. `pnpm --filter backend db:generate` → file migration terbuat di `apps/backend/drizzle/`
    2. `pnpm --filter backend db:migrate` → sukses, semua 11 tabel terbuat
    3. Verifikasi via psql: `\dt` menampilkan semua tabel
  </verify>
  <done>AC-2 satisfied: Semua 11 tabel terbuat di PostgreSQL via Drizzle migration</done>
</task>

<task type="auto">
  <name>Task 3: Seed Data Dummy</name>
  <files>
    apps/backend/src/db/seed.ts
  </files>
  <action>
    Buat seed script TypeScript yang mengisi database dengan data dummy siap test.

    **Urutan insert (harus respek foreign key):**
    1. roles
    2. permissions
    3. role_permissions
    4. users (Super Admin)
    5. school_units
    6. school_years
    7. classes
    8. students
    9. student_classes
    10. payment_posts

    **Data yang di-insert:**

    Roles:
    - Super Admin, Admin, Bendahara, Operator

    Permissions (format: `resource.action`):
    - `dashboard.view`
    - `school_unit.view`, `school_unit.create`, `school_unit.update`
    - `school_year.view`, `school_year.create`, `school_year.update`
    - `class.view`, `class.create`, `class.update`
    - `student.view`, `student.create`, `student.update`
    - `payment_post.view`, `payment_post.create`, `payment_post.update`
    - `bill.view`, `bill.create`, `bill.generate`
    - `transaction.view`, `transaction.create`, `transaction.void`
    - `report.view`, `report.export`
    - `user.view`, `user.create`, `user.update`
    - `role.view`, `role.manage`

    Role-Permission mapping:
    - Super Admin: semua permissions
    - Admin: semua kecuali role.manage, user.create
    - Bendahara: dashboard.view, bill.*, transaction.*, report.*
    - Operator: dashboard.view, student.view, bill.view, transaction.create

    Users:
    - name: "Super Admin", email: "admin@alhasaniyyah.sch.id"
    - password: hash bcryptjs dari "Admin123!" (rounds: 10)
    - role_id: Super Admin

    School Units: SD Al-Hasaniyyah (SD), SMP Al-Hasaniyyah (SMP), SMA Al-Hasaniyyah (SMA), Pondok Pesantren (PP)

    School Years: "2025/2026", start_year: 2025, end_year: 2026, is_active: true

    Classes (per unit, school_year 2025/2026):
    - SD: Kelas 1A (level 1), Kelas 2A (level 2)
    - SMP: Kelas VII A (level 7), Kelas VIII A (level 8)
    - SMA: Kelas X IPA (level 10), Kelas XI IPA (level 11)
    - PP: Kelas Ula 1 (level 1), Kelas Wustho 1 (level 4)

    Students (10 per unit = 40 total):
    - Generate NIS: 3 huruf random + ddmmyy(tanggal lahir) + 2 digit tahun masuk (25)
    - Gunakan nama Indonesia yang realistis
    - gender: mix L dan P
    - student_status: 'aktif'
    - registration_status: 'baru'
    - entry_year: 2025
    - Semua di-assign ke student_classes dengan kelas yang sesuai unitnya

    Payment Posts:
    - SPP (code: SPP, type: bulanan, desc: "Sumbangan Pembinaan Pendidikan")
    - Uang Gedung (code: UGD, type: bebas)
    - Seragam (code: SRM, type: bebas)
    - Buku (code: BKU, type: bebas)

    Script harus:
    - Cek apakah data sudah ada sebelum insert (idempotent — aman dijalankan berkali-kali)
    - Tampilkan log progress: "Seeding roles... done (4)", dst.
    - Tampilkan summary di akhir
    - Handle error dengan try/catch dan exit(1) jika gagal
  </action>
  <verify>
    1. `pnpm --filter backend db:seed` berjalan tanpa error
    2. Query: `SELECT COUNT(*) FROM students;` → 40
    3. Query: `SELECT email FROM users;` → admin@alhasaniyyah.sch.id
    4. Query: `SELECT COUNT(*) FROM student_classes;` → 40
  </verify>
  <done>AC-3 satisfied: Semua dummy data tersedia, seed idempotent</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Arsitektur ERD dari plan-simka-v1.md — jangan ubah struktur tabel
- Constraint: jangan simpan class_id di tabel students
- Constraint: student_classes harus punya unique(student_id, school_year_id)

## SCOPE LIMITS
- Plan ini TIDAK membuat auth endpoints (login/logout) — itu Plan 01-02
- Plan ini TIDAK membuat REST API endpoints — itu Plan 01-03
- Plan ini TIDAK setup shadcn/ui atau theming — itu Plan 01-04
- Frontend hanya scaffold minimal (App.tsx placeholder), belum ada UI nyata
- Jangan tambahkan tabel billing (bills, bill_months, payment_templates) — Phase 2
- Jangan tambahkan BullMQ atau Puppeteer — tidak dibutuhkan Phase 1

</boundaries>

<verification>
Sebelum plan dinyatakan selesai:
- [ ] `pnpm install` dari root sukses
- [ ] `pnpm --filter backend start:dev` running tanpa error di port 3000
- [ ] `pnpm --filter frontend dev` running di port 5173
- [ ] `pnpm --filter backend db:migrate` sukses — 11 tabel terbuat
- [ ] `pnpm --filter backend db:seed` sukses — data dummy tersedia
- [ ] TypeScript strict tidak ada error (`pnpm --filter backend build`)
- [ ] Query COUNT(*) di semua tabel kembali angka yang benar
</verification>

<success_criteria>
- Monorepo berjalan dengan satu perintah `pnpm dev`
- Semua 11 tabel PostgreSQL terbuat via Drizzle migration
- 40 siswa dummy, 1 Super Admin, 4 unit sekolah tersedia di database
- TypeScript compile tanpa error di backend dan frontend
- Plan 01-02 (auth) dan 01-03 (master API) bisa langsung dikerjakan
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/01-core-system/01-01-SUMMARY.md` dengan:
- Struktur folder yang dibuat
- Versi package utama yang diinstall
- Perintah untuk menjalankan development
- Keputusan teknis yang dibuat selama implementasi
</output>
```
