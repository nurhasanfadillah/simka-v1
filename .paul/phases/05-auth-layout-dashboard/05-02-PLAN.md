---
phase: 05-auth-layout-dashboard
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - apps/frontend/src/App.tsx
  - apps/frontend/src/router.tsx
  - apps/frontend/src/components/ProtectedRoute.tsx
  - apps/frontend/src/pages/login/index.tsx
  - apps/frontend/src/layouts/AppLayout.tsx
  - apps/frontend/src/components/sidebar/index.tsx
  - apps/frontend/src/components/sidebar/NavItem.tsx
  - apps/frontend/src/pages/dashboard/index.tsx
autonomous: false
---

<objective>
## Goal
Setup React Router v7 (library mode) dengan ProtectedRoute, buat AppLayout dengan sidebar hijau (#1A3829) sesuai DESIGN-SPEC.md, dan wiring routing lengkap dari login → dashboard.

## Purpose
Plan 05-01 membangun fondasi (deps, store, login page). Plan ini menambahkan navigasi — tanpa routing dan layout, tidak ada halaman yang bisa diakses setelah login. Plan 05-03 (Dashboard) langsung build di atas AppLayout yang dibuat di sini.

## Output
- React Router v7 aktif: `/login`, `/dashboard`, `/` (redirect)
- ProtectedRoute: redirect ke `/login` jika tidak authenticated
- AppLayout: sidebar fixed 260px (#1A3829 gradient) + main content area
- Sidebar: logo + nav items + section labels + user info + logout button
- Dashboard placeholder: halaman kosong di dalam AppLayout
- Flow lengkap: login → dashboard, logout → login, akses langsung → ProtectedRoute
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/DESIGN-SPEC.md

## Prior Plan
@.paul/phases/05-auth-layout-dashboard/05-01-SUMMARY.md

## Source Files (yang dimodifikasi/dipakai)
@apps/frontend/src/App.tsx
@apps/frontend/src/stores/auth.store.ts
@apps/frontend/src/pages/login/index.tsx
</context>

<acceptance_criteria>

## AC-1: Routing berjalan dengan auth guard
```gherkin
Given user belum login (isAuthenticated = false)
When user akses http://localhost:5173/dashboard
Then user di-redirect ke /login

Given user sudah login (isAuthenticated = true)
When user akses http://localhost:5173/login
Then user di-redirect ke /dashboard

Given user sudah login
When user akses http://localhost:5173/ (root)
Then user di-redirect ke /dashboard
```

## AC-2: AppLayout render dengan sidebar sesuai design spec
```gherkin
Given user sudah login dan berada di /dashboard
When halaman dirender
Then sidebar (260px) tampil di kiri dengan background gradient #1A3829 → #2D5A3D
  And nav items tampil dengan icons (lucide-react) dan label sesuai DESIGN-SPEC.md
  And active nav item highlight berwarna #00A651
  And content area di kanan berwarna #F9FAFB
```

## AC-3: User info dan logout berfungsi
```gherkin
Given user sudah login dengan data { name, role } di auth store
When sidebar dirender
Then inisial nama tampil dalam circle di bagian bawah sidebar
  And nama dan role user tampil

Given user klik tombol Logout
When logout dieksekusi
Then useAuthStore.logout() dipanggil
  And user di-redirect ke /login
  And localStorage 'simka-auth' terhapus
```

## AC-4: Section labels dan nav structure sesuai spec
```gherkin
Given user berada di halaman manapun dalam AppLayout
When sidebar dirender
Then tampak section label "MASTER DATA" (uppercase, gray kecil)
  And semua 9 nav items tampil sesuai urutan DESIGN-SPEC.md
  And nav item yang aktif sesuai current route (useLocation)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: React Router v7 setup + ProtectedRoute + login redirect</name>
  <files>
    apps/frontend/src/router.tsx,
    apps/frontend/src/components/ProtectedRoute.tsx,
    apps/frontend/src/App.tsx,
    apps/frontend/src/pages/login/index.tsx
  </files>
  <action>
    **Create `apps/frontend/src/router.tsx`:**

    Gunakan React Router v7 library mode (BrowserRouter + Routes + Route).
    Route structure:
    - `/login` → `<LoginPage>` (redirect ke /dashboard jika sudah auth)
    - `/dashboard` → `<ProtectedRoute>` → `<AppLayout>` → `<DashboardPage>`
    - `/` → `<Navigate to="/dashboard" replace />`
    - `*` → `<Navigate to="/dashboard" replace />` (catch-all)

    ```tsx
    import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom'
    import LoginPage from '@/pages/login'
    import DashboardPage from '@/pages/dashboard'
    import AppLayout from '@/layouts/AppLayout'
    import ProtectedRoute from '@/components/ProtectedRoute'

    export default function Router() {
      return (
        <BrowserRouter>
          <Routes>
            <Route path="/login" element={<LoginPage />} />
            <Route
              path="/dashboard"
              element={
                <ProtectedRoute>
                  <AppLayout>
                    <DashboardPage />
                  </AppLayout>
                </ProtectedRoute>
              }
            />
            <Route path="/" element={<Navigate to="/dashboard" replace />} />
            <Route path="*" element={<Navigate to="/dashboard" replace />} />
          </Routes>
        </BrowserRouter>
      )
    }
    ```

    **Create `apps/frontend/src/components/ProtectedRoute.tsx`:**

    ```tsx
    import { type ReactNode } from 'react'
    import { Navigate } from 'react-router-dom'
    import { useAuthStore } from '@/stores/auth.store'

    interface ProtectedRouteProps {
      children: ReactNode
    }

    export default function ProtectedRoute({ children }: ProtectedRouteProps) {
      const isAuthenticated = useAuthStore((s) => s.isAuthenticated)
      if (!isAuthenticated) return <Navigate to="/login" replace />
      return <>{children}</>
    }
    ```

    **Update `apps/frontend/src/App.tsx`:**

    ```tsx
    import Router from '@/router'

    function App() {
      return <Router />
    }

    export default App
    ```

    **Update `apps/frontend/src/pages/login/index.tsx`:**

    Tambahkan dua hal:
    1. Redirect ke /dashboard jika sudah authenticated (cek di awal komponen)
    2. Setelah `setAuth()` berhasil, navigate ke `/dashboard`

    Import tambahan: `import { useNavigate, Navigate } from 'react-router-dom'`

    Di awal komponen (sebelum form):
    ```tsx
    const isAuthenticated = useAuthStore((s) => s.isAuthenticated)
    const navigate = useNavigate()
    if (isAuthenticated) return <Navigate to="/dashboard" replace />
    ```

    Di `onSubmit` setelah `setAuth(...)`:
    ```tsx
    navigate('/dashboard')
    ```

    Avoid: jangan gunakan `window.location.href` — pakai React Router `navigate()`.
    Avoid: jangan tambahkan BrowserRouter di sini — router sudah di router.tsx.
  </action>
  <verify>
    ```
    cd apps/frontend && pnpm lint
    ```
    0 TypeScript error.
    File `src/router.tsx` dan `src/components/ProtectedRoute.tsx` harus ada.
  </verify>
  <done>AC-1 satisfied: routing structure dan auth guard terdefinisi</done>
</task>

<task type="auto">
  <name>Task 2: AppLayout + Sidebar dengan design sesuai spec</name>
  <files>
    apps/frontend/src/layouts/AppLayout.tsx,
    apps/frontend/src/components/sidebar/index.tsx,
    apps/frontend/src/components/sidebar/NavItem.tsx
  </files>
  <action>
    **Create `apps/frontend/src/components/sidebar/NavItem.tsx`:**

    Single nav item — icon + label, active state background #00A651, hover #2D5A3D.

    ```tsx
    import { type LucideIcon } from 'lucide-react'
    import { NavLink } from 'react-router-dom'
    import { cn } from '@/lib/utils'

    interface NavItemProps {
      to: string
      icon: LucideIcon
      label: string
    }

    export default function NavItem({ to, icon: Icon, label }: NavItemProps) {
      return (
        <NavLink
          to={to}
          className={({ isActive }) =>
            cn(
              'flex items-center gap-3 px-3 py-2 rounded-lg text-sm transition-colors',
              isActive
                ? 'bg-accent text-white font-medium'
                : 'text-white/80 hover:bg-primary-light hover:text-white'
            )
          }
        >
          <Icon className="w-4 h-4 shrink-0" />
          <span>{label}</span>
        </NavLink>
      )
    }
    ```

    **Create `apps/frontend/src/components/sidebar/index.tsx`:**

    Nav structure sesuai DESIGN-SPEC.md. Icons dari lucide-react:
    - Dashboard → LayoutDashboard
    - Tahun Pelajaran → CalendarDays
    - Manajemen Kelas → Users
    - POS Keuangan → CreditCard
    - Data Siswa → UserRound
    - Transaksi → ArrowLeftRight
    - Tagihan → ClipboardList
    - Laporan → BarChart3
    - Pengaturan → Settings

    Sidebar layout:
    - Top: logo.png + "SIMKA" bold white + subtitle kecil
    - Middle (scrollable): nav items + section label "MASTER DATA"
    - Bottom: avatar inisial + nama + role + tombol Logout

    ```tsx
    import {
      LayoutDashboard, CalendarDays, Users, CreditCard,
      UserRound, ArrowLeftRight, ClipboardList, BarChart3,
      Settings, LogOut,
    } from 'lucide-react'
    import { useNavigate } from 'react-router-dom'
    import NavItem from './NavItem'
    import { useAuthStore } from '@/stores/auth.store'

    function getInitials(name: string) {
      return name
        .split(' ')
        .slice(0, 2)
        .map((w) => w[0])
        .join('')
        .toUpperCase()
    }

    export default function Sidebar() {
      const user = useAuthStore((s) => s.user)
      const logout = useAuthStore((s) => s.logout)
      const navigate = useNavigate()

      const handleLogout = () => {
        logout()
        navigate('/login', { replace: true })
      }

      return (
        <aside className="flex flex-col w-64 min-h-screen bg-gradient-to-b from-primary to-primary-light shrink-0">
          {/* Logo area */}
          <div className="flex flex-col items-center py-6 px-4 border-b border-white/10">
            <img src="/logo.png" alt="Logo" className="w-10 h-10 object-contain mb-2" />
            <span className="text-white font-bold text-base leading-tight">SIMKA</span>
            <span className="text-white/60 text-[10px] text-center leading-tight mt-0.5">
              Sistem Manajemen Keuangan
            </span>
          </div>

          {/* Nav */}
          <nav className="flex-1 overflow-y-auto px-3 py-4 space-y-0.5">
            <NavItem to="/dashboard" icon={LayoutDashboard} label="Dashboard" />

            <p className="text-white/40 text-[10px] font-semibold uppercase tracking-wider px-3 pt-4 pb-1">
              Master Data
            </p>
            <NavItem to="/master/tahun-pelajaran" icon={CalendarDays} label="Tahun Pelajaran" />
            <NavItem to="/master/kelas" icon={Users} label="Manajemen Kelas" />
            <NavItem to="/master/pos" icon={CreditCard} label="POS Keuangan" />
            <NavItem to="/master/siswa" icon={UserRound} label="Data Siswa" />

            <div className="border-t border-white/10 my-2" />
            <NavItem to="/keuangan/transaksi/baru" icon={ArrowLeftRight} label="Transaksi" />
            <NavItem to="/keuangan/tagihan" icon={ClipboardList} label="Tagihan" />
            <NavItem to="/laporan/harian" icon={BarChart3} label="Laporan" />
            <NavItem to="/pengaturan/users" icon={Settings} label="Pengaturan" />
          </nav>

          {/* User info + logout */}
          <div className="px-3 py-4 border-t border-white/10">
            <div className="flex items-center gap-3 px-2 py-2 rounded-lg">
              <div className="w-8 h-8 rounded-full bg-accent flex items-center justify-center shrink-0">
                <span className="text-white text-xs font-bold">
                  {user ? getInitials(user.name) : 'U'}
                </span>
              </div>
              <div className="flex-1 min-w-0">
                <p className="text-white text-sm font-medium truncate">{user?.name ?? 'User'}</p>
                <p className="text-white/50 text-xs truncate">{user?.role ?? ''}</p>
              </div>
            </div>
            <button
              onClick={handleLogout}
              className="mt-1 flex items-center gap-2 w-full px-3 py-2 rounded-lg text-white/70 hover:text-white hover:bg-white/10 text-sm transition-colors"
            >
              <LogOut className="w-4 h-4" />
              <span>Logout</span>
            </button>
          </div>
        </aside>
      )
    }
    ```

    **Create `apps/frontend/src/layouts/AppLayout.tsx`:**

    ```tsx
    import { type ReactNode } from 'react'
    import Sidebar from '@/components/sidebar'

    interface AppLayoutProps {
      children: ReactNode
    }

    export default function AppLayout({ children }: AppLayoutProps) {
      return (
        <div className="flex min-h-screen bg-gray-50">
          <Sidebar />
          <main className="flex-1 overflow-auto">
            {children}
          </main>
        </div>
      )
    }
    ```

    Avoid: jangan tambahkan header/topbar di AppLayout — itu di-handle per-halaman.
    Avoid: jangan modifikasi shadcn components.
  </action>
  <verify>
    ```
    cd apps/frontend && pnpm lint
    ```
    0 TypeScript error.
    Files `src/layouts/AppLayout.tsx`, `src/components/sidebar/index.tsx`,
    `src/components/sidebar/NavItem.tsx` harus ada.
  </verify>
  <done>AC-2 dan AC-4 satisfied: AppLayout + Sidebar terdefinisi sesuai design spec</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard placeholder page</name>
  <files>
    apps/frontend/src/pages/dashboard/index.tsx
  </files>
  <action>
    **Create `apps/frontend/src/pages/dashboard/index.tsx`:**

    Placeholder sederhana — cukup untuk verify routing dan layout bekerja.
    Plan 05-03 akan replace ini dengan konten penuh.

    ```tsx
    export default function DashboardPage() {
      return (
        <div className="p-6">
          <h1 className="text-2xl font-bold text-gray-900">Dashboard</h1>
          <p className="text-gray-500 mt-1">Memuat data...</p>
        </div>
      )
    }
    ```

    Avoid: jangan tambahkan data fetching atau chart di sini — scope Plan 05-03.
  </action>
  <verify>
    Jalankan `pnpm dev` dari root dan buka http://localhost:5173.
    - Akses root `/` → harus redirect ke `/login` (belum auth)
    - Login dengan kredensial admin → harus redirect ke `/dashboard`
    - `/dashboard` menampilkan sidebar hijau + "Dashboard" di konten
    - Klik Logout → kembali ke `/login`
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: routing + layout + logout flow berjalan</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    React Router v7 + ProtectedRoute + AppLayout + Sidebar sesuai DESIGN-SPEC.md:
    - Routing: /login, /dashboard, / (redirect), catch-all
    - Auth guard: unauthenticated → redirect /login
    - Sidebar: gradient #1A3829, logo, 9 nav items + section label, user info, logout
    - Active nav: highlight #00A651
    - Dashboard placeholder dalam AppLayout
  </what-built>
  <how-to-verify>
    1. Jalankan: `pnpm dev` dari root (atau pastikan sudah running di port 5173/5175)
    2. Test auth guard:
       - [ ] Akses http://localhost:5175/ → redirect ke /login (belum login)
       - [ ] Akses http://localhost:5175/dashboard → redirect ke /login
    3. Login:
       - [ ] Isi email + password admin → klik Masuk
       - [ ] Redirect otomatis ke /dashboard
    4. Verifikasi sidebar:
       - [ ] Background gradient hijau gelap (#1A3829)
       - [ ] Logo segi lima + "SIMKA" di atas
       - [ ] Section label "MASTER DATA" (uppercase kecil)
       - [ ] 9 nav items dengan icon: Dashboard, Tahun Pelajaran, Manajemen Kelas, POS, Siswa, Transaksi, Tagihan, Laporan, Pengaturan
       - [ ] "Dashboard" nav item highlight hijau (#00A651) karena active
       - [ ] User name + role di bawah sidebar
       - [ ] Tombol Logout tersedia
    5. Test logout:
       - [ ] Klik Logout → redirect ke /login
       - [ ] Akses /dashboard lagi → redirect ke /login (auth terhapus)
  </how-to-verify>
  <resume-signal>Ketik "approved" untuk lanjut ke Plan 05-03 (Dashboard page), atau deskripsikan masalah</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `apps/backend/**` — backend sudah live, jangan disentuh
- `apps/frontend/src/components/ui/**` — shadcn components, jangan dimodifikasi
- `apps/frontend/src/lib/api.ts` — axios instance, sudah benar
- `apps/frontend/src/stores/auth.store.ts` — auth store, sudah benar
- `apps/frontend/src/lib/utils.ts` — cn helper, sudah benar
- `apps/frontend/tailwind.config.ts` — sudah benar setelah fix 05-01
- `apps/frontend/src/index.css` — sudah benar setelah fix 05-01

## SCOPE LIMITS
- Plan ini TIDAK buat konten dashboard (stat cards, charts) — scope Plan 05-03
- Plan ini TIDAK buat halaman master/keuangan/laporan — scope Phase 06-08
- Header/topbar per-halaman TIDAK dibuat di sini — ditambahkan di plan masing-masing

</boundaries>

<verification>
Sebelum declare plan complete:
- [ ] `pnpm lint` (dari apps/frontend) → 0 TypeScript error
- [ ] `pnpm dev` berhasil start tanpa error di terminal
- [ ] Akses / → redirect ke /login (unauthenticated)
- [ ] Login → redirect ke /dashboard
- [ ] Sidebar tampil dengan gradient hijau + nav items + user info
- [ ] Logout → redirect ke /login
- [ ] User checkpoint: visual sidebar sesuai design spec
</verification>

<success_criteria>
- Semua task completed tanpa error
- React Router v7 routing berjalan (auth guard, redirects)
- AppLayout + Sidebar render sesuai DESIGN-SPEC.md
- Login → dashboard flow end-to-end berjalan
- Logout flow berjalan (clear auth store + redirect)
- Siap untuk Plan 05-03 (konten dashboard)
</success_criteria>

<output>
Setelah completion, buat `.paul/phases/05-auth-layout-dashboard/05-02-SUMMARY.md`
</output>
