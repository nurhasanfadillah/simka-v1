---
phase: 05-auth-layout-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/package.json
  - apps/frontend/tailwind.config.ts
  - apps/frontend/postcss.config.js
  - apps/frontend/src/index.css
  - apps/frontend/components.json
  - apps/frontend/src/lib/utils.ts
  - apps/frontend/src/stores/auth.store.ts
  - apps/frontend/src/lib/api.ts
  - apps/frontend/src/pages/login/index.tsx
  - apps/frontend/src/App.tsx
autonomous: false
---

<objective>
## Goal
Setup fondasi frontend SIMKA: install semua dependencies, konfigurasi Tailwind + shadcn/ui, buat Zustand auth store, axios instance dengan interceptor, dan login page sesuai design spec.

## Purpose
Frontend saat ini hanya React 18 kosong. Plan ini membangun seluruh infrastruktur yang dibutuhkan sebelum bisa membangun halaman manapun — tanpa fondasi ini, Phase 05-02 (routing + layout) tidak bisa berjalan.

## Output
- Tailwind v3 + custom color tokens (#1A3829, #00A651) aktif
- shadcn/ui tersetup dengan components.json
- Zustand auth store (access_token, user, isAuthenticated)
- Axios instance dengan Bearer token interceptor + refresh token flow
- Login page (`/login`) — logo besar, green gradient, form email+password
- `pnpm dev` di apps/frontend menampilkan login page tanpa error
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/DESIGN-SPEC.md

## Source Files
@apps/frontend/package.json
@apps/frontend/src/App.tsx
@apps/frontend/vite.config.ts
@apps/frontend/tsconfig.json
</context>

<acceptance_criteria>

## AC-1: Tailwind + shadcn/ui aktif
```gherkin
Given frontend di-run dengan `pnpm dev`
When halaman dibuka di browser
Then tidak ada error build, Tailwind class berjalan (bg-color, text-color, dll)
  And komponen shadcn/ui (Button, Input) dapat diimport tanpa error
```

## AC-2: Auth store berfungsi
```gherkin
Given aplikasi fresh load (tidak ada token di localStorage)
When useAuthStore dipanggil
Then isAuthenticated = false, accessToken = null, user = null

Given login berhasil dipanggil dengan token valid
When setAuth(accessToken, refreshToken, user) dipanggil
Then isAuthenticated = true, token tersimpan di store
```

## AC-3: Axios instance dengan interceptor
```gherkin
Given accessToken tersimpan di auth store
When request API dikirim via apiClient
Then header Authorization: Bearer {token} otomatis terpasang

Given server mengembalikan 401 (token expired)
When refresh token masih valid
Then apiClient otomatis call POST /auth/refresh, update token, dan retry request
  
Given refresh token juga expired/invalid
When server mengembalikan error pada /auth/refresh
Then auth store di-logout() dan tidak ada infinite retry
```

## AC-4: Login page sesuai design spec
```gherkin
Given user membuka halaman root (/)
When login page dirender
Then tampak: logo segi lima (w-20 h-20), judul "SIMKA", subtitle, 
  form email + password, tombol Login hijau (#00A651)
  And background adalah gradient hijau (#1A3829 → #2D5A3D)
  And card putih centered di tengah layar
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies + setup Tailwind v3 + shadcn/ui config</name>
  <files>
    apps/frontend/package.json,
    apps/frontend/tailwind.config.ts,
    apps/frontend/postcss.config.js,
    apps/frontend/src/index.css,
    apps/frontend/components.json,
    apps/frontend/src/lib/utils.ts
  </files>
  <action>
    **Step 1 — Install runtime dependencies** (jalankan dari root monorepo):
    ```
    pnpm --filter frontend add react-router-dom@^7 zustand axios react-hook-form @hookform/resolvers zod lucide-react recharts @tanstack/react-table clsx tailwind-merge class-variance-authority @radix-ui/react-slot @radix-ui/react-label
    ```

    **Step 2 — Install dev dependencies** (dari root monorepo):
    ```
    pnpm --filter frontend add -D tailwindcss@^3 postcss autoprefixer
    ```

    **Step 3 — Create `apps/frontend/tailwind.config.ts`:**
    ```ts
    import type { Config } from 'tailwindcss'

    const config: Config = {
      darkMode: ['class'],
      content: ['./index.html', './src/**/*.{ts,tsx}'],
      theme: {
        extend: {
          colors: {
            primary: {
              DEFAULT: '#1A3829',
              light: '#2D5A3D',
            },
            accent: {
              DEFAULT: '#00A651',
              light: '#E8F5EE',
            },
          },
          borderRadius: {
            lg: 'var(--radius)',
            md: 'calc(var(--radius) - 2px)',
            sm: 'calc(var(--radius) - 4px)',
          },
        },
      },
      plugins: [],
    }

    export default config
    ```

    **Step 4 — Create `apps/frontend/postcss.config.js`:**
    ```js
    export default {
      plugins: {
        tailwindcss: {},
        autoprefixer: {},
      },
    }
    ```

    **Step 5 — Replace `apps/frontend/src/index.css`** dengan Tailwind directives + CSS variables shadcn/ui:
    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    @layer base {
      :root {
        --background: 0 0% 100%;
        --foreground: 222.2 84% 4.9%;
        --card: 0 0% 100%;
        --card-foreground: 222.2 84% 4.9%;
        --popover: 0 0% 100%;
        --popover-foreground: 222.2 84% 4.9%;
        --primary: 156 39% 16%;
        --primary-foreground: 0 0% 100%;
        --secondary: 210 40% 96.1%;
        --secondary-foreground: 222.2 47.4% 11.2%;
        --muted: 210 40% 96.1%;
        --muted-foreground: 215.4 16.3% 46.9%;
        --accent: 148 100% 32%;
        --accent-foreground: 0 0% 100%;
        --destructive: 0 84.2% 60.2%;
        --destructive-foreground: 210 40% 98%;
        --border: 214.3 31.8% 91.4%;
        --input: 214.3 31.8% 91.4%;
        --ring: 148 100% 32%;
        --radius: 0.5rem;
      }

      * {
        @apply border-border;
      }

      body {
        @apply bg-background text-foreground;
      }
    }
    ```

    **Step 6 — Create `apps/frontend/components.json`** (shadcn/ui config tanpa CLI):
    ```json
    {
      "$schema": "https://ui.shadcn.com/schema.json",
      "style": "default",
      "rsc": false,
      "tsx": true,
      "tailwind": {
        "config": "tailwind.config.ts",
        "css": "src/index.css",
        "baseColor": "neutral",
        "cssVariables": true,
        "prefix": ""
      },
      "aliases": {
        "components": "@/components",
        "utils": "@/lib/utils",
        "ui": "@/components/ui",
        "lib": "@/lib",
        "hooks": "@/hooks"
      },
      "iconLibrary": "lucide"
    }
    ```

    **Step 7 — Create `apps/frontend/src/lib/utils.ts`:**
    ```ts
    import { clsx, type ClassValue } from 'clsx'
    import { twMerge } from 'tailwind-merge'

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs))
    }
    ```

    **Step 8 — Add shadcn/ui components** (jalankan dari `apps/frontend`):
    ```
    cd apps/frontend && pnpm dlx shadcn@latest add button input label card
    ```
    Ini menghasilkan: `src/components/ui/{button,input,label,card}.tsx`

    Avoid: jangan modifikasi tsconfig.json — path alias `@/*` sudah benar.
    Avoid: jangan install tailwindcss v4 (shadcn/ui masih butuh v3 saat ini).
  </action>
  <verify>
    ```
    cd apps/frontend && pnpm lint
    ```
    Harus 0 TypeScript error.
    File `src/components/ui/button.tsx` harus ada.
    File `src/lib/utils.ts` harus ada dengan fungsi `cn`.
  </verify>
  <done>AC-1 satisfied: dependencies terinstall, Tailwind + shadcn/ui terkonfigurasi</done>
</task>

<task type="auto">
  <name>Task 2: Zustand auth store + axios instance dengan interceptor</name>
  <files>
    apps/frontend/src/stores/auth.store.ts,
    apps/frontend/src/lib/api.ts
  </files>
  <action>
    **Create `apps/frontend/src/stores/auth.store.ts`:**

    Store ini menyimpan auth state, persist ke localStorage agar tetap login setelah refresh.

    ```ts
    import { create } from 'zustand'
    import { persist } from 'zustand/middleware'

    interface AuthUser {
      id: string
      name: string
      email: string
      role: string
    }

    interface AuthState {
      accessToken: string | null
      refreshToken: string | null
      user: AuthUser | null
      isAuthenticated: boolean
      setAuth: (accessToken: string, refreshToken: string, user: AuthUser) => void
      updateAccessToken: (accessToken: string) => void
      logout: () => void
    }

    export const useAuthStore = create<AuthState>()(
      persist(
        (set) => ({
          accessToken: null,
          refreshToken: null,
          user: null,
          isAuthenticated: false,
          setAuth: (accessToken, refreshToken, user) =>
            set({ accessToken, refreshToken, user, isAuthenticated: true }),
          updateAccessToken: (accessToken) =>
            set({ accessToken }),
          logout: () =>
            set({ accessToken: null, refreshToken: null, user: null, isAuthenticated: false }),
        }),
        {
          name: 'simka-auth',
          partialObject: (state) => ({
            accessToken: state.accessToken,
            refreshToken: state.refreshToken,
            user: state.user,
            isAuthenticated: state.isAuthenticated,
          }),
        }
      )
    )
    ```

    Note: `partialObject` filter agar hanya state (bukan actions) yang di-persist.
    Jika `persist` tidak support `partialObject`, gunakan `partialize` (nama field bervariasi per Zustand versi).
    Periksa Zustand v5 API — field-nya mungkin `partialize: (state) => ({ ... })`.

    **Create `apps/frontend/src/lib/api.ts`:**

    Axios instance yang:
    1. Attach Bearer token dari auth store di setiap request
    2. Handle 401 → coba refresh → retry request
    3. Handle refresh gagal → logout

    ```ts
    import axios, { type AxiosRequestConfig } from 'axios'
    import { useAuthStore } from '@/stores/auth.store'

    export const apiClient = axios.create({
      baseURL: '/api',
      headers: { 'Content-Type': 'application/json' },
    })

    // Request interceptor — pasang Bearer token
    apiClient.interceptors.request.use((config) => {
      const token = useAuthStore.getState().accessToken
      if (token) {
        config.headers.Authorization = `Bearer ${token}`
      }
      return config
    })

    // Response interceptor — handle 401 + refresh
    let isRefreshing = false
    let failedQueue: Array<{ resolve: (token: string) => void; reject: (err: unknown) => void }> = []

    const processQueue = (error: unknown, token: string | null) => {
      failedQueue.forEach((p) => (error ? p.reject(error) : p.resolve(token!)))
      failedQueue = []
    }

    apiClient.interceptors.response.use(
      (response) => response,
      async (error) => {
        const originalRequest = error.config as AxiosRequestConfig & { _retry?: boolean }

        if (error.response?.status !== 401 || originalRequest._retry) {
          return Promise.reject(error)
        }

        const { refreshToken, updateAccessToken, logout } = useAuthStore.getState()

        if (!refreshToken) {
          logout()
          return Promise.reject(error)
        }

        if (isRefreshing) {
          return new Promise((resolve, reject) => {
            failedQueue.push({
              resolve: (token) => {
                originalRequest.headers = {
                  ...originalRequest.headers,
                  Authorization: `Bearer ${token}`,
                }
                resolve(apiClient(originalRequest))
              },
              reject,
            })
          })
        }

        originalRequest._retry = true
        isRefreshing = true

        try {
          const res = await axios.post<{ access_token: string }>('/api/auth/refresh', {
            refresh_token: refreshToken,
          })
          const newToken = res.data.access_token
          updateAccessToken(newToken)
          processQueue(null, newToken)
          originalRequest.headers = {
            ...originalRequest.headers,
            Authorization: `Bearer ${newToken}`,
          }
          return apiClient(originalRequest)
        } catch (refreshError) {
          processQueue(refreshError, null)
          logout()
          return Promise.reject(refreshError)
        } finally {
          isRefreshing = false
        }
      }
    )
    ```

    Avoid: jangan import useAuthStore dengan React hook syntax (no `useAuthStore()`) —
    gunakan `useAuthStore.getState()` karena interceptor berjalan di luar React component.
  </action>
  <verify>
    ```
    cd apps/frontend && pnpm lint
    ```
    File `src/stores/auth.store.ts` dan `src/lib/api.ts` harus ada tanpa TypeScript error.
  </verify>
  <done>AC-2 dan AC-3 satisfied: auth store persists state, axios interceptor attach token dan handle refresh</done>
</task>

<task type="auto">
  <name>Task 3: Login page + update App.tsx</name>
  <files>
    apps/frontend/src/pages/login/index.tsx,
    apps/frontend/src/App.tsx
  </files>
  <action>
    **Create `apps/frontend/src/pages/login/index.tsx`:**

    Design spec: background gradient hijau penuh (#1A3829 → #2D5A3D), card putih centered,
    logo segi lima (w-20 h-20), judul SIMKA, subtitle, form email+password, tombol Login.

    Menggunakan: react-hook-form + zod validation, shadcn Button/Input/Label/Card,
    apiClient POST /auth/login, useAuthStore.setAuth pada success.

    ```tsx
    import { useState } from 'react'
    import { useForm } from 'react-hook-form'
    import { zodResolver } from '@hookform/resolvers/zod'
    import { z } from 'zod'
    import { Button } from '@/components/ui/button'
    import { Input } from '@/components/ui/input'
    import { Label } from '@/components/ui/label'
    import { apiClient } from '@/lib/api'
    import { useAuthStore } from '@/stores/auth.store'

    const loginSchema = z.object({
      email: z.string().email('Email tidak valid'),
      password: z.string().min(1, 'Password wajib diisi'),
    })

    type LoginForm = z.infer<typeof loginSchema>

    interface LoginResponse {
      access_token: string
      refresh_token: string
      user: { id: string; name: string; email: string; role: string }
    }

    export default function LoginPage() {
      const setAuth = useAuthStore((s) => s.setAuth)
      const [serverError, setServerError] = useState<string | null>(null)

      const {
        register,
        handleSubmit,
        formState: { errors, isSubmitting },
      } = useForm<LoginForm>({ resolver: zodResolver(loginSchema) })

      const onSubmit = async (data: LoginForm) => {
        setServerError(null)
        try {
          const res = await apiClient.post<LoginResponse>('/auth/login', data)
          setAuth(res.data.access_token, res.data.refresh_token, res.data.user)
        } catch {
          setServerError('Email atau password salah')
        }
      }

      return (
        <div className="min-h-screen flex items-center justify-center bg-gradient-to-b from-primary to-primary-light px-4">
          <div className="w-full max-w-sm bg-white rounded-2xl shadow-xl p-8 space-y-6">
            {/* Logo + Judul */}
            <div className="flex flex-col items-center space-y-2">
              <img src="/logo.png" alt="Logo Al-Hasaniyyah" className="w-20 h-20 object-contain" />
              <h1 className="text-2xl font-bold text-accent">SIMKA</h1>
              <p className="text-sm text-gray-500 text-center">
                Sistem Manajemen Keuangan Al-Hasaniyyah
              </p>
            </div>

            {/* Form */}
            <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
              <div className="space-y-1">
                <Label htmlFor="email">Email</Label>
                <Input
                  id="email"
                  type="email"
                  placeholder="admin@alhasaniyyah.sch.id"
                  {...register('email')}
                />
                {errors.email && (
                  <p className="text-xs text-red-500">{errors.email.message}</p>
                )}
              </div>

              <div className="space-y-1">
                <Label htmlFor="password">Password</Label>
                <Input
                  id="password"
                  type="password"
                  placeholder="••••••••"
                  {...register('password')}
                />
                {errors.password && (
                  <p className="text-xs text-red-500">{errors.password.message}</p>
                )}
              </div>

              {serverError && (
                <p className="text-sm text-red-500 text-center">{serverError}</p>
              )}

              <Button
                type="submit"
                className="w-full bg-accent hover:bg-accent/90 text-white"
                disabled={isSubmitting}
              >
                {isSubmitting ? 'Masuk...' : 'Masuk'}
              </Button>
            </form>
          </div>
        </div>
      )
    }
    ```

    **Update `apps/frontend/src/App.tsx`:**

    Untuk sementara render LoginPage langsung (routing ditambahkan di Plan 05-02):

    ```tsx
    import LoginPage from '@/pages/login'

    function App() {
      return <LoginPage />
    }

    export default App
    ```

    **Update `apps/frontend/src/main.tsx`** — tambahkan import index.css jika belum ada:
    Pastikan ada baris: `import './index.css'`

    Avoid: jangan setup React Router di task ini — itu scope Plan 05-02.
    Avoid: jangan handle redirect after login — itu juga scope Plan 05-02.
  </action>
  <verify>
    Jalankan `pnpm dev` dari root monorepo (atau `cd apps/frontend && pnpm dev`).
    Buka http://localhost:5173 — harus tampil login page dengan background hijau,
    logo, dan form.
  </verify>
  <done>AC-4 satisfied: login page dirender sesuai design spec</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Login page foundation SIMKA:
    - Tailwind aktif dengan custom colors (primary #1A3829, accent #00A651)
    - shadcn/ui components tersedia
    - Zustand auth store + axios interceptor
    - Login page dengan background gradient hijau, logo, form email+password
  </what-built>
  <how-to-verify>
    1. Pastikan Docker running: `docker start simka-postgres simka-redis`
    2. Jalankan dari root: `pnpm dev` (atau khusus frontend: `cd apps/frontend && pnpm dev`)
    3. Buka: http://localhost:5173
    4. Verifikasi visual:
       - [ ] Background gradient hijau gelap (#1A3829)
       - [ ] Card putih centered dengan logo segi lima
       - [ ] Judul "SIMKA" berwarna hijau (#00A651)
       - [ ] Subtitle "Sistem Manajemen Keuangan Al-Hasaniyyah"
       - [ ] Input email + password
       - [ ] Tombol "Masuk" berwarna hijau
    5. Test login:
       - [ ] Klik Masuk tanpa isi → tampil validasi error
       - [ ] Isi email+password admin (default seed) → redirect tidak terjadi (belum ada routing — normal)
       - [ ] Console tidak ada TypeScript/runtime error merah
  </how-to-verify>
  <resume-signal>Ketik "approved" untuk lanjut ke Plan 05-02, atau deskripsikan masalah yang perlu diperbaiki</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `apps/backend/**` — backend sudah live, jangan disentuh
- `apps/frontend/vite.config.ts` — proxy config sudah benar
- `apps/frontend/tsconfig.json` — path alias `@/*` sudah benar
- `packages/shared/**` — shared types, jangan dimodifikasi

## SCOPE LIMITS
- Plan ini TIDAK setup React Router — routing ada di Plan 05-02
- Plan ini TIDAK buat sidebar/layout — itu Plan 05-02
- Plan ini TIDAK buat dashboard page — itu Plan 05-03
- Login redirect setelah sukses TIDAK diimplementasikan di plan ini

</boundaries>

<verification>
Sebelum declare plan complete:
- [ ] `pnpm lint` (dari apps/frontend) → 0 TypeScript error
- [ ] `pnpm dev` berhasil start tanpa error di terminal
- [ ] http://localhost:5173 menampilkan login page (background hijau + card putih + form)
- [ ] `apps/frontend/src/components/ui/button.tsx` ada (shadcn component)
- [ ] `apps/frontend/src/lib/utils.ts` ada (cn helper)
- [ ] `apps/frontend/src/stores/auth.store.ts` ada
- [ ] `apps/frontend/src/lib/api.ts` ada
- [ ] User checkpoint: visual sesuai design spec
</verification>

<success_criteria>
- Semua task completed tanpa error
- Tailwind custom colors berfungsi (class `bg-primary`, `text-accent` tersedia)
- shadcn/ui Button, Input, Label, Card bisa diimport
- Zustand auth store bisa get/set state
- Axios interceptor attach Bearer token otomatis
- Login page render sesuai DESIGN-SPEC.md
- Fondasi siap untuk Plan 05-02 (routing + layout)
</success_criteria>

<output>
Setelah completion, buat `.paul/phases/05-auth-layout-dashboard/05-01-SUMMARY.md`
</output>
