---
phase: 03-transaction-system
plan: 04
type: execute
wave: 1
depends_on: ["03-02"]
files_modified:
  - apps/backend/package.json
  - apps/backend/src/transactions/pdf/receipt.template.ts
  - apps/backend/src/transactions/pdf/pdf.service.ts
  - apps/backend/src/transactions/transactions.controller.ts
  - apps/backend/src/transactions/transactions.module.ts
autonomous: false
---

<objective>
## Goal
Generate PDF kwitansi menggunakan Puppeteer: endpoint `GET /transactions/:id/receipt` merender HTML template ke PDF dan mengembalikannya sebagai response binary (application/pdf).

## Purpose
Operator perlu mencetak kwitansi setelah pembayaran berhasil. Puppeteer di-decision di PROJECT.md sebagai PDF engine. Plan ini mengimplementasikan layer PDF di atas data transaksi yang sudah ada dari Plan 03-02.

## Output
- Puppeteer terinstall (`pnpm add puppeteer --filter backend`)
- `src/transactions/pdf/receipt.template.ts` — fungsi HTML template kwitansi
- `src/transactions/pdf/pdf.service.ts` — PdfService dengan Puppeteer
- Endpoint `GET /transactions/:id/receipt` → response `application/pdf`
- TransactionsModule updated untuk include PdfService
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Prior Work
@.paul/phases/03-transaction-system/03-02-SUMMARY.md

## Source Files
@apps/backend/src/transactions/transactions.service.ts
@apps/backend/src/transactions/transactions.controller.ts
@apps/backend/src/transactions/transactions.module.ts
</context>

<acceptance_criteria>

## AC-1: GET /transactions/:id/receipt mengembalikan PDF binary
```gherkin
Given transaction valid dengan status 'aktif' atau 'void'
When GET /transactions/:id/receipt dengan Authorization header valid
Then response Content-Type: application/pdf
And response body adalah PDF binary yang bisa dibuka
And PDF berisi transactionNumber, studentName, NIS, totalAmount, tanggal
```

## AC-2: Receipt berisi data items yang benar
```gherkin
Given transaction dengan 1+ items
When PDF di-generate
Then PDF menampilkan baris per item: deskripsi tagihan, jumlah (Rp)
And total amount tertera di footer tabel
```

## AC-3: Transaction tidak ditemukan → 404
```gherkin
Given transaction ID tidak ada
When GET /transactions/9999/receipt
Then response 404 Not Found (bukan PDF kosong)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Install Puppeteer + PdfService scaffolding</name>
  <files>
    apps/backend/package.json,
    apps/backend/src/transactions/pdf/pdf.service.ts,
    apps/backend/src/transactions/transactions.module.ts
  </files>
  <action>
    **1. Install Puppeteer:**
    ```bash
    pnpm add puppeteer --filter backend
    ```
    Puppeteer akan mengunduh Chromium secara otomatis (~170MB). Tunggu hingga selesai.

    **2. Buat `apps/backend/src/transactions/pdf/pdf.service.ts`:**
    ```typescript
    import { Injectable } from '@nestjs/common';
    import puppeteer from 'puppeteer';

    @Injectable()
    export class PdfService {
      async generateFromHtml(html: string): Promise<Buffer> {
        const browser = await puppeteer.launch({
          headless: true,
          args: ['--no-sandbox', '--disable-setuid-sandbox'],
        });
        try {
          const page = await browser.newPage();
          await page.setContent(html, { waitUntil: 'networkidle0' });
          const pdf = await page.pdf({
            format: 'A5',
            printBackground: true,
            margin: { top: '10mm', right: '10mm', bottom: '10mm', left: '10mm' },
          });
          return Buffer.from(pdf);
        } finally {
          await browser.close();
        }
      }
    }
    ```

    **3. Update `apps/backend/src/transactions/transactions.module.ts`:**
    Tambah import PdfService dan masukkan ke providers:
    ```typescript
    import { PdfService } from './pdf/pdf.service';
    // ...
    providers: [TransactionsService, PdfService],
    ```

    AVOID: Jangan ubah imports DatabaseModule atau controller list.
  </action>
  <verify>
    ```bash
    pnpm --filter backend build
    # → sukses, tidak ada error import puppeteer
    ```
  </verify>
  <done>PdfService terdefinisi dan build sukses</done>
</task>

<task type="auto">
  <name>Task 2: HTML receipt template + getReceiptData() di TransactionsService</name>
  <files>
    apps/backend/src/transactions/pdf/receipt.template.ts,
    apps/backend/src/transactions/transactions.service.ts
  </files>
  <action>
    **1. Buat `apps/backend/src/transactions/pdf/receipt.template.ts`:**

    Fungsi yang menerima data transaksi dan mengembalikan string HTML:
    ```typescript
    interface ReceiptItem {
      billId: number;
      billMonthId: number | null;
      amount: number;
    }

    interface ReceiptData {
      transactionNumber: string;
      studentName: string;
      nis: string;
      totalAmount: number;
      status: string;
      notes: string | null;
      createdAt: Date | string;
      items: ReceiptItem[];
    }

    function formatRupiah(amount: number): string {
      return new Intl.NumberFormat('id-ID', {
        style: 'currency',
        currency: 'IDR',
        minimumFractionDigits: 0,
      }).format(amount);
    }

    function formatDate(date: Date | string): string {
      return new Date(date).toLocaleDateString('id-ID', {
        day: '2-digit', month: 'long', year: 'numeric',
      });
    }

    export function buildReceiptHtml(data: ReceiptData): string {
      const itemRows = data.items.map((item, i) =>
        `<tr>
          <td style="padding:6px 8px;border:1px solid #ddd;">${i + 1}</td>
          <td style="padding:6px 8px;border:1px solid #ddd;">Tagihan #${item.billId}${item.billMonthId ? ` (Bln ${item.billMonthId})` : ''}</td>
          <td style="padding:6px 8px;border:1px solid #ddd;text-align:right;">${formatRupiah(item.amount)}</td>
        </tr>`
      ).join('');

      return `<!DOCTYPE html>
    <html lang="id">
    <head>
      <meta charset="UTF-8">
      <style>
        body { font-family: Arial, sans-serif; font-size: 12px; color: #1a1a1a; margin: 0; padding: 0; }
        .header { background: #1A3829; color: white; padding: 16px; text-align: center; }
        .header h1 { margin: 0; font-size: 16px; }
        .header p { margin: 4px 0 0; font-size: 11px; opacity: 0.85; }
        .body { padding: 16px; }
        .title { font-size: 14px; font-weight: bold; text-align: center; margin: 12px 0; letter-spacing: 1px; }
        .info-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 4px 16px; margin-bottom: 12px; }
        .info-row { display: flex; gap: 4px; }
        .info-label { color: #555; min-width: 100px; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 8px; }
        th { background: #1A3829; color: white; padding: 8px; text-align: left; font-size: 11px; }
        th:last-child { text-align: right; }
        .total-row td { font-weight: bold; background: #f5f5f5; padding: 8px; border: 1px solid #ddd; }
        .total-row td:last-child { text-align: right; color: #1A3829; }
        .footer { margin-top: 24px; display: flex; justify-content: flex-end; }
        .sign-box { text-align: center; }
        .sign-box p { margin: 0 0 48px; }
        .sign-line { border-top: 1px solid #333; padding-top: 4px; min-width: 120px; }
        .void-badge { background: #dc2626; color: white; padding: 2px 8px; border-radius: 4px; font-size: 10px; font-weight: bold; }
      </style>
    </head>
    <body>
      <div class="header">
        <h1>Yayasan Al-Hasaniyyah</h1>
        <p>Sistem Informasi Keuangan Sekolah</p>
      </div>
      <div class="body">
        <div class="title">KWITANSI PEMBAYARAN ${data.status === 'void' ? '<span class="void-badge">VOID</span>' : ''}</div>
        <div class="info-grid">
          <div class="info-row"><span class="info-label">No. Transaksi</span><span>: ${data.transactionNumber}</span></div>
          <div class="info-row"><span class="info-label">Tanggal</span><span>: ${formatDate(data.createdAt)}</span></div>
          <div class="info-row"><span class="info-label">Nama Siswa</span><span>: ${data.studentName}</span></div>
          <div class="info-row"><span class="info-label">NIS</span><span>: ${data.nis}</span></div>
          ${data.notes ? `<div class="info-row" style="grid-column:1/-1"><span class="info-label">Keterangan</span><span>: ${data.notes}</span></div>` : ''}
        </div>
        <table>
          <thead>
            <tr>
              <th style="width:30px">No</th>
              <th>Keterangan</th>
              <th style="width:120px">Jumlah</th>
            </tr>
          </thead>
          <tbody>
            ${itemRows}
            <tr class="total-row">
              <td colspan="2">Total Pembayaran</td>
              <td>${formatRupiah(data.totalAmount)}</td>
            </tr>
          </tbody>
        </table>
        <div class="footer">
          <div class="sign-box">
            <p>Petugas Keuangan</p>
            <div class="sign-line">(__________________)</div>
          </div>
        </div>
      </div>
    </body>
    </html>`;
    }
    ```

    **2. Tambah method `getReceiptData(id)` ke TransactionsService:**
    Method ini mirip findOne() tapi return plain object tanpa spread (untuk dipass ke template):
    ```typescript
    async getReceiptData(id: number) {
      const data = await this.findOne(id);  // reuse findOne yang sudah ada
      return data;
    }
    ```

    AVOID: Jangan duplikasi query findOne — cukup delegate ke method yang sudah ada.
  </action>
  <verify>
    ```bash
    pnpm --filter backend build
    # → sukses
    ```
  </verify>
  <done>Template dan getReceiptData() terdefinisi, build bersih</done>
</task>

<task type="auto">
  <name>Task 3: Endpoint GET /transactions/:id/receipt di controller</name>
  <files>apps/backend/src/transactions/transactions.controller.ts</files>
  <action>
    Tambah endpoint receipt ke controller.

    **Import tambahan:**
    ```typescript
    import { Res } from '@nestjs/common';
    import { Response } from 'express';
    import { PdfService } from './pdf/pdf.service';
    import { buildReceiptHtml } from './pdf/receipt.template';
    ```

    **Update constructor** untuk inject PdfService:
    ```typescript
    constructor(
      private readonly service: TransactionsService,
      private readonly pdfService: PdfService,
    ) {}
    ```

    **Tambah route** (taruh SEBELUM `@Get(':id')` agar tidak di-ambush oleh param route):
    ```typescript
    @Get(':id/receipt')
    @RequirePermissions('transaction.view')
    async getReceipt(
      @Param('id', ParseIntPipe) id: number,
      @Res() res: Response,
    ) {
      const data = await this.service.getReceiptData(id);
      const html = buildReceiptHtml(data as any);
      const pdf = await this.pdfService.generateFromHtml(html);
      res.set({
        'Content-Type': 'application/pdf',
        'Content-Disposition': `inline; filename="kwitansi-${data.transactionNumber}.pdf"`,
        'Content-Length': pdf.length,
      });
      res.end(pdf);
    }
    ```

    PENTING: `@Get(':id/receipt')` harus ditempatkan SEBELUM `@Get(':id')` di file.
             NestJS akan match `:id` sebelum `:id/receipt` jika urutan terbalik.
    PENTING: Gunakan `res.end(pdf)` bukan `res.send(pdf)` untuk binary buffer yang tepat.
    AVOID: Jangan hapus atau ubah route lain.
  </action>
  <verify>
    ```bash
    pnpm --filter backend build
    # → sukses

    # Integration test (server harus berjalan):
    TOKEN=$(curl -s -X POST http://localhost:3000/api/auth/login \
      -H "Content-Type: application/json" \
      -d '{"email":"admin@alhasaniyyah.sch.id","password":"Admin123!"}' \
      | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')

    curl -s "http://localhost:3000/api/transactions/1/receipt" \
      -H "Authorization: Bearer $TOKEN" \
      -o /tmp/kwitansi.pdf

    # Verify PDF header
    head -c 4 /tmp/kwitansi.pdf
    # → harus menampilkan: %PDF
    ```
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: PDF ter-generate, data benar, 404 untuk ID invalid</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    PDF kwitansi endpoint: GET /transactions/:id/receipt
    - Puppeteer render HTML → PDF A5
    - Header hijau (#1A3829) dengan nama yayasan
    - Grid info: nomor transaksi, tanggal, nama siswa, NIS
    - Tabel items dengan total
    - Footer tanda tangan
    - Badge VOID merah jika status void
  </what-built>
  <how-to-verify>
    1. Jalankan command ini di terminal (atau ketik `! <command>` di prompt):
       ```bash
       TOKEN=$(curl -s -X POST http://localhost:3000/api/auth/login \
         -H "Content-Type: application/json" \
         -d '{"email":"admin@alhasaniyyah.sch.id","password":"Admin123!"}' \
         | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')

       curl -s "http://localhost:3000/api/transactions/1/receipt" \
         -H "Authorization: Bearer $TOKEN" \
         -o /tmp/kwitansi-1.pdf && echo "Downloaded"
       ```
    2. Buka `/tmp/kwitansi-1.pdf` di PDF viewer
    3. Pastikan: nomor transaksi TRX-..., nama siswa, NIS, tabel items, total amount tertera
    4. Test 404: `curl -s http://localhost:3000/api/transactions/9999/receipt -H "Authorization: Bearer $TOKEN"` → harus return JSON 404, bukan PDF
  </how-to-verify>
  <resume-signal>Ketik "approved" jika PDF tampil benar, atau deskripsikan masalah yang ditemukan</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `apps/backend/src/db/schema/` — tidak ada perubahan schema
- `apps/backend/drizzle/` — tidak ada migration
- `apps/backend/src/transactions/transactions.service.ts` method: create(), findAll(), findOne(), voidTransaction()
- `apps/backend/src/billing/` — tidak disentuh

## SCOPE LIMITS
- PDF hanya untuk kwitansi transaksi — bukan laporan (itu Phase 04)
- Tidak ada frontend di plan ini
- Tidak ada email/kirim PDF otomatis
- Template desain cukup fungsional — tidak perlu pixel-perfect

</boundaries>

<verification>
- [ ] `pnpm --filter backend build` sukses (dengan puppeteer)
- [ ] `head -c 4 /tmp/kwitansi.pdf` menampilkan `%PDF`
- [ ] PDF berisi transactionNumber, studentName, totalAmount
- [ ] GET /transactions/9999/receipt → 404 JSON (bukan PDF)
</verification>

<success_criteria>
- Task 1, 2, dan 3 completed
- TypeScript build bersih
- PDF ter-generate dan bisa dibuka
- Checkpoint human-verify approved
</success_criteria>

<output>
Setelah completion, buat `.paul/phases/03-transaction-system/03-04-SUMMARY.md`
</output>
