---
phase: 03-transaction-system
plan: 01
type: execute
wave: 1
depends_on: ["02-04"]
files_modified:
  - apps/backend/src/db/schema/financial.schema.ts
autonomous: true
---

<objective>
## Goal
Tambah tabel `transactions` dan `transaction_items` ke financial schema beserta migration-nya agar Phase 03 punya fondasi data yang lengkap.

## Purpose
Phase 03 (Transaction System) membutuhkan tabel transaksi sebagai target persist setiap pembayaran. Tanpa schema ini, Plan 03-02 (Transaction API) tidak bisa dieksekusi.

## Output
- `transactionStatusEnum` baru di financial.schema.ts
- Tabel `transactions` (header transaksi + void support)
- Tabel `transaction_items` (detail: 1 item = 1 bill_month yang dibayar)
- Relations lengkap untuk kedua tabel
- Migration `0002_*.sql` ter-generate dan ter-apply ke `simka_db`
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Source Files
@apps/backend/src/db/schema/financial.schema.ts
@apps/backend/src/db/schema/auth.schema.ts
@apps/backend/src/db/schema/academic.schema.ts
</context>

<acceptance_criteria>

## AC-1: Schema TypeScript valid dan build bersih
```gherkin
Given financial.schema.ts diextend dengan transactions + transaction_items
When pnpm --filter backend build dijalankan
Then build sukses tanpa error TypeScript
```

## AC-2: Migration ter-generate
```gherkin
Given schema sudah diupdate
When pnpm --filter backend db:generate dijalankan
Then file 0002_*.sql baru muncul di apps/backend/drizzle/
And berisi CREATE TYPE transaction_status_enum + CREATE TABLE transactions + CREATE TABLE transaction_items
```

## AC-3: Migration ter-apply ke database
```gherkin
Given migration 0002_*.sql ter-generate
When pnpm --filter backend db:migrate dijalankan
Then tabel transactions dan transaction_items ada di simka_db
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extend financial.schema.ts — tambah transactionStatusEnum, transactions, transactionItems + relations</name>
  <files>apps/backend/src/db/schema/financial.schema.ts</files>
  <action>
    **Import tambahan di bagian atas:**
    Tambah `text` ke import dari 'drizzle-orm/pg-core' (sudah ada, skip jika ada).
    Tambah import `users` dari './auth.schema':
    ```typescript
    import { users } from './auth.schema';
    ```

    **1. Tambah enum baru setelah `billMonthStatusEnum`:**
    ```typescript
    export const transactionStatusEnum = pgEnum('transaction_status_enum', [
      'aktif',
      'void',
    ]);
    ```

    **2. Tambah tabel `transactions` setelah `billMonths`:**
    ```typescript
    export const transactions = pgTable('transactions', {
      id: bigserial('id', { mode: 'number' }).primaryKey(),
      transactionNumber: varchar('transaction_number', { length: 30 }).notNull().unique(),
      studentId: bigint('student_id', { mode: 'number' })
        .notNull()
        .references(() => students.id),
      totalAmount: integer('total_amount').notNull(),
      status: transactionStatusEnum('status').default('aktif').notNull(),
      notes: text('notes'),
      createdBy: bigint('created_by', { mode: 'number' })
        .references(() => users.id, { onDelete: 'set null' }),
      voidedAt: timestamp('voided_at'),
      voidedBy: bigint('voided_by', { mode: 'number' })
        .references(() => users.id, { onDelete: 'set null' }),
      voidReason: text('void_reason'),
      createdAt: timestamp('created_at').defaultNow().notNull(),
      updatedAt: timestamp('updated_at').defaultNow().notNull(),
    });
    ```

    **3. Tambah tabel `transactionItems` setelah `transactions`:**
    ```typescript
    export const transactionItems = pgTable('transaction_items', {
      id: bigserial('id', { mode: 'number' }).primaryKey(),
      transactionId: bigint('transaction_id', { mode: 'number' })
        .notNull()
        .references(() => transactions.id, { onDelete: 'cascade' }),
      billId: bigint('bill_id', { mode: 'number' })
        .notNull()
        .references(() => bills.id),
      billMonthId: bigint('bill_month_id', { mode: 'number' })
        .references(() => billMonths.id),
      amount: integer('amount').notNull(),
      createdAt: timestamp('created_at').defaultNow().notNull(),
    });
    ```

    **4. Tambah relations di bagian bawah file (setelah billMonthsRelations):**
    ```typescript
    export const transactionsRelations = relations(transactions, ({ one, many }) => ({
      student: one(students, { fields: [transactions.studentId], references: [students.id] }),
      createdByUser: one(users, { fields: [transactions.createdBy], references: [users.id], relationName: 'transactionCreatedBy' }),
      voidedByUser: one(users, { fields: [transactions.voidedBy], references: [users.id], relationName: 'transactionVoidedBy' }),
      items: many(transactionItems),
    }));

    export const transactionItemsRelations = relations(transactionItems, ({ one }) => ({
      transaction: one(transactions, { fields: [transactionItems.transactionId], references: [transactions.id] }),
      bill: one(bills, { fields: [transactionItems.billId], references: [bills.id] }),
      billMonth: one(billMonths, { fields: [transactionItems.billMonthId], references: [billMonths.id] }),
    }));
    ```

    PENTING: `transactionItems` di-place SETELAH `transactions` karena ada FK ke `transactions.id`.
    AVOID: Jangan ubah tabel yang sudah ada (paymentPosts, paymentTemplates, bills, billMonths).
  </action>
  <verify>
    ```bash
    pnpm --filter backend build
    # → harus sukses tanpa error

    grep "transactionStatusEnum\|transactions\|transactionItems" apps/backend/src/db/schema/financial.schema.ts | head -10
    ```
  </verify>
  <done>AC-1 satisfied: schema TypeScript valid, build bersih</done>
</task>

<task type="auto">
  <name>Task 2: Generate dan apply migration 0002</name>
  <files>apps/backend/drizzle/</files>
  <action>
    **1. Generate migration:**
    ```bash
    pnpm --filter backend db:generate
    ```
    Output: file `0002_*.sql` baru di `apps/backend/drizzle/`

    **2. Verify isi migration mengandung:**
    ```bash
    ls -t apps/backend/drizzle/*.sql | head -3
    # → 0002_*.sql harus muncul sebagai yang terbaru
    ```

    **3. Apply migration:**
    ```bash
    pnpm --filter backend db:migrate
    ```
    Output: "Migrations applied successfully" atau sejenisnya.

    PENTING: Pastikan Docker PostgreSQL (`simka-postgres`) sudah berjalan sebelum db:migrate.
    AVOID: Jangan edit file .sql secara manual — biarkan drizzle-kit yang generate.
  </action>
  <verify>
    ```bash
    # File migration ada
    ls apps/backend/drizzle/0002_*.sql

    # Migration berisi tabel yang benar
    grep "transaction_status_enum\|CREATE TABLE.*transactions\|CREATE TABLE.*transaction_items" apps/backend/drizzle/0002_*.sql
    ```
  </verify>
  <done>AC-2 dan AC-3 satisfied: migration ter-generate dan ter-apply</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `apps/backend/src/db/schema/audit.schema.ts` — audit_logs sudah ada dan lengkap
- `apps/backend/src/db/schema/auth.schema.ts` — hanya di-import, tidak dimodifikasi
- `apps/backend/src/db/schema/academic.schema.ts` — hanya di-import, tidak dimodifikasi
- `apps/backend/drizzle/0000_*.sql` dan `0001_*.sql` — migration lama tidak disentuh
- Semua tabel billing yang sudah ada (paymentPosts, paymentTemplates, bills, billMonths)

## SCOPE LIMITS
- Hanya schema + migration — belum ada service atau controller (itu Plan 03-02)
- Tidak ada perubahan pada seed.ts
- Tidak ada endpoint baru di fase ini

</boundaries>

<verification>
- [ ] `pnpm --filter backend build` sukses
- [ ] File `apps/backend/drizzle/0002_*.sql` ter-generate
- [ ] `grep "transaction_status_enum"` ada di migration file
- [ ] `grep "CREATE TABLE.*transactions"` ada di migration file
- [ ] `grep "CREATE TABLE.*transaction_items"` ada di migration file
- [ ] `pnpm --filter backend db:migrate` sukses (pastikan Docker simka-postgres berjalan)
</verification>

<success_criteria>
- Task 1 dan Task 2 completed
- TypeScript build bersih
- Migration ter-apply ke simka_db
- Schema siap digunakan oleh Plan 03-02 (Transaction Service)
</success_criteria>

<output>
Setelah completion, buat `.paul/phases/03-transaction-system/03-01-SUMMARY.md`
</output>
