---
phase: 04-reporting
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - apps/backend/src/reports/reports.service.ts
  - apps/backend/src/reports/reports.controller.ts
autonomous: false
---

<objective>
## Goal
Tambah 2 endpoint ke ReportsModule: GET /reports/tunggakan (siswa dengan tagihan belum lunas, filter kelas/unit/tahun) dan GET /reports/rekap-pos (total penerimaan per Payment Post, filter tanggal/tahun ajaran).

## Purpose
Bendahara perlu tahu siapa saja yang belum bayar (tunggakan) dan berapa total penerimaan per jenis tagihan (rekap POS). Keduanya adalah laporan operasional harian yang dibutuhkan untuk tindak lanjut.

## Output
- Method `getLaporanTunggakan()` di ReportsService
- Method `getRekapPos()` di ReportsService
- Endpoint GET /reports/tunggakan di ReportsController
- Endpoint GET /reports/rekap-pos di ReportsController
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Prior Work
@.paul/phases/04-reporting/04-01-SUMMARY.md

## Source Files
@apps/backend/src/reports/reports.service.ts
@apps/backend/src/reports/reports.controller.ts
@apps/backend/src/db/schema/financial.schema.ts
@apps/backend/src/db/schema/academic.schema.ts
</context>

<acceptance_criteria>

## AC-1: Laporan tunggakan mengembalikan siswa dengan tagihan belum lunas
```gherkin
Given ada bills dengan status 'belum_bayar' atau 'cicilan'
When GET /reports/tunggakan?schoolYearId=1
Then response berisi array siswa dengan field: studentId, studentName, nis, className, totalTunggakan, jumlahTagihan
And totalTunggakan adalah SUM amount dari bills belum_bayar/cicilan siswa tersebut
And optional filter: classId, unitId
```

## AC-2: Filter tunggakan berjalan benar
```gherkin
Given ada siswa dari berbagai kelas
When GET /reports/tunggakan?schoolYearId=1&classId=2
Then hanya siswa dari classId=2 yang muncul
When GET /reports/tunggakan?schoolYearId=1&unitId=1
Then hanya siswa dari unit sekolah unitId=1 yang muncul
```

## AC-3: Rekap POS mengembalikan total per payment post
```gherkin
Given ada transaksi aktif yang sudah dibayar
When GET /reports/rekap-pos
Then response berisi array per POS: posId, posCode, posName, totalPenerimaan, jumlahTransaksi, jumlahSiswa
And hanya transaksi status='aktif' yang dihitung
And optional filter: dateFrom, dateTo, schoolYearId
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Laporan Tunggakan — getLaporanTunggakan()</name>
  <files>
    apps/backend/src/reports/reports.service.ts,
    apps/backend/src/reports/reports.controller.ts
  </files>
  <action>
    **Tambah method ke ReportsService:**

    Import tambahan yang dibutuhkan di reports.service.ts:
    ```typescript
    import { inArray } from 'drizzle-orm';
    import { bills, paymentTemplates } from '../db/schema/financial.schema';
    import { classes, schoolUnits, studentClasses } from '../db/schema/academic.schema';
    ```

    Method `getLaporanTunggakan`:
    ```typescript
    async getLaporanTunggakan(filters: {
      schoolYearId?: number;
      classId?: number;
      unitId?: number;
    } = {}) {
      const conditions: any[] = [
        inArray(bills.status, ['belum_bayar', 'cicilan']),
      ];

      if (filters.schoolYearId) {
        conditions.push(eq(bills.schoolYearId, filters.schoolYearId));
      }
      if (filters.classId) {
        conditions.push(eq(paymentTemplates.classId, filters.classId));
      }
      if (filters.unitId) {
        conditions.push(eq(classes.schoolUnitId, filters.unitId));
      }

      const rows = await this.db
        .select({
          studentId: students.id,
          studentName: students.name,
          nis: students.nis,
          className: classes.name,
          billId: bills.id,
          billAmount: bills.amount,
          billStatus: bills.status,
        })
        .from(bills)
        .innerJoin(students, eq(bills.studentId, students.id))
        .innerJoin(paymentTemplates, eq(bills.paymentTemplateId, paymentTemplates.id))
        .innerJoin(classes, eq(paymentTemplates.classId, classes.id))
        .innerJoin(schoolUnits, eq(classes.schoolUnitId, schoolUnits.id))
        .where(and(...conditions))
        .orderBy(students.name);

      // Agregasi per siswa di aplikasi layer
      const byStudent = new Map<number, {
        studentId: number;
        studentName: string;
        nis: string;
        className: string;
        totalTunggakan: number;
        jumlahTagihan: number;
      }>();

      for (const row of rows) {
        const existing = byStudent.get(row.studentId);
        if (existing) {
          existing.totalTunggakan += row.billAmount;
          existing.jumlahTagihan += 1;
        } else {
          byStudent.set(row.studentId, {
            studentId: row.studentId,
            studentName: row.studentName,
            nis: row.nis,
            className: row.className,
            totalTunggakan: row.billAmount,
            jumlahTagihan: 1,
          });
        }
      }

      return {
        total: byStudent.size,
        siswa: Array.from(byStudent.values()),
      };
    }
    ```

    **Tambah endpoint ke ReportsController:**
    ```typescript
    @Get('tunggakan')
    @RequirePermissions('report.view')
    getLaporanTunggakan(
      @Query('schoolYearId') schoolYearId?: string,
      @Query('classId') classId?: string,
      @Query('unitId') unitId?: string,
    ) {
      return this.service.getLaporanTunggakan({
        schoolYearId: schoolYearId ? Number(schoolYearId) : undefined,
        classId: classId ? Number(classId) : undefined,
        unitId: unitId ? Number(unitId) : undefined,
      });
    }
    ```

    PENTING: Route `@Get('tunggakan')` harus ditempatkan SEBELUM route param lain di controller untuk menghindari ambiguitas.
    AVOID: Jangan delegate ke BillsService — query ini di ReportsService untuk konsistensi.
  </action>
  <verify>
    ```bash
    pnpm --filter backend build
    # → sukses

    curl -s "http://localhost:3000/api/reports/tunggakan?schoolYearId=1" \
      -H "Authorization: Bearer $TOKEN"
    # → { total: N, siswa: [...] }
    ```
  </verify>
  <done>AC-1, AC-2 satisfied: endpoint tunggakan return array siswa dengan totalTunggakan dan filter berjalan</done>
</task>

<task type="auto">
  <name>Task 2: Rekap POS — getRekapPos()</name>
  <files>
    apps/backend/src/reports/reports.service.ts,
    apps/backend/src/reports/reports.controller.ts
  </files>
  <action>
    **Tambah import tambahan ke reports.service.ts (jika belum ada):**
    ```typescript
    import { transactionItems } from '../db/schema/financial.schema';
    ```

    **Method `getRekapPos`:**
    ```typescript
    async getRekapPos(filters: {
      dateFrom?: string;
      dateTo?: string;
      schoolYearId?: number;
    } = {}) {
      const conditions: any[] = [eq(transactions.status, 'aktif')];

      if (filters.dateFrom) {
        conditions.push(gte(transactions.createdAt, new Date(filters.dateFrom + 'T00:00:00')));
      }
      if (filters.dateTo) {
        conditions.push(lte(transactions.createdAt, new Date(filters.dateTo + 'T23:59:59')));
      }
      if (filters.schoolYearId) {
        conditions.push(eq(bills.schoolYearId, filters.schoolYearId));
      }

      const rows = await this.db
        .select({
          posId: paymentPosts.id,
          posCode: paymentPosts.code,
          posName: paymentPosts.name,
          totalPenerimaan: sum(transactionItems.amount),
          jumlahTransaksi: countDistinct(transactions.id),
          jumlahSiswa: countDistinct(transactions.studentId),
        })
        .from(transactions)
        .innerJoin(transactionItems, eq(transactionItems.transactionId, transactions.id))
        .innerJoin(bills, eq(bills.id, transactionItems.billId))
        .innerJoin(paymentTemplates, eq(paymentTemplates.id, bills.paymentTemplateId))
        .innerJoin(paymentPosts, eq(paymentPosts.id, paymentTemplates.paymentPostId))
        .where(and(...conditions))
        .groupBy(paymentPosts.id, paymentPosts.code, paymentPosts.name)
        .orderBy(paymentPosts.code);

      return {
        total: rows.length,
        perPos: rows.map((r) => ({
          posId: r.posId,
          posCode: r.posCode,
          posName: r.posName,
          totalPenerimaan: Number(r.totalPenerimaan ?? 0),
          jumlahTransaksi: r.jumlahTransaksi,
          jumlahSiswa: r.jumlahSiswa,
        })),
      };
    }
    ```

    **Tambah endpoint ke ReportsController:**
    ```typescript
    @Get('rekap-pos')
    @RequirePermissions('report.view')
    getRekapPos(
      @Query('dateFrom') dateFrom?: string,
      @Query('dateTo') dateTo?: string,
      @Query('schoolYearId') schoolYearId?: string,
    ) {
      return this.service.getRekapPos({
        dateFrom,
        dateTo,
        schoolYearId: schoolYearId ? Number(schoolYearId) : undefined,
      });
    }
    ```

    AVOID: Jangan ubah method yang sudah ada (getDashboardStats, getLaporanHarian, getLaporanBulanan, getLaporanTahunan).
  </action>
  <verify>
    ```bash
    pnpm --filter backend build
    # → sukses

    curl -s "http://localhost:3000/api/reports/rekap-pos" \
      -H "Authorization: Bearer $TOKEN"
    # → { total: N, perPos: [{ posId, posCode, posName, totalPenerimaan, jumlahTransaksi, jumlahSiswa }] }
    ```
  </verify>
  <done>AC-3 satisfied: rekap-pos return total per POS hanya dari transaksi aktif, filter dateFrom/dateTo/schoolYearId berjalan</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    2 endpoint laporan operasional:
    - GET /reports/tunggakan?schoolYearId=1[&classId=N][&unitId=N]
      → { total, siswa: [{ studentId, studentName, nis, className, totalTunggakan, jumlahTagihan }] }
    - GET /reports/rekap-pos[?dateFrom=&dateTo=&schoolYearId=N]
      → { total, perPos: [{ posId, posCode, posName, totalPenerimaan, jumlahTransaksi, jumlahSiswa }] }
  </what-built>
  <how-to-verify>
    Server sudah jalan dari sebelumnya. Jalankan:

    1. Test tunggakan (semua tahun ajaran schoolYearId=1):
       ```bash
       curl -s "http://localhost:3000/api/reports/tunggakan?schoolYearId=1" \
         -H "Authorization: Bearer $TOKEN"
       ```
       → siswa[] berisi siswa yang masih punya tunggakan

    2. Test rekap POS (semua transaksi):
       ```bash
       curl -s "http://localhost:3000/api/reports/rekap-pos" \
         -H "Authorization: Bearer $TOKEN"
       ```
       → perPos[] berisi breakdown per jenis pembayaran

    3. Test rekap POS dengan filter tanggal:
       ```bash
       curl -s "http://localhost:3000/api/reports/rekap-pos?dateFrom=2026-05-01&dateTo=2026-05-31" \
         -H "Authorization: Bearer $TOKEN"
       ```
       → hasil sama/subset dari test 2
  </how-to-verify>
  <resume-signal>Ketik "approved" jika data masuk akal, atau deskripsikan masalah</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `apps/backend/src/db/schema/` — tidak ada perubahan schema
- `apps/backend/src/billing/` — tidak disentuh
- `apps/backend/src/transactions/` — tidak disentuh
- Method yang sudah ada di reports.service.ts (getDashboardStats, getLaporanHarian, getLaporanBulanan, getLaporanTahunan)

## SCOPE LIMITS
- Plan ini hanya backend API — tidak ada frontend
- Export Excel/PDF dihandle di Plan 04-03 dan 04-04
- Tidak perlu pagination — volume data sekolah kecil (< 1000 siswa)

</boundaries>

<verification>
- [ ] `pnpm --filter backend build` sukses
- [ ] GET /reports/tunggakan → 200 dengan array siswa berisi totalTunggakan
- [ ] GET /reports/tunggakan?classId=N → hanya siswa dari kelas N
- [ ] GET /reports/rekap-pos → 200 dengan array per POS
- [ ] GET /reports/rekap-pos?dateFrom=...&dateTo=... → hasil terfilter
</verification>

<success_criteria>
- Task 1 dan 2 completed
- TypeScript build bersih
- 2 endpoint aktif dan return data yang benar
- Checkpoint human-verify approved
</success_criteria>

<output>
Setelah completion, buat `.paul/phases/04-reporting/04-02-SUMMARY.md`
</output>
