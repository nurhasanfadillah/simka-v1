---
phase: 04-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/app.module.ts
  - apps/backend/src/reports/reports.module.ts
  - apps/backend/src/reports/reports.controller.ts
  - apps/backend/src/reports/reports.service.ts
autonomous: false
---

<objective>
## Goal
Buat ReportingModule dengan 4 endpoint: GET /reports/dashboard (3 stats agregat), GET /reports/harian (daftar transaksi + total per hari), GET /reports/bulanan (rekap per hari dalam satu bulan), dan GET /reports/tahunan (rekap per bulan dalam satu tahun ajaran).

## Purpose
Bendahara perlu melihat ringkasan keuangan harian/bulanan/tahunan. Dashboard stats memberikan snapshot cepat. Laporan keuangan dengan filter lengkap menggantikan pencatatan manual di buku kas.

## Output
- `apps/backend/src/reports/reports.module.ts` — NestJS module
- `apps/backend/src/reports/reports.controller.ts` — 4 endpoint dengan @RequirePermissions
- `apps/backend/src/reports/reports.service.ts` — query agregasi Drizzle
- `apps/backend/src/app.module.ts` — ReportsModule terdaftar
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Prior Work
@.paul/phases/03-transaction-system/03-02-SUMMARY.md

## Source Files
@apps/backend/src/app.module.ts
@apps/backend/src/transactions/transactions.service.ts
@apps/backend/src/db/schema/financial.schema.ts
@apps/backend/src/db/schema/academic.schema.ts
</context>

<acceptance_criteria>

## AC-1: Dashboard stats mengembalikan 3 angka yang akurat
```gherkin
Given ada data transaksi aktif di bulan berjalan
When GET /reports/dashboard dengan Authorization header valid
Then response berisi:
  - totalSiswaAktif: jumlah students WHERE studentStatus='aktif'
  - penerimaanBulanIni: SUM totalAmount WHERE status='aktif' AND bulan=sekarang
  - pembayarBulanIni: COUNT DISTINCT studentId WHERE status='aktif' AND bulan=sekarang
And response 200 JSON
```

## AC-2: Laporan harian mengembalikan daftar transaksi per hari
```gherkin
Given ada transaksi pada tanggal tertentu
When GET /reports/harian?date=2026-05-29
Then response berisi: date, totalPenerimaan, jumlahTransaksi, dan array transactions
And setiap transaksi punya: id, transactionNumber, studentName, nis, totalAmount, createdAt
And hanya transaksi status='aktif' yang dihitung di totalPenerimaan
```

## AC-3: Laporan bulanan mengembalikan rekap per hari
```gherkin
Given ada transaksi di bulan Mei 2026
When GET /reports/bulanan?month=5&year=2026
Then response berisi: month, year, totalPenerimaan, jumlahTransaksi, array perHari
And perHari: [{ date, totalPenerimaan, jumlahTransaksi }] hanya hari yang ada transaksi
And optional filter: classId, unitId (filter via student_classes JOIN)
```

## AC-4: Laporan tahunan mengembalikan rekap per bulan
```gherkin
Given schoolYearId valid
When GET /reports/tahunan?schoolYearId=1
Then response berisi: schoolYearId, totalPenerimaan, jumlahTransaksi, array perBulan
And perBulan: [{ month, year, totalPenerimaan, jumlahTransaksi }] dari bulan yang ada transaksi
And hanya transaksi status='aktif'
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: ReportingModule scaffolding + Dashboard Stats</name>
  <files>
    apps/backend/src/reports/reports.module.ts,
    apps/backend/src/reports/reports.controller.ts,
    apps/backend/src/reports/reports.service.ts,
    apps/backend/src/app.module.ts
  </files>
  <action>
    **1. Buat `apps/backend/src/reports/reports.service.ts`:**

    ```typescript
    import { Inject, Injectable } from '@nestjs/common';
    import { and, count, countDistinct, eq, gte, lte, sql, sum } from 'drizzle-orm';
    import { DRIZZLE } from '../db/db.module';
    import type { NodePgDatabase } from 'drizzle-orm/node-postgres';
    import type * as schema from '../db/schema';
    import { students, studentClasses, classes } from '../db/schema/academic.schema';
    import { transactions, transactionItems, paymentPosts } from '../db/schema/financial.schema';

    @Injectable()
    export class ReportsService {
      constructor(@Inject(DRIZZLE) private db: NodePgDatabase<typeof schema>) {}

      async getDashboardStats() {
        const now = new Date();
        const startOfMonth = new Date(now.getFullYear(), now.getMonth(), 1);
        const endOfMonth = new Date(now.getFullYear(), now.getMonth() + 1, 0, 23, 59, 59);

        const [siswaResult] = await this.db
          .select({ total: count() })
          .from(students)
          .where(eq(students.studentStatus, 'aktif'));

        const [penerimaanResult] = await this.db
          .select({ total: sum(transactions.totalAmount) })
          .from(transactions)
          .where(
            and(
              eq(transactions.status, 'aktif'),
              gte(transactions.createdAt, startOfMonth),
              lte(transactions.createdAt, endOfMonth),
            ),
          );

        const [pembayarResult] = await this.db
          .select({ total: countDistinct(transactions.studentId) })
          .from(transactions)
          .where(
            and(
              eq(transactions.status, 'aktif'),
              gte(transactions.createdAt, startOfMonth),
              lte(transactions.createdAt, endOfMonth),
            ),
          );

        return {
          totalSiswaAktif: siswaResult.total,
          penerimaanBulanIni: Number(penerimaanResult.total ?? 0),
          pembayarBulanIni: pembayarResult.total,
        };
      }
    }
    ```

    **2. Buat `apps/backend/src/reports/reports.controller.ts`:**

    ```typescript
    import { Controller, Get } from '@nestjs/common';
    import { ReportsService } from './reports.service';
    import { RequirePermissions } from '../auth/decorators/require-permissions.decorator';

    @Controller('reports')
    export class ReportsController {
      constructor(private readonly service: ReportsService) {}

      @Get('dashboard')
      @RequirePermissions('report.view')
      getDashboard() {
        return this.service.getDashboardStats();
      }
    }
    ```

    **3. Buat `apps/backend/src/reports/reports.module.ts`:**

    ```typescript
    import { Module } from '@nestjs/common';
    import { ReportsController } from './reports.controller';
    import { ReportsService } from './reports.service';

    @Module({
      controllers: [ReportsController],
      providers: [ReportsService],
    })
    export class ReportsModule {}
    ```

    **4. Tambah ReportsModule ke `apps/backend/src/app.module.ts`:**
    ```typescript
    import { ReportsModule } from './reports/reports.module';
    // tambah di imports array setelah TransactionsModule:
    ReportsModule,
    ```

    AVOID: Jangan buat DatabaseModule baru — inject DRIZZLE token langsung seperti TransactionsService.
    AVOID: Jangan pakai sql`raw` untuk query sederhana yang bisa pakai helper Drizzle.
  </action>
  <verify>
    ```bash
    pnpm --filter backend build
    # → sukses, tidak ada error TypeScript
    ```
  </verify>
  <done>ReportsModule terdaftar di AppModule, getDashboardStats() terdefinisi, build bersih</done>
</task>

<task type="auto">
  <name>Task 2: Endpoint Laporan Harian, Bulanan, Tahunan</name>
  <files>
    apps/backend/src/reports/reports.service.ts,
    apps/backend/src/reports/reports.controller.ts
  </files>
  <action>
    **Tambah 3 method ke ReportsService:**

    **Method getLaporanHarian(date: string):**
    ```typescript
    async getLaporanHarian(date: string) {
      const start = new Date(date + 'T00:00:00');
      const end = new Date(date + 'T23:59:59');

      const rows = await this.db
        .select({
          id: transactions.id,
          transactionNumber: transactions.transactionNumber,
          studentName: students.name,
          nis: students.nis,
          totalAmount: transactions.totalAmount,
          status: transactions.status,
          createdAt: transactions.createdAt,
        })
        .from(transactions)
        .innerJoin(students, eq(transactions.studentId, students.id))
        .where(and(gte(transactions.createdAt, start), lte(transactions.createdAt, end)))
        .orderBy(transactions.createdAt);

      const aktifRows = rows.filter((r) => r.status === 'aktif');
      const totalPenerimaan = aktifRows.reduce((s, r) => s + r.totalAmount, 0);

      return {
        date,
        totalPenerimaan,
        jumlahTransaksi: aktifRows.length,
        transactions: rows,
      };
    }
    ```

    **Method getLaporanBulanan(month: number, year: number, classId?: number, unitId?: number):**
    ```typescript
    async getLaporanBulanan(month: number, year: number, classId?: number, unitId?: number) {
      const start = new Date(year, month - 1, 1);
      const end = new Date(year, month, 0, 23, 59, 59);

      const conditions: SQL[] = [
        eq(transactions.status, 'aktif'),
        gte(transactions.createdAt, start),
        lte(transactions.createdAt, end),
      ];

      let query = this.db
        .select({
          date: sql<string>`DATE(${transactions.createdAt})`.as('date'),
          totalPenerimaan: sum(transactions.totalAmount),
          jumlahTransaksi: count(),
        })
        .from(transactions)
        .innerJoin(students, eq(transactions.studentId, students.id));

      if (classId || unitId) {
        query = query.innerJoin(
          studentClasses,
          and(
            eq(studentClasses.studentId, students.id),
            eq(studentClasses.schoolYearId, transactions.id), // akan di-override di where
          ),
        ) as typeof query;
      }

      // Simpler approach: filter via subquery if classId/unitId present
      // Jika classId: tambah kondisi pada studentClasses join
      // Untuk simplisitas plan ini: filter classId/unitId via correlated check
      if (classId) conditions.push(
        sql`EXISTS (SELECT 1 FROM student_classes sc WHERE sc.student_id = ${transactions.studentId} AND sc.class_id = ${classId})`
      );
      if (unitId) conditions.push(
        sql`EXISTS (SELECT 1 FROM student_classes sc2 INNER JOIN classes cl ON cl.id = sc2.class_id WHERE sc2.student_id = ${transactions.studentId} AND cl.school_unit_id = ${unitId})`
      );

      const rows = await this.db
        .select({
          date: sql<string>`DATE(${transactions.createdAt})`.as('date'),
          totalPenerimaan: sum(transactions.totalAmount),
          jumlahTransaksi: count(),
        })
        .from(transactions)
        .innerJoin(students, eq(transactions.studentId, students.id))
        .where(and(...conditions))
        .groupBy(sql`DATE(${transactions.createdAt})`)
        .orderBy(sql`DATE(${transactions.createdAt})`);

      const totalPenerimaan = rows.reduce((s, r) => s + Number(r.totalPenerimaan ?? 0), 0);
      const jumlahTransaksi = rows.reduce((s, r) => s + r.jumlahTransaksi, 0);

      return {
        month,
        year,
        totalPenerimaan,
        jumlahTransaksi,
        perHari: rows.map((r) => ({
          date: r.date,
          totalPenerimaan: Number(r.totalPenerimaan ?? 0),
          jumlahTransaksi: r.jumlahTransaksi,
        })),
      };
    }
    ```

    **Method getLaporanTahunan(schoolYearId: number):**
    ```typescript
    async getLaporanTahunan(schoolYearId: number) {
      // Filter: transaksi dari siswa yang terdaftar di schoolYearId ini
      const rows = await this.db
        .select({
          month: sql<number>`EXTRACT(MONTH FROM ${transactions.createdAt})::int`.as('month'),
          year: sql<number>`EXTRACT(YEAR FROM ${transactions.createdAt})::int`.as('year'),
          totalPenerimaan: sum(transactions.totalAmount),
          jumlahTransaksi: count(),
        })
        .from(transactions)
        .innerJoin(students, eq(transactions.studentId, students.id))
        .where(
          and(
            eq(transactions.status, 'aktif'),
            sql`EXISTS (SELECT 1 FROM student_classes sc WHERE sc.student_id = ${transactions.studentId} AND sc.school_year_id = ${schoolYearId})`,
          ),
        )
        .groupBy(
          sql`EXTRACT(YEAR FROM ${transactions.createdAt})`,
          sql`EXTRACT(MONTH FROM ${transactions.createdAt})`,
        )
        .orderBy(
          sql`EXTRACT(YEAR FROM ${transactions.createdAt})`,
          sql`EXTRACT(MONTH FROM ${transactions.createdAt})`,
        );

      const totalPenerimaan = rows.reduce((s, r) => s + Number(r.totalPenerimaan ?? 0), 0);
      const jumlahTransaksi = rows.reduce((s, r) => s + r.jumlahTransaksi, 0);

      return {
        schoolYearId,
        totalPenerimaan,
        jumlahTransaksi,
        perBulan: rows.map((r) => ({
          month: r.month,
          year: r.year,
          totalPenerimaan: Number(r.totalPenerimaan ?? 0),
          jumlahTransaksi: r.jumlahTransaksi,
        })),
      };
    }
    ```

    **Tambah 3 endpoint ke ReportsController:**
    ```typescript
    import { Query, ParseIntPipe } from '@nestjs/common';

    @Get('harian')
    @RequirePermissions('report.view')
    getLaporanHarian(@Query('date') date: string) {
      return this.service.getLaporanHarian(date ?? new Date().toISOString().slice(0, 10));
    }

    @Get('bulanan')
    @RequirePermissions('report.view')
    getLaporanBulanan(
      @Query('month', ParseIntPipe) month: number,
      @Query('year', ParseIntPipe) year: number,
      @Query('classId') classId?: string,
      @Query('unitId') unitId?: string,
    ) {
      return this.service.getLaporanBulanan(
        month,
        year,
        classId ? Number(classId) : undefined,
        unitId ? Number(unitId) : undefined,
      );
    }

    @Get('tahunan')
    @RequirePermissions('report.view')
    getLaporanTahunan(@Query('schoolYearId', ParseIntPipe) schoolYearId: number) {
      return this.service.getLaporanTahunan(schoolYearId);
    }
    ```

    PENTING: Import `SQL` type dari `drizzle-orm` untuk type annotation conditions array.
    AVOID: Jangan hapus method getDashboardStats() atau endpoint dashboard.
    AVOID: Jangan filter void transactions di laporan total — filter sudah dilakukan di query (status='aktif' only).
  </action>
  <verify>
    ```bash
    pnpm --filter backend build
    # → sukses tanpa error

    # Smoke test (server harus jalan):
    TOKEN=$(curl -s -X POST http://localhost:3000/api/auth/login \
      -H "Content-Type: application/json" \
      -d '{"email":"admin@alhasaniyyah.sch.id","password":"Admin123!"}' \
      | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')

    curl -s "http://localhost:3000/api/reports/dashboard" \
      -H "Authorization: Bearer $TOKEN" | python3 -m json.tool

    curl -s "http://localhost:3000/api/reports/harian?date=2026-05-29" \
      -H "Authorization: Bearer $TOKEN" | python3 -m json.tool
    ```
  </verify>
  <done>AC-2, AC-3, AC-4 satisfied: 3 endpoint laporan return data terstruktur dengan benar</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    4 endpoint reporting:
    - GET /reports/dashboard → { totalSiswaAktif, penerimaanBulanIni, pembayarBulanIni }
    - GET /reports/harian?date=YYYY-MM-DD → { date, totalPenerimaan, jumlahTransaksi, transactions[] }
    - GET /reports/bulanan?month=M&year=YYYY[&classId=N][&unitId=N] → { perHari[] }
    - GET /reports/tahunan?schoolYearId=N → { perBulan[] }
  </what-built>
  <how-to-verify>
    1. Jalankan server: `pnpm dev`
    2. Test dashboard:
       ```bash
       curl -s "http://localhost:3000/api/reports/dashboard" \
         -H "Authorization: Bearer $TOKEN"
       ```
       → pastikan 3 field ada dan angkanya masuk akal (totalSiswaAktif > 0 jika ada data seed)

    3. Test laporan harian dengan tanggal yang ada data:
       ```bash
       curl -s "http://localhost:3000/api/reports/harian?date=2026-05-29" \
         -H "Authorization: Bearer $TOKEN"
       ```
       → transactions[] tidak kosong jika ada transaksi di tanggal itu

    4. Test laporan bulanan:
       ```bash
       curl -s "http://localhost:3000/api/reports/bulanan?month=5&year=2026" \
         -H "Authorization: Bearer $TOKEN"
       ```
       → perHari[] berisi rekap per hari

    5. Test laporan tahunan:
       ```bash
       curl -s "http://localhost:3000/api/reports/tahunan?schoolYearId=1" \
         -H "Authorization: Bearer $TOKEN"
       ```
       → perBulan[] terisi
  </how-to-verify>
  <resume-signal>Ketik "approved" jika semua endpoint return data valid, atau deskripsikan masalah</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `apps/backend/src/db/schema/` — tidak ada perubahan schema
- `apps/backend/drizzle/` — tidak ada migration baru
- `apps/backend/src/transactions/` — tidak disentuh
- `apps/backend/src/billing/` — tidak disentuh
- `apps/backend/src/auth/` — tidak disentuh

## SCOPE LIMITS
- Plan ini hanya backend API — tidak ada frontend
- Export Excel/PDF dihandle di Plan 04-03 dan 04-04
- Laporan tunggakan dan rekap POS dihandle di Plan 04-02
- Permission 'report.view' diasumsikan sudah ada di seed — jangan buat migration baru

</boundaries>

<verification>
- [ ] `pnpm --filter backend build` sukses
- [ ] GET /reports/dashboard → 200 JSON dengan 3 field
- [ ] GET /reports/harian?date=... → 200 JSON dengan transactions array
- [ ] GET /reports/bulanan?month=5&year=2026 → 200 JSON dengan perHari array
- [ ] GET /reports/tahunan?schoolYearId=1 → 200 JSON dengan perBulan array
- [ ] Tanpa auth → 401 (guard aktif)
</verification>

<success_criteria>
- Task 1 dan 2 completed
- TypeScript build bersih
- 4 endpoint aktif dan return data yang benar
- Checkpoint human-verify approved
</success_criteria>

<output>
Setelah completion, buat `.paul/phases/04-reporting/04-01-SUMMARY.md`
</output>
