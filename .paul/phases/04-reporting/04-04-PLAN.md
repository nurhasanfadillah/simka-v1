---
phase: 04-reporting
plan: 04
type: execute
wave: 1
depends_on: ["04-03"]
files_modified:
  - apps/backend/src/transactions/pdf/pdf.service.ts
  - apps/backend/src/reports/pdf-reports.service.ts
  - apps/backend/src/reports/reports.controller.ts
  - apps/backend/src/reports/reports.module.ts
autonomous: true
---

<objective>
## Goal
Tambah export PDF untuk 4 laporan (harian, bulanan, tunggakan, rekap-pos) dengan reuse PdfService dari Phase 03, menggunakan format A4 portrait.

## Purpose
Bendahara butuh laporan dalam format PDF untuk pengarsipan resmi, print, dan laporan ke yayasan. Ini deliverable terakhir Phase 04 sebelum milestone v1.0 complete.

## Output
- `apps/backend/src/transactions/pdf/pdf.service.ts` — tambah optional `options` param untuk support A4
- `apps/backend/src/reports/pdf-reports.service.ts` — service baru: HTML template + 4 method generate PDF
- `reports.controller.ts` — 4 endpoint GET baru: `/harian/export/pdf`, `/bulanan/export/pdf`, `/tunggakan/export/pdf`, `/rekap-pos/export/pdf`
- `reports.module.ts` — PdfReportsService + PdfService didaftarkan sebagai provider
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/04-reporting/04-03-SUMMARY.md

## Source Files
@apps/backend/src/transactions/pdf/pdf.service.ts
@apps/backend/src/reports/reports.service.ts
@apps/backend/src/reports/reports.controller.ts
@apps/backend/src/reports/reports.module.ts
</context>

<acceptance_criteria>

## AC-1: Export Laporan Harian → File PDF
```gherkin
Given server berjalan dan user terautentikasi
When GET /api/reports/harian/export/pdf?date=2026-05-29
Then response Content-Type: application/pdf
  AND file PDF berisi tabel transaksi hari tersebut dengan kolom: No Transaksi, Nama Siswa, NIS, Total, Status, Waktu
  AND header halaman berisi: "Laporan Harian - [tanggal]" + nama sekolah "Al-Hasaniyyah"
  AND footer berisi total penerimaan
```

## AC-2: Export Laporan Bulanan → File PDF
```gherkin
Given server berjalan dan user terautentikasi
When GET /api/reports/bulanan/export/pdf?month=5&year=2026
Then response adalah file PDF dengan tabel per hari: Tanggal, Total Penerimaan, Jumlah Transaksi
  AND header: "Laporan Bulanan - [Bulan Tahun]"
  AND ringkasan total di akhir
```

## AC-3: Export Tunggakan → File PDF
```gherkin
Given server berjalan dan user terautentikasi
When GET /api/reports/tunggakan/export/pdf
Then response adalah file PDF dengan tabel siswa tunggakan: Nama Siswa, NIS, Kelas, Total Tunggakan, Jumlah Tagihan
  AND header: "Laporan Tunggakan"
  AND total jumlah siswa tunggakan di akhir
```

## AC-4: Export Rekap POS → File PDF
```gherkin
Given server berjalan dan user terautentikasi
When GET /api/reports/rekap-pos/export/pdf
Then response adalah file PDF dengan tabel per POS: Kode, Nama POS, Total Penerimaan, Jml Transaksi, Jml Siswa
  AND header: "Rekap POS Pembayaran"
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extend PdfService untuk support optional page format</name>
  <files>apps/backend/src/transactions/pdf/pdf.service.ts</files>
  <action>
    Tambah optional parameter `options` ke `generateFromHtml()`:

    ```typescript
    interface PdfOptions {
      format?: 'A4' | 'A5';
      landscape?: boolean;
    }

    async generateFromHtml(html: string, options: PdfOptions = {}): Promise<Buffer> {
      const { format = 'A5', landscape = false } = options;
      // ...
      const pdf = await page.pdf({
        format,
        landscape,
        printBackground: true,
        margin: { top: '15mm', right: '15mm', bottom: '15mm', left: '15mm' },
      });
    }
    ```

    Default tetap A5 landscape=false agar kwitansi yang sudah ada (Phase 03) tidak terpengaruh.
    Pastikan TypeScript compile bersih setelah perubahan ini.
  </action>
  <verify>
    cd apps/backend && npx tsc --noEmit
    (tidak ada error baru di pdf.service.ts atau transactions/)
  </verify>
  <done>AC-1, AC-2, AC-3, AC-4 partially: PdfService bisa generate A4 tanpa breaking kwitansi A5</done>
</task>

<task type="auto">
  <name>Task 2: Buat PdfReportsService dengan 4 HTML template dan 4 method generate</name>
  <files>apps/backend/src/reports/pdf-reports.service.ts</files>
  <action>
    Buat file baru `apps/backend/src/reports/pdf-reports.service.ts`.

    Import: `Injectable`, `PdfService` dari transactions, `ReportsService`.

    Implement 4 method — masing-masing return `Promise<Buffer>`:

    **generateHarian(date: string): Promise<Buffer>**
    - Panggil `this.reportsService.getLaporanHarian(date)`
    - Build HTML dengan struktur:
      ```html
      <!DOCTYPE html>
      <html>
      <head><meta charset="UTF-8">
      <style>
        body { font-family: Arial, sans-serif; font-size: 11px; color: #1a1a1a; }
        .header { text-align: center; border-bottom: 2px solid #1A3829; padding-bottom: 8px; margin-bottom: 16px; }
        .header h1 { color: #1A3829; font-size: 16px; margin: 0; }
        .header h2 { font-size: 12px; margin: 4px 0 0; font-weight: normal; }
        table { width: 100%; border-collapse: collapse; }
        th { background: #1A3829; color: white; padding: 6px 8px; text-align: left; font-size: 10px; }
        td { padding: 5px 8px; border-bottom: 1px solid #e0e0e0; }
        tr:nth-child(even) { background: #f5f5f5; }
        .total-row { font-weight: bold; border-top: 2px solid #1A3829; }
        .footer { margin-top: 12px; text-align: right; font-size: 10px; color: #666; }
        .amount { text-align: right; }
      </style>
      </head>
      <body>
        <div class="header">
          <h1>Laporan Harian</h1>
          <h2>Yayasan Al-Hasaniyyah | Tanggal: ${date}</h2>
        </div>
        <table>
          <tr><th>No Transaksi</th><th>Nama Siswa</th><th>NIS</th><th class="amount">Total</th><th>Status</th><th>Waktu</th></tr>
          ${rows}
          <tr class="total-row"><td colspan="3">TOTAL PENERIMAAN</td><td class="amount">${formatRupiah(result.totalPenerimaan)}</td><td colspan="2">${result.jumlahTransaksi} transaksi aktif</td></tr>
        </table>
        <div class="footer">Dicetak: ${new Date().toLocaleString('id-ID')}</div>
      </body>
      </html>
      ```
    - Panggil `this.pdfService.generateFromHtml(html, { format: 'A4' })`

    **generateBulanan(month, year, classId?, unitId?): Promise<Buffer>**
    - Panggil `this.reportsService.getLaporanBulanan(month, year, classId, unitId)`
    - HTML: header "Laporan Bulanan - [nama bulan] [year]", tabel Tanggal | Total Penerimaan | Jml Transaksi, baris total
    - Nama bulan: array `['Januari','Februari',...,'Desember'][month-1]`
    - Format A4

    **generateTunggakan(filters): Promise<Buffer>**
    - Panggil `this.reportsService.getLaporanTunggakan(filters)`
    - HTML: header "Laporan Tunggakan", tabel No | Nama Siswa | NIS | Kelas | Total Tunggakan | Jml Tagihan, baris total siswa
    - Format A4

    **generateRekapPos(filters): Promise<Buffer>**
    - Panggil `this.reportsService.getRekapPos(filters)`
    - HTML: header "Rekap POS Pembayaran", tabel Kode POS | Nama POS | Total Penerimaan | Jml Transaksi | Jml Siswa
    - Format A4

    **Helper formatRupiah** (private method):
    ```typescript
    private formatRupiah(amount: number): string {
      return new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', minimumFractionDigits: 0 }).format(amount);
    }
    ```

    Dekorasi class: `@Injectable()`.
  </action>
  <verify>
    cd apps/backend && npx tsc --noEmit
    (tidak ada TypeScript error di pdf-reports.service.ts)
  </verify>
  <done>AC-1, AC-2, AC-3, AC-4 partially: PdfReportsService dengan 4 method compile bersih</done>
</task>

<task type="auto">
  <name>Task 3: Tambah 4 endpoint PDF ke controller dan daftarkan services di module</name>
  <files>
    apps/backend/src/reports/reports.controller.ts,
    apps/backend/src/reports/reports.module.ts
  </files>
  <action>
    **reports.controller.ts:**
    - Import `PdfReportsService`
    - Inject via constructor: `private readonly pdfReportsService: PdfReportsService`
    - Helper untuk set PDF response headers:
      ```typescript
      private setPdfHeaders(res: Response, filename: string) {
        res.setHeader('Content-Type', 'application/pdf');
        res.setHeader('Content-Disposition', `attachment; filename="${filename}"`);
      }
      ```

    Tambah 4 endpoint baru (tempatkan SEBELUM JSON counterpart-nya masing-masing, setelah export excel route):

    ```typescript
    @Get('harian/export/pdf')
    @RequirePermissions('report.view')
    async exportHarianPdf(@Res() res: Response, @Query('date') date: string) {
      const d = date ?? new Date().toISOString().slice(0, 10);
      const buffer = await this.pdfReportsService.generateHarian(d);
      this.setPdfHeaders(res, `laporan-harian-${d}.pdf`);
      res.end(buffer);
    }

    @Get('bulanan/export/pdf')
    @RequirePermissions('report.view')
    async exportBulananPdf(
      @Res() res: Response,
      @Query('month', ParseIntPipe) month: number,
      @Query('year', ParseIntPipe) year: number,
      @Query('classId') classId?: string,
      @Query('unitId') unitId?: string,
    ) {
      const buffer = await this.pdfReportsService.generateBulanan(
        month, year,
        classId ? Number(classId) : undefined,
        unitId ? Number(unitId) : undefined,
      );
      this.setPdfHeaders(res, `laporan-bulanan-${year}-${month}.pdf`);
      res.end(buffer);
    }

    @Get('tunggakan/export/pdf')
    @RequirePermissions('report.view')
    async exportTunggakanPdf(
      @Res() res: Response,
      @Query('schoolYearId') schoolYearId?: string,
      @Query('classId') classId?: string,
      @Query('unitId') unitId?: string,
    ) {
      const buffer = await this.pdfReportsService.generateTunggakan({
        schoolYearId: schoolYearId ? Number(schoolYearId) : undefined,
        classId: classId ? Number(classId) : undefined,
        unitId: unitId ? Number(unitId) : undefined,
      });
      this.setPdfHeaders(res, 'laporan-tunggakan.pdf');
      res.end(buffer);
    }

    @Get('rekap-pos/export/pdf')
    @RequirePermissions('report.view')
    async exportRekapPosPdf(
      @Res() res: Response,
      @Query('dateFrom') dateFrom?: string,
      @Query('dateTo') dateTo?: string,
      @Query('schoolYearId') schoolYearId?: string,
    ) {
      const buffer = await this.pdfReportsService.generateRekapPos({
        dateFrom, dateTo,
        schoolYearId: schoolYearId ? Number(schoolYearId) : undefined,
      });
      this.setPdfHeaders(res, 'rekap-pos.pdf');
      res.end(buffer);
    }
    ```

    Penting: `@Res()` sebagai parameter PERTAMA (pattern dari Plan 04-03).

    **reports.module.ts:**
    - Import `PdfReportsService` dan `PdfService` dari `'../transactions/pdf/pdf.service'`
    - Tambah keduanya ke array `providers`

    PdfService tidak perlu di-export dari TransactionsModule — cukup daftarkan langsung di ReportsModule sebagai provider tambahan (PdfService tidak punya dependency injeksi NestJS, hanya puppeteer).
  </action>
  <verify>
    cd apps/backend && npx tsc --noEmit
    Lalu test live:
    TOKEN=$(curl -s -X POST http://localhost:3000/api/auth/login -H "Content-Type: application/json" -d '{"email":"admin@alhasaniyyah.sch.id","password":"Admin123!"}' | grep -o '"access_token":"[^"]*"' | sed 's/"access_token":"//;s/"//')
    curl -s -o /tmp/test.pdf -w "%{http_code}" "http://localhost:3000/api/reports/harian/export/pdf?date=2026-05-29" -H "Authorization: Bearer $TOKEN"
    (harus 200 dan file PDF valid — magic bytes %PDF)
  </verify>
  <done>AC-1, AC-2, AC-3, AC-4 fully satisfied: 4 endpoint export PDF live dan return file .pdf valid</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Logic query di `reports.service.ts` — PdfReportsService reuse method yang ada
- Endpoint kwitansi di `transactions.controller.ts` — tidak ada perubahan Phase 03
- Behavior default PdfService (A5, no landscape) — hanya tambah optional param

## SCOPE LIMITS
- Plan ini hanya export PDF — Excel sudah di Plan 04-03
- Tidak ada perubahan schema database
- Tidak ada perubahan frontend di plan ini
- HTML template cukup fungsional (tabel readable) — tidak perlu desain pixel-perfect

</boundaries>

<verification>
Sebelum declare complete:
- [ ] `cd apps/backend && npx tsc --noEmit` tanpa error
- [ ] GET /api/reports/harian/export/pdf return file PDF (magic bytes `%PDF`)
- [ ] GET /api/reports/bulanan/export/pdf?month=5&year=2026 return file PDF
- [ ] GET /api/reports/tunggakan/export/pdf return file PDF
- [ ] GET /api/reports/rekap-pos/export/pdf return file PDF
- [ ] Endpoint kwitansi GET /api/transactions/:id/receipt masih bekerja (format A5 tidak berubah)
- [ ] Semua endpoint JSON dan Excel lama masih 200
</verification>

<success_criteria>
- PdfService support optional A4 format tanpa breaking kwitansi A5
- PdfReportsService terdaftar di ReportsModule dengan 4 method generate
- 4 endpoint export PDF berfungsi dan return binary .pdf
- TypeScript compile tanpa error
- Semua endpoint lama tidak terganggu
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/04-reporting/04-04-SUMMARY.md`
</output>
