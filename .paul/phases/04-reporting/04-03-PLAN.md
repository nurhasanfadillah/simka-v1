---
phase: 04-reporting
plan: 03
type: execute
wave: 1
depends_on: ["04-02"]
files_modified:
  - apps/backend/src/reports/excel.service.ts
  - apps/backend/src/reports/reports.controller.ts
  - apps/backend/src/reports/reports.module.ts
autonomous: true
---

<objective>
## Goal
Tambah ExcelService dan 4 endpoint export Excel (harian, bulanan, tunggakan, rekap-pos) ke ReportsModule menggunakan ExcelJS.

## Purpose
Bendahara butuh data laporan dalam format Excel untuk rekap manual, pengarsipan, dan pelaporan ke yayasan. Ini adalah deliverable eksplisit di ROADMAP Phase 04.

## Output
- `apps/backend/src/reports/excel.service.ts` — service baru dengan 4 method export
- `reports.controller.ts` — 4 endpoint GET baru: `/harian/export/excel`, `/bulanan/export/excel`, `/tunggakan/export/excel`, `/rekap-pos/export/excel`
- `reports.module.ts` — ExcelService didaftarkan sebagai provider
- ExcelJS terinstall di backend workspace
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/04-reporting/04-02-SUMMARY.md

## Source Files
@apps/backend/src/reports/reports.service.ts
@apps/backend/src/reports/reports.controller.ts
@apps/backend/src/reports/reports.module.ts
</context>

<acceptance_criteria>

## AC-1: ExcelJS Terinstall
```gherkin
Given monorepo pnpm workspaces
When `pnpm add exceljs --filter backend` dijalankan
Then package exceljs muncul di apps/backend/package.json dependencies
```

## AC-2: Export Laporan Harian → File Excel
```gherkin
Given server berjalan dan user terautentikasi
When GET /api/reports/harian/export/excel?date=2026-05-29
Then response Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
  AND file .xlsx berisi sheet "Laporan Harian" dengan kolom: No, No Transaksi, Nama Siswa, NIS, Total, Status, Waktu
  AND baris data sesuai transaksi pada tanggal tersebut
  AND baris terakhir adalah total penerimaan
```

## AC-3: Export Laporan Bulanan → File Excel
```gherkin
Given server berjalan dan user terautentikasi
When GET /api/reports/bulanan/export/excel?month=5&year=2026
Then response adalah file .xlsx dengan sheet "Laporan Bulanan"
  AND kolom: Tanggal, Total Penerimaan, Jumlah Transaksi
  AND baris per hari dalam bulan tersebut
  AND baris ringkasan total di akhir
```

## AC-4: Export Tunggakan → File Excel
```gherkin
Given server berjalan dan user terautentikasi
When GET /api/reports/tunggakan/export/excel (dengan filter opsional)
Then response adalah file .xlsx dengan sheet "Laporan Tunggakan"
  AND kolom: No, Nama Siswa, NIS, Kelas, Total Tunggakan, Jumlah Tagihan
  AND data diurutkan by nama siswa
```

## AC-5: Export Rekap POS → File Excel
```gherkin
Given server berjalan dan user terautentikasi
When GET /api/reports/rekap-pos/export/excel (dengan filter opsional)
Then response adalah file .xlsx dengan sheet "Rekap POS"
  AND kolom: Kode POS, Nama POS, Total Penerimaan, Jumlah Transaksi, Jumlah Siswa
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Install ExcelJS di backend workspace</name>
  <files>apps/backend/package.json</files>
  <action>
    Jalankan dari root simka-v1:
    ```
    pnpm add exceljs --filter backend
    ```
    Jangan install di root atau frontend — hanya backend yang butuh ExcelJS.
    Setelah install, verifikasi entry di apps/backend/package.json.
  </action>
  <verify>grep "exceljs" apps/backend/package.json</verify>
  <done>AC-1 satisfied: exceljs muncul di apps/backend/package.json</done>
</task>

<task type="auto">
  <name>Task 2: Buat ExcelService dengan 4 method export</name>
  <files>apps/backend/src/reports/excel.service.ts</files>
  <action>
    Buat file baru `apps/backend/src/reports/excel.service.ts`.

    Import: `import * as ExcelJS from 'exceljs'` dan inject ReportsService via constructor.

    Implement 4 method — masing-masing return `Buffer`:

    **exportHarian(date: string): Promise&lt;Buffer&gt;**
    - Panggil `this.reportsService.getLaporanHarian(date)`
    - Sheet name: "Laporan Harian"
    - Header row (bold, background #1A3829, font putih): No | No Transaksi | Nama Siswa | NIS | Total | Status | Waktu
    - Data rows dari `result.transactions`
    - Baris terakhir: "TOTAL PENERIMAAN" | - | - | - | result.totalPenerimaan | - | -
    - Column widths: 5, 25, 30, 15, 18, 12, 20

    **exportBulanan(month: number, year: number, classId?: number, unitId?: number): Promise&lt;Buffer&gt;**
    - Panggil `this.reportsService.getLaporanBulanan(month, year, classId, unitId)`
    - Sheet name: "Laporan Bulanan"
    - Header: Tanggal | Total Penerimaan | Jumlah Transaksi
    - Data rows dari `result.perHari`
    - Baris ringkasan total di akhir

    **exportTunggakan(filters): Promise&lt;Buffer&gt;**
    - Panggil `this.reportsService.getLaporanTunggakan(filters)`
    - Sheet name: "Laporan Tunggakan"
    - Header: No | Nama Siswa | NIS | Kelas | Total Tunggakan | Jumlah Tagihan
    - Data rows dari `result.siswa`

    **exportRekapPos(filters): Promise&lt;Buffer&gt;**
    - Panggil `this.reportsService.getRekapPos(filters)`
    - Sheet name: "Rekap POS"
    - Header: Kode POS | Nama POS | Total Penerimaan | Jumlah Transaksi | Jumlah Siswa
    - Data rows dari `result.perPos`

    **Pattern untuk generate buffer (sama di semua method):**
    ```typescript
    const workbook = new ExcelJS.Workbook();
    const sheet = workbook.addWorksheet('...');
    // ... isi data ...
    const buffer = await workbook.xlsx.writeBuffer();
    return Buffer.from(buffer);
    ```

    **Style header (terapkan di semua method):**
    ```typescript
    const headerRow = sheet.getRow(1);
    headerRow.font = { bold: true, color: { argb: 'FFFFFFFF' } };
    headerRow.fill = { type: 'pattern', pattern: 'solid', fgColor: { argb: 'FF1A3829' } };
    headerRow.alignment = { horizontal: 'center' };
    ```

    Format kolom "Total" menggunakan numFmt: `'#,##0'` (rupiah tanpa desimal).

    Dekorasi class: `@Injectable()`.
  </action>
  <verify>
    cd apps/backend && npx tsc --noEmit
    (tidak ada TypeScript error di excel.service.ts)
  </verify>
  <done>AC-2, AC-3, AC-4, AC-5 partially satisfied: ExcelService dengan 4 method export berhasil dikompilasi</done>
</task>

<task type="auto">
  <name>Task 3: Tambah 4 endpoint export ke controller dan daftarkan ExcelService di module</name>
  <files>
    apps/backend/src/reports/reports.controller.ts,
    apps/backend/src/reports/reports.module.ts
  </files>
  <action>
    **reports.controller.ts:**
    - Import `ExcelService` dan `Res` dari `@nestjs/common`, `Response` dari `express`
    - Inject ExcelService via constructor: `private readonly excelService: ExcelService`
    - Helper untuk set response headers Excel:
      ```typescript
      private setExcelHeaders(res: Response, filename: string) {
        res.setHeader('Content-Type', 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet');
        res.setHeader('Content-Disposition', `attachment; filename="${filename}"`);
      }
      ```

    Tambah 4 endpoint baru (tempatkan SEBELUM route `:id` pattern jika ada, letakkan setelah endpoint JSON-nya masing-masing):

    ```typescript
    @Get('harian/export/excel')
    @RequirePermissions('report.view')
    async exportHarian(@Query('date') date: string, @Res() res: Response) {
      const d = date ?? new Date().toISOString().slice(0, 10);
      const buffer = await this.excelService.exportHarian(d);
      this.setExcelHeaders(res, `laporan-harian-${d}.xlsx`);
      res.end(buffer);
    }

    @Get('bulanan/export/excel')
    @RequirePermissions('report.view')
    async exportBulanan(
      @Query('month', ParseIntPipe) month: number,
      @Query('year', ParseIntPipe) year: number,
      @Query('classId') classId?: string,
      @Query('unitId') unitId?: string,
      @Res() res?: Response,
    ) {
      const buffer = await this.excelService.exportBulanan(
        month, year,
        classId ? Number(classId) : undefined,
        unitId ? Number(unitId) : undefined,
      );
      this.setExcelHeaders(res, `laporan-bulanan-${year}-${month}.xlsx`);
      res.end(buffer);
    }

    @Get('tunggakan/export/excel')
    @RequirePermissions('report.view')
    async exportTunggakan(
      @Query('schoolYearId') schoolYearId?: string,
      @Query('classId') classId?: string,
      @Query('unitId') unitId?: string,
      @Res() res?: Response,
    ) {
      const buffer = await this.excelService.exportTunggakan({
        schoolYearId: schoolYearId ? Number(schoolYearId) : undefined,
        classId: classId ? Number(classId) : undefined,
        unitId: unitId ? Number(unitId) : undefined,
      });
      this.setExcelHeaders(res, 'laporan-tunggakan.xlsx');
      res.end(buffer);
    }

    @Get('rekap-pos/export/excel')
    @RequirePermissions('report.view')
    async exportRekapPos(
      @Query('dateFrom') dateFrom?: string,
      @Query('dateTo') dateTo?: string,
      @Query('schoolYearId') schoolYearId?: string,
      @Res() res?: Response,
    ) {
      const buffer = await this.excelService.exportRekapPos({
        dateFrom, dateTo,
        schoolYearId: schoolYearId ? Number(schoolYearId) : undefined,
      });
      this.setExcelHeaders(res, 'rekap-pos.xlsx');
      res.end(buffer);
    }
    ```

    Penting: `@Res()` dengan `passthrough: false` (default) — NestJS tidak handle response, kita pakai `res.end()` manual seperti pattern PdfService.

    **reports.module.ts:**
    - Import ExcelService
    - Tambah ExcelService ke array `providers`
    - Tambah ReportsService ke `exports` jika belum ada (ExcelService butuh inject ReportsService)

    Pastikan ExcelService inject ReportsService bukan langsung ke DB — ExcelService memanggil method ReportsService yang sudah ada.
  </action>
  <verify>
    cd apps/backend && npx tsc --noEmit
    Lalu cek: curl -s -o /dev/null -w "%{http_code}" "http://localhost:3000/api/reports/harian/export/excel?date=2026-05-29" -H "Authorization: Bearer TOKEN"
    (setelah login, harus return 200 bukan 500/404)
  </verify>
  <done>AC-2, AC-3, AC-4, AC-5 fully satisfied: 4 endpoint export Excel live dan return file .xlsx</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `apps/backend/src/reports/reports.service.ts` — ExcelService reuse method yang ada, jangan ubah logic query
- `apps/backend/src/db/schema/` — tidak ada perubahan schema
- Endpoint JSON yang sudah ada (dashboard, harian, bulanan, tahunan, tunggakan, rekap-pos) — hanya tambah route baru, jangan ubah yang lama

## SCOPE LIMITS
- Plan ini hanya export Excel — PDF export ada di Plan 04-04
- Tidak ada styling yang kompleks — header hijau #1A3829 + data plain sudah cukup
- Tidak perlu merge cells atau formula Excel
- Tidak ada perubahan frontend di plan ini

</boundaries>

<verification>
Sebelum declare complete:
- [ ] `pnpm add exceljs --filter backend` berhasil
- [ ] `cd apps/backend && npx tsc --noEmit` tanpa error
- [ ] GET /api/reports/harian/export/excel return file .xlsx (bukan JSON)
- [ ] GET /api/reports/bulanan/export/excel?month=5&year=2026 return file .xlsx
- [ ] GET /api/reports/tunggakan/export/excel return file .xlsx
- [ ] GET /api/reports/rekap-pos/export/excel return file .xlsx
- [ ] File Excel bisa dibuka (tidak corrupt)
- [ ] Endpoint JSON lama (harian, bulanan, dll) masih bekerja normal
</verification>

<success_criteria>
- ExcelJS terinstall di backend workspace
- ExcelService terdaftar di ReportsModule
- 4 endpoint export Excel berfungsi dan return binary .xlsx
- TypeScript compile tanpa error
- Endpoint reports JSON yang sudah ada tidak terganggu
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/04-reporting/04-03-SUMMARY.md`
</output>
