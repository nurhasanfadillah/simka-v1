---
phase: 20-panduan-keuangan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/pages/keuangan/generate/index.tsx
  - apps/frontend/src/pages/keuangan/transaksi/baru/index.tsx
  - apps/frontend/src/pages/keuangan/riwayat/index.tsx
  - apps/frontend/src/pages/keuangan/tagihan/index.tsx
  - apps/frontend/src/pages/keuangan/tunggakan/index.tsx
autonomous: true
---

<objective>
## Goal
Tambah teks panduan penggunaan di seluruh halaman Keuangan: Generate Tagihan, Form Bayar, Riwayat Pembayaran (merah italic — krusial), serta Data Tagihan dan Tunggakan (gray italic — biasa).

## Purpose
Melengkapi Milestone v1.3 untuk modul Keuangan — modul paling krusial yang digunakan operator setiap hari. Panduan merah memberi sinyal visual bahwa tindakan di halaman tersebut berdampak langsung pada data keuangan.

## Output
Lima file frontend diubah: ditambah satu elemen `<p>` panduan di bawah header masing-masing halaman.
</objective>

<context>
@.paul/PROJECT.md
@.paul/phases/19-panduan-master-dashboard/19-01-SUMMARY.md
@apps/frontend/src/pages/keuangan/generate/index.tsx
@apps/frontend/src/pages/keuangan/transaksi/baru/index.tsx
@apps/frontend/src/pages/keuangan/riwayat/index.tsx
@apps/frontend/src/pages/keuangan/tagihan/index.tsx
@apps/frontend/src/pages/keuangan/tunggakan/index.tsx
</context>

<acceptance_criteria>

## AC-1: Panduan tampil di Generate Tagihan — merah
```gherkin
Given halaman Generate Tagihan dibuka
When halaman tampil
Then teks panduan muncul di bawah judul "Generate Tagihan", font kecil italic merah
```

## AC-2: Panduan tampil di Form Bayar (Transaksi Baru) — merah
```gherkin
Given halaman Transaksi Baru dibuka
When halaman tampil
Then teks panduan muncul di bawah subjudul "Catat pembayaran siswa", font kecil italic merah
```

## AC-3: Panduan tampil di Riwayat Pembayaran — merah
```gherkin
Given halaman Riwayat Pembayaran dibuka
When halaman tampil
Then teks panduan muncul di bawah header (setelah filter bar), font kecil italic merah
```

## AC-4: Panduan tampil di Data Tagihan — gray
```gherkin
Given halaman Data Tagihan dibuka
When halaman tampil
Then teks panduan muncul di bawah header, font kecil italic abu-abu
```

## AC-5: Panduan tampil di Tunggakan — gray
```gherkin
Given halaman Tunggakan dibuka
When halaman tampil
Then teks panduan muncul di bawah header, font kecil italic abu-abu
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Panduan merah — Generate Tagihan, Form Bayar, Riwayat Pembayaran</name>
  <files>
    apps/frontend/src/pages/keuangan/generate/index.tsx,
    apps/frontend/src/pages/keuangan/transaksi/baru/index.tsx,
    apps/frontend/src/pages/keuangan/riwayat/index.tsx
  </files>
  <action>
    **generate/index.tsx:**
    Header berupa `<div className="max-w-2xl">` (line 83) yang berisi h1 + p subtitle, lalu ditutup `</div>` (line 86).
    Setelah `</div>` penutup div max-w-2xl tersebut, tambahkan:
    ```tsx
    <p className="text-xs italic text-red-500 mt-2 max-w-2xl">
      Halaman ini digunakan untuk membuat tagihan secara massal. Pilih tahun pelajaran dan kelas, lalu klik Preview sebelum Generate. Pastikan data template sudah benar — tagihan yang digenerate hanya bisa dibatalkan via Void transaksi.
    </p>
    ```
    Gunakan `max-w-2xl` agar lebar panduan sejajar dengan konten form di bawahnya.

    **transaksi/baru/index.tsx:**
    Header berupa h1 dan p subtitle sebagai direct children dari `<div className="p-6 max-w-3xl">` (line 188-190).
    Setelah baris:
    ```tsx
    <p className="text-gray-500 mt-1">Catat pembayaran siswa</p>
    ```
    Tambahkan:
    ```tsx
    <p className="text-xs italic text-red-500 mt-1">
      Catat pembayaran siswa di sini. Cari siswa terlebih dahulu, pilih tagihan yang akan dibayar, lalu masukkan nominal. Pastikan data sudah benar sebelum menyimpan — transaksi yang salah hanya bisa dibatalkan melalui Void dan tidak bisa dihapus.
    </p>
    ```

    **riwayat/index.tsx:**
    Header berupa `<div className="flex items-center justify-between mb-6">` (line 86) dengan filter di sebelah kanan.
    Div ini ditutup di line 115.
    Ganti `mb-6` menjadi `mb-2`, lalu setelah `</div>` penutup (line 115) tambahkan:
    ```tsx
    <p className="text-xs italic text-red-500 mb-4">
      Daftar semua transaksi pembayaran yang tercatat. Untuk membatalkan transaksi yang salah, gunakan tombol Void — tindakan ini tidak dapat diurungkan dan akan mempengaruhi status tagihan siswa.
    </p>
    ```

    Jangan ubah logika state, handler, atau filter di ketiga file.
  </action>
  <verify>
    Baca ulang ketiga file setelah edit. Pastikan:
    - `<p className="text-xs italic text-red-500 ...">` ada di posisi yang benar di masing-masing file
    - riwayat/index.tsx: `mb-6` sudah diganti `mb-2`
    - Tidak ada perubahan pada handler atau state
    - `npx tsc --noEmit` dari apps/frontend: 0 error
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: panduan merah tampil di Generate Tagihan, Form Bayar, dan Riwayat Pembayaran</done>
</task>

<task type="auto">
  <name>Task 2: Panduan gray — Data Tagihan, Tunggakan</name>
  <files>
    apps/frontend/src/pages/keuangan/tagihan/index.tsx,
    apps/frontend/src/pages/keuangan/tunggakan/index.tsx
  </files>
  <action>
    **tagihan/index.tsx:**
    Header berupa `<div className="flex items-center justify-between mb-6">` (line 88) dengan beberapa filter select.
    Div ini ditutup di line 125.
    Ganti `mb-6` menjadi `mb-2`, lalu setelah `</div>` penutup (line 125) tambahkan:
    ```tsx
    <p className="text-xs italic text-gray-400 mb-4">
      Daftar tagihan yang telah digenerate untuk setiap siswa. Gunakan filter untuk mencari tagihan berdasarkan kelas, tahun pelajaran, atau status pembayaran. Klik baris untuk melihat detail tagihan per bulan.
    </p>
    ```

    **tunggakan/index.tsx:**
    Header berupa `<div className="flex items-center justify-between mb-6">` (line 57) dengan filter select.
    Div ini ditutup di line 84.
    Ganti `mb-6` menjadi `mb-2`, lalu setelah `</div>` penutup (line 84) tambahkan:
    ```tsx
    <p className="text-xs italic text-gray-400 mb-4">
      Daftar siswa yang memiliki tagihan belum lunas. Gunakan filter kelas dan tahun pelajaran untuk melihat tunggakan per kelompok siswa.
    </p>
    ```

    Jangan ubah logika state, handler, atau filter di kedua file.
  </action>
  <verify>
    Baca ulang kedua file setelah edit. Pastikan:
    - `<p className="text-xs italic text-gray-400 mb-4">` ada di posisi yang benar
    - `mb-6` sudah diganti `mb-2` di kedua file
    - Tidak ada perubahan pada handler atau state
    - `npx tsc --noEmit` dari apps/frontend: 0 error
  </verify>
  <done>AC-4 dan AC-5 satisfied: panduan gray tampil di Data Tagihan dan Tunggakan</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Logika state, handler, dan fetch functions di semua file
- Dialog, modal, form, tabel, dan filter yang sudah ada
- Routing dan struktur komponen
- Style apapun selain penambahan elemen `<p>` panduan dan perubahan `mb-6` → `mb-2`

## SCOPE LIMITS
- Hanya menambah satu elemen `<p>` per halaman di bawah header
- Tidak membuat komponen baru atau shared component untuk panduan
- Tidak menyentuh halaman Laporan atau Pengaturan (masuk Phase 21)

</boundaries>

<verification>
- [ ] generate/index.tsx: `<p className="text-xs italic text-red-500 ...">` ada setelah div max-w-2xl
- [ ] transaksi/baru/index.tsx: `<p className="text-xs italic text-red-500 ...">` ada setelah subtitle
- [ ] riwayat/index.tsx: panduan merah ada, `mb-6` → `mb-2` diubah
- [ ] tagihan/index.tsx: panduan gray ada, `mb-6` → `mb-2` diubah
- [ ] tunggakan/index.tsx: panduan gray ada, `mb-6` → `mb-2` diubah
- [ ] `npx tsc --noEmit` dari apps/frontend: 0 error
- [ ] Semua AC terpenuhi
</verification>

<success_criteria>
- 5 halaman Keuangan memiliki teks panduan di bawah judul
- Bahasa awam, mudah dipahami operator non-teknis
- Generate Tagihan, Form Bayar, Riwayat Pembayaran: teks merah italic (krusial)
- Data Tagihan, Tunggakan: teks abu-abu italic
- Tidak ada regresi pada fungsi yang sudah ada
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/20-panduan-keuangan/20-01-SUMMARY.md`
</output>
