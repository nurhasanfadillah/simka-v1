---
phase: 34-ui-modernization
plan: 06
type: execute
wave: 2
depends_on: [34-01, 34-05]
files_modified:
  - apps/frontend/src/pages/**/index.tsx
  - apps/frontend/src/pages/**/mapping.tsx
  - apps/frontend/src/pages/**/migrasi-status.tsx
  - apps/frontend/src/pages/**/import.tsx
  - apps/frontend/src/pages/**/[id].tsx
  - apps/frontend/src/components/ui/button.tsx
autonomous: true
---

<objective>
## Goal
Tambah motion & micro-interactions ke seluruh SIMKA — page transitions, card hover, button press, stagger list. Semua via className changes (aman untuk batch regex).

## Purpose
Setelah 34-05 standardisasi warna, sekarang tambah "life" ke UI. Motion yang subtle membuat aplikasi terasa premium tanpa mengubah struktur.

## Output
1. **Page transition** — `animate-fade-in-up` di wrapper semua halaman
2. **Card hover lift** — shadow-md + slight translateY pada hover card
3. **Button press** — `active:scale-[0.98]` di button.tsx
4. **Table row hover** — smooth transition
5. **Nav item transition** — sudah ada, verify
</objective>

<context>
@.paul/phases/34-ui-modernization/34-01-SUMMARY.md
@.paul/phases/34-ui-modernization/34-05-SUMMARY.md
@apps/frontend/src/index.css (custom keyframes)
@apps/frontend/src/components/ui/button.tsx
@apps/frontend/src/components/sidebar/NavItem.tsx
</context>

<acceptance_criteria>
## AC-1: Page Transition di Semua Halaman
Given navigasi ke halaman manapun (kecuali login)
When halaman mount
Then konten fade-in-up dengan animasi halus 0.4s

## AC-2: Card Hover Lift
Given card (Card component atau div card) di-hover
When cursor di atas card
Then card sedikit terangkat (translateY -2px) + shadow meningkat, transition 200ms

## AC-3: Button Press Effect  
Given button di-klik
When active state
Then button scale 0.98 (sedikit mengecil) — feedback tactile

## AC-4: Build Pass
Given semua perubahan
When build
Then sukses
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Add Page Transition + Card Hover ke Semua Halaman</name>
  <files>Semua pages/*.tsx (kecuali dashboard + login)</files>
  <action>
    1. Di SETIAP halaman (kecuali dashboard yg sudah punya, dan login):
       - Cari `<div className="p-6` (outer wrapper)
       - Tambah `animate-fade-in-up` ke className
    
    2. Di SETIAP card wrapper (div dengan `bg-white rounded-xl shadow-sm border`):
       - Tambah `hover:shadow-md hover:-translate-y-0.5 transition-all duration-200`
    
    Gunakan PowerShell untuk batch karena ini className-level changes:
    ```
    # Page transition
    $c = $c -replace 'className="p-6', 'className="p-6 animate-fade-in-up'
    
    # Card hover (table wrapper cards)
    $c = $c -replace 'bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden', 'bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden hover:shadow-md transition-shadow duration-200'
    $c = $c -replace 'bg-white rounded-xl shadow-sm border border-gray-100 overflow-x-auto', 'bg-white rounded-xl shadow-sm border border-gray-100 overflow-x-auto hover:shadow-md transition-shadow duration-200'
    
    # Stat/summary cards  
    $c = $c -replace 'bg-white rounded-xl border border-gray-100 shadow-sm p-6', 'bg-white rounded-xl border border-gray-100 shadow-sm p-6 hover:shadow-md transition-shadow duration-200'
    ```

    KECUALI: jangan sentuh file dashboard (sudah punya page-transition). Jangan sentuh login (full-screen layout berbeda).
  </action>
  <verify>pnpm --filter frontend build</verify>
  <done>AC-1, AC-2 satisfied: page transition + card hover di semua halaman; build pass</done>
</task>

<task type="auto">
  <name>Add Button Press Effect + Ensure Nav Transition</name>
  <files>apps/frontend/src/components/ui/button.tsx</files>
  <action>
    1. Baca `button.tsx`
    2. Tambah `active:scale-[0.98]` ke `buttonVariants` base className
    3. Tambah `transition-transform` jika belum ada
    4. Verify NavItem sudah punya `transition-colors duration-200` (harusnya sudah dari 34-02)
    
    Code: di `buttonVariants` CVA, tambah ke base:
    ```
    "inline-flex items-center justify-center ... transition-all active:scale-[0.98]"
    ```
    
    Ini akan apply ke SEMUA button di aplikasi — shadcn Button, termasuk yang di dialog, form, tabel.
  </action>
  <verify>pnpm --filter frontend build</verify>
  <done>AC-3 satisfied: button press effect global; build pass</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Logic, state, data fetching
- Struktur JSX
- Dialog/modal components
- Login page layout
- Dashboard (already has transitions from 34-04)

## SCOPE LIMITS
- HANYA className changes (string replacement)
- JANGAN ubah struktur komponen
- JANGAN tambah dependency baru
- JANGAN tambah JavaScript animation logic
</boundaries>

<verification>
1. `pnpm --filter frontend build` → sukses
2. Buka browser → navigasi antar halaman → fade-in-up terasa
3. Hover card → shadow lift
4. Klik button → scale press
</verification>
</output>
