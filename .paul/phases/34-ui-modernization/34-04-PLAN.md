---
phase: 34-ui-modernization
plan: 04
type: execute
wave: 1
depends_on: [34-01, 34-03]
files_modified:
  - apps/frontend/src/pages/dashboard/index.tsx
autonomous: true
---

<objective>
## Goal
Polish halaman dashboard dengan component baru (Card glass, Skeleton, Badge, Table) + page transition animation. Menjadi showcase pertama dari design modernization SIMKA.

## Purpose
Dashboard adalah halaman pertama yang dilihat user setelah login. Ini jadi "north star" visual untuk seluruh aplikasi — standar yang akan diikuti halaman lain di plan 34-05.

## Output
1. Stat cards dengan Card variant="glass" + icon circle accent + angka bold
2. Skeleton loading (bukan div manual)
3. Badge status di tabel (bukan span inline)
4. Table component shadcn untuk riwayat transaksi
5. Yayasan info card dalam Card component
6. Page transition `animate-fade-in-up` di wrapper
7. Visual hierarchy diperbaiki: spacing, font weight, icon sizing
</objective>

<context>
@.paul/phases/34-ui-modernization/34-01-SUMMARY.md
@.paul/phases/34-ui-modernization/34-03-SUMMARY.md
@apps/frontend/src/pages/dashboard/index.tsx
@apps/frontend/src/components/ui/card.tsx
@apps/frontend/src/components/ui/skeleton.tsx
@apps/frontend/src/components/ui/badge.tsx
@apps/frontend/src/components/ui/table.tsx
@apps/frontend/src/index.css
@apps/frontend/src/types/dashboard.ts
</context>

<acceptance_criteria>
## AC-1: Glass Stat Cards
Given dashboard dimuat
When data tersedia
Then 3 stat cards menggunakan Card variant="glass" — backdrop-blur, border subtle, shadow elevation. Icon circle di accent-light, angka di text-2xl font-bold.

## AC-2: Skeleton Loading
Given dashboard loading
When data belum tersedia  
Then stat cards + tabel menampilkan Skeleton component (3 baris untuk stats, 5 baris untuk tabel) — bukan div manual animate-pulse

## AC-3: Badge Status
Given tabel riwayat transaksi
When status = "aktif" atau "void"
Then menggunakan Badge variant="success" (hijau) untuk aktif dan variant="danger" (merah) untuk void

## AC-4: Table Component
Given tabel riwayat transaksi
When dirender
Then menggunakan Table/TableHeader/TableBody/TableRow/TableHead/TableCell dari shadcn/ui

## AC-5: Yayasan Card
Given dashboard dimuat
When info yayasan tampil
Then berada dalam Card component dengan logo + nama + alamat + kontak — bukan div polos

## AC-6: Page Transition
Given dashboard dibuka
When halaman mount
Then konten muncul dengan animasi fade-in-up 0.4s

## AC-7: Visual Hierarchy
Given dashboard dirender
When inspeksi visual
Then spacing konsisten (space-y-6 antar section), heading size hierarchy (text-lg section, text-2xl stat numbers), warna konsisten hijau Al-Hasaniyyah

## AC-8: Build Pass
Given perubahan di dashboard
When `pnpm --filter frontend build`
Then sukses
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Rewrite DashboardPage dengan Semua Komponen Baru</name>
  <files>apps/frontend/src/pages/dashboard/index.tsx</files>
  <action>
    Rewrite dashboard/index.tsx sepenuhnya:

    IMPORTS:
    - Tambah Skeleton, Badge, Card+CardContent, Table+TableHeader+TableBody+TableRow+TableHead+TableCell
    - Tetap import: Users, CreditCard, Banknote, BarChart2, ChevronRight dari lucide-react
    - Tetap import: apiClient, DashboardStats, Transaction
    - Hapus useState untuk loading/error jika tidak berubah

    STRUCTURE:
    ```
    <div className="p-[var(--spacing-page)] space-y-[var(--spacing-section)] page-transition">
      {/* Error — tetap */}
      
      {/* Yayasan Info — dalam Card */}
      <Card variant="glass">
        <CardContent className="p-6">
          <div className="flex items-start gap-6">
            <img src="/logo.png" className="w-24 h-24 object-contain flex-shrink-0" />
            <div>
              <h3 className="text-xl font-bold text-primary">YAYASAN PENDIDIKAN ISLAM AL-HASANIYYAH</h3>
              <div className="mt-4 space-y-1.5 text-sm text-muted-foreground">
                <p>Jl. Raya Cileungsi-Jonggo Km. 10, Cipeucang, Cileungsi, Bogor, Jawa Barat 16820</p>
                <p>(0262) 123-4567</p>
              </div>
            </div>
          </div>
        </CardContent>
      </Card>

      {/* Stat Cards */}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
        {loading ? (
          <>
            <Skeleton className="h-28 rounded-xl" />
            <Skeleton className="h-28 rounded-xl" />
            <Skeleton className="h-28 rounded-xl" />
          </>
        ) : (
          <>
            <Card variant="glass">
              <CardContent className="p-6">
                <div className="flex items-center gap-4">
                  <div className="w-12 h-12 rounded-full bg-accent-light flex items-center justify-center shrink-0">
                    <Users className="w-6 h-6 text-accent" />
                  </div>
                  <div>
                    <p className="text-sm text-muted-foreground">Siswa Aktif</p>
                    <p className="text-2xl font-bold mt-1">{stats?.totalSiswaAktif ?? 0}</p>
                  </div>
                </div>
              </CardContent>
            </Card>
            {/* Card 2 & 3 — sama pattern, ganti icon & label */}
          </>
        )}
      </div>

      {/* Quick Action — dalam Card */}
      <Card variant="glass">
        <CardContent className="p-6">
          <h2 className="text-base font-semibold mb-4">Aksi Cepat</h2>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
            {/* Tombol Transaksi Baru & Lihat Laporan — sama */}
          </div>
        </CardContent>
      </Card>

      {/* Tabel Riwayat — dalam Card + Table component */}
      <Card variant="glass">
        <CardContent className="p-6">
          <h2 className="text-lg font-semibold mb-4">Riwayat Transaksi Terbaru</h2>
          <Table>
            <TableHeader>
              <TableRow>
                <TableHead>No. Transaksi</TableHead>
                <TableHead>Nama Siswa</TableHead>
                <TableHead>NIS</TableHead>
                <TableHead>Nominal</TableHead>
                <TableHead>Status</TableHead>
                <TableHead>Tanggal</TableHead>
              </TableRow>
            </TableHeader>
            <TableBody>
              {loading ? (
                [0,1,2,3,4].map(i => (
                  <TableRow key={i}>
                    <TableCell colSpan={6}><Skeleton className="h-5 w-full" /></TableCell>
                  </TableRow>
                ))
              ) : transactions.length === 0 ? (
                <TableRow>
                  <TableCell colSpan={6} className="text-center text-muted-foreground py-8">
                    Belum ada transaksi
                  </TableCell>
                </TableRow>
              ) : (
                transactions.map(tx => (
                  <TableRow key={tx.id}>
                    <TableCell className="font-mono text-sm">{tx.transactionNumber}</TableCell>
                    <TableCell className="font-medium">{tx.studentName}</TableCell>
                    <TableCell className="text-muted-foreground">{tx.nis}</TableCell>
                    <TableCell className="font-semibold text-accent">{formatRupiah(tx.totalAmount)}</TableCell>
                    <TableCell>
                      <Badge variant={tx.status === 'aktif' ? 'success' : 'danger'}>
                        {tx.status}
                      </Badge>
                    </TableCell>
                    <TableCell className="text-muted-foreground">{formatTanggal(tx.createdAt)}</TableCell>
                  </TableRow>
                ))
              )}
            </TableBody>
          </Table>
        </CardContent>
      </Card>
    </div>
    ```

    DETAIL:
    - Semua Card pakai `variant="glass"`
    - Semua loading pakai `<Skeleton>`
    - Semua status pakai `<Badge>`
    - Tabel pakai `<Table>` component chain
    - Spacing konsisten: `p-[var(--spacing-page)]` wrapper, `space-y-[var(--spacing-section)]` antar section
    - Wrapper pakai class `page-transition` untuk fade-in-up
    - Warna: `text-primary` untuk dark text, `text-accent` untuk hijau, `text-muted-foreground` untuk gray
    - Icon warna: `text-accent` (bukan hardcoded #00A651)
    - Background icon: `bg-accent-light` (bukan hardcoded #E8F5EE)
  </action>
  <verify>pnpm --filter frontend build</verify>
  <done>AC-1 through AC-8 satisfied: glass cards, skeleton, badge, table, yayasan card, transition, hierarchy, build pass</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- API calls (fetchData, apiClient.get)
- formatRupiah & formatTanggal helpers
- Data fetching logic (useEffect, error handling)
- Navigasi ke halaman lain
- Types (DashboardStats, Transaction)
- Sidebar, AppLayout, routing

## SCOPE LIMITS
- HANYA 1 file: `pages/dashboard/index.tsx`
- Tidak tambah chart/recharts di plan ini (butuh backend API baru)
- Tidak ubah struktur data types
</boundaries>

<verification>
1. `pnpm --filter frontend build` → sukses
2. Dashboard render: Card glass terlihat, Skeleton saat loading, Badge di tabel, Table component
3. Animasi fade-in-up saat navigasi ke dashboard
4. Tidak ada hardcoded hex color — semua via token (text-accent, bg-accent-light, text-muted-foreground)
</verification>
</output>
