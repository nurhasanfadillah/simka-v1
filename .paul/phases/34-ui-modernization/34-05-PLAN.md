---
phase: 34-ui-modernization
plan: 05
type: execute
wave: 1
depends_on: [34-01, 34-03, 34-04]
files_modified:
  - apps/frontend/src/pages/keuangan/generate/index.tsx
  - apps/frontend/src/pages/keuangan/tagihan/index.tsx
  - apps/frontend/src/pages/keuangan/transaksi/baru/index.tsx
  - apps/frontend/src/pages/keuangan/riwayat/index.tsx
  - apps/frontend/src/pages/keuangan/tunggakan/index.tsx
  - apps/frontend/src/pages/master/tahun-pelajaran/index.tsx
  - apps/frontend/src/pages/master/kelas/index.tsx
  - apps/frontend/src/pages/master/kelas/mapping.tsx
  - apps/frontend/src/pages/master/migrasi-status.tsx
  - apps/frontend/src/pages/master/siswa/index.tsx
  - apps/frontend/src/pages/master/siswa/[id].tsx
  - apps/frontend/src/pages/master/siswa/import.tsx
  - apps/frontend/src/pages/master/pos/index.tsx
  - apps/frontend/src/pages/master/manajemen-pembayaran/index.tsx
  - apps/frontend/src/pages/laporan/harian/index.tsx
  - apps/frontend/src/pages/laporan/bulanan/index.tsx
  - apps/frontend/src/pages/laporan/tahunan/index.tsx
  - apps/frontend/src/pages/laporan/tunggakan/index.tsx
  - apps/frontend/src/pages/laporan/rekap-pos/index.tsx
  - apps/frontend/src/pages/pengaturan/profil/index.tsx
  - apps/frontend/src/pages/pengaturan/users/index.tsx
  - apps/frontend/src/pages/pengaturan/roles/index.tsx
  - apps/frontend/src/pages/login/index.tsx
autonomous: true
---

<objective>
## Goal
Terapkan konsistensi visual ke SEMUA 23 halaman SIMKA: global color token replacement, component upgrade (Table, Skeleton, Badge, Card), spacing token, dan page transition.

## Purpose
Setelah 34-01 (foundation), 34-03 (components), 34-04 (dashboard showcase), sekarang saatnya apply pola yang sama ke seluruh aplikasi. Semua halaman akan "naik level" secara visual serempak.

## Output
1. **Global**: semua `bg-[#00A651]` → `bg-accent`, `hover:bg-[#008C44]` → `hover:bg-accent/90`, `text-[#00A651]` → `text-accent`, `focus:ring-[#00A651]` → `focus:ring-ring`, `bg-red-600 hover:bg-red-700` → `variant="destructive"`
2. **Tables**: raw `<table>` → `<Table>` component di semua halaman
3. **Loading**: `animate-pulse bg-gray-200` div → `<Skeleton>`
4. **Badges**: inline span status → `<Badge variant="success"/"warning"/"danger"/"secondary"/>`
5. **Stat cards**: `bg-white rounded-xl border` → `Card variant="glass"`
6. **Spacing**: `p-6` → `p-[var(--spacing-page)]` + `space-y-[var(--spacing-section)]`
7. **Transition**: wrapper ditambah class `animate-fade-in-up`
8. **Panduan text**: tetap dipertahankan (gray italic / red italic)
</objective>

<context>
@.paul/phases/34-ui-modernization/34-01-SUMMARY.md
@.paul/phases/34-ui-modernization/34-03-SUMMARY.md
@.paul/phases/34-ui-modernization/34-04-SUMMARY.md
@apps/frontend/src/pages/dashboard/index.tsx (reference pattern)
@apps/frontend/src/index.css (design tokens)
</context>

<acceptance_criteria>
## AC-1: Zero Hardcoded Green
Given seluruh kode frontend
When grep `#00A651` atau `#008C44` atau `#E8F5EE`
Then hasil = 0 (semua sudah pakai token `accent`, `accent/90`, `accent-light`)

## AC-2: Table Component di Semua Halaman
Given SEMUA halaman dengan tabel
When inspeksi source
Then menggunakan `<Table><TableHeader><TableBody><TableRow><TableHead><TableCell>` — bukan `<table className="w-full">`

## AC-3: Skeleton di Semua Loading State
Given SEMUA halaman dengan loading state
When loading = true
Then menggunakan `<Skeleton>` component — bukan `animate-pulse bg-gray-200`

## AC-4: Badge di Semua Status
Given SEMUA halaman dengan status pill (aktif/void/nonaktif/lunas/belum_bayar)
When status dirender
Then menggunakan `<Badge>` dengan variant yang sesuai

## AC-5: Stat/Summary Cards pakai Card Component
Given halaman dengan summary/stat cards (laporan harian, dll)
When card dirender
Then menggunakan `<Card variant="glass">`

## AC-6: Spacing Token Konsisten
Given SEMUA halaman
When wrapper div di-inspeksi
Then menggunakan `p-[var(--spacing-page)]` dan `space-y-[var(--spacing-section)]`

## AC-7: Page Transition
Given navigasi ke halaman manapun
When halaman mount
Then konten fade-in-up dengan class `animate-fade-in-up`

## AC-8: Build Pass
Given semua perubahan
When `pnpm --filter frontend build`
Then sukses tanpa error
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Global Color Token Replacement + spacing/transition di SEMUA halaman</name>
  <files>Semua 23 file pages/*.tsx</files>
  <action>
    Lakukan search-and-replace di SEMUA file pages:

    1. `bg-[#00A651]` → `bg-accent`
    2. `hover:bg-[#008C44]` → `hover:bg-accent/90`
    3. `text-[#00A651]` → `text-accent`
    4. `focus:ring-[#00A651]` → `focus:ring-ring`
    5. `bg-[#E8F5EE]` → `bg-accent-light`
    6. `text-[#1A3829]` → `text-primary`
    7. `bg-red-600 hover:bg-red-700 text-white` → `variant="destructive"` (pada Button component — perlu pastikan import Button)
    8. `p-6` → `p-[var(--spacing-page)]` di wrapper div terluar SETIAP halaman
    9. Tambah `animate-fade-in-up` di wrapper div terluar SETIAP halaman (jika belum ada)
    10. `space-y-6` → `space-y-[var(--spacing-section)]` di wrapper
    11. `mb-2` di header div (judul halaman) → `mb-4` (spacing lebih lapang)

    CARA: Baca setiap file, lakukan edit via replaceAll/strategis.
    Fokus: hanya edit className — jangan ubah logic, state, atau struktur JSX.
    KECUALI: jika ada `<Button className="bg-[#00A651]...">` → ganti jadi `<Button>` (default variant sudah primary/accent pattern dari shadcn) atau `<Button className="bg-accent hover:bg-accent/90">` jika button.tsx default bukan accent.
  </action>
  <verify>pnpm --filter frontend build</verify>
  <done>AC-1 satisfied: 0 hardcoded hex green; AC-6, AC-7 satisfied: spacing token + transition di semua halaman; build pass</done>
</task>

<task type="auto">
  <name>Component Upgrade: Keuangan Pages (5 halaman)</name>
  <files>apps/frontend/src/pages/keuangan/*/index.tsx</files>
  <action>
    Untuk SETIAP halaman keuangan (generate, tagihan, transaksi/baru, riwayat, tunggakan):

    1. Ganti `<table className="w-full">` + `<thead>` + `<tbody>` → `<Table><TableHeader><TableBody>` pattern
       - `<tr className="bg-gray-50">` → `<TableRow>` (TableHead otomatis)
       - `<th className="px-6 py-3...">` → `<TableHead>`
       - `<td className="px-6 py-4...">` → `<TableCell>`
       - `<tr className="border-t...">` → `<TableRow>`
    2. Ganti loading skeleton: 
       - `<div className="animate-pulse bg-gray-200 h-6 rounded" />` → `<Skeleton className="h-6 w-full" />`
    3. Ganti status badge inline:
       - `<span className="inline-flex px-2 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">aktif</span>` → `<Badge variant="success">Aktif</Badge>`
       - `bg-red-100 text-red-800` → `<Badge variant="danger">Void</Badge>`
       - `bg-yellow-100 text-yellow-800` (belum bayar/belum lunas) → `<Badge variant="warning">...</Badge>`
    4. Ganti table wrapper card:
       - `<div className="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden">` → `<Card variant="glass">`
    5. Ganti raw `<select>` → shadcn `<Select>` (jika ada)
    6. Ganti summary/stat card (seperti di laporan):
       - `<div className="bg-white rounded-xl border border-gray-100 shadow-sm p-6">` → `<Card variant="glass"><CardContent className="p-6">...`
    7. Import tambahan: Table/TableHeader/TableBody/TableRow/TableHead/TableCell, Skeleton, Badge, Card/CardContent

    IMPORT: Di setiap file, tambah import:
    ```
    import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'
    import { Skeleton } from '@/components/ui/skeleton'
    import { Badge } from '@/components/ui/badge'
    import { Card, CardContent } from '@/components/ui/card'
    ```
    Hapus import yang sudah tidak dipakai (jika ada).
  </action>
  <verify>pnpm --filter frontend build</verify>
  <done>AC-2, AC-3, AC-4, AC-5 satisfied untuk 5 halaman keuangan; build pass</done>
</task>

<task type="auto">
  <name>Component Upgrade: Master Data Pages (9 halaman)</name>
  <files>apps/frontend/src/pages/master/**/index.tsx, apps/frontend/src/pages/master/**/[id].tsx, apps/frontend/src/pages/master/**/mapping.tsx, apps/frontend/src/pages/master/migrasi-status.tsx, apps/frontend/src/pages/master/**/import.tsx</files>
  <action>
    SAMA persis seperti Task 2, untuk SEMUA halaman master:
    - tahun-pelajaran/index.tsx
    - kelas/index.tsx
    - kelas/mapping.tsx
    - migrasi-status.tsx
    - siswa/index.tsx
    - siswa/[id].tsx
    - siswa/import.tsx
    - pos/index.tsx
    - manajemen-pembayaran/index.tsx

    Pattern: Table + Skeleton + Badge + Card upgrade.
    Table wrapper: `<div className="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden">` → `<Card variant="glass">`

    KHUSUS manajemen-pembayaran: pakai layout dual-table — kedua table pakai Table component.

    KHUSUS siswa/import: ada progress/steps UI — hanya upgrade table hasil import dan loading skeleton. Jangan ubah flow steps.

    KHUSUS siswa/[id]: halaman detail siswa — ganti info cards ke Card, ganti table tagihan ke Table.

    KHUSUS kelas/mapping: dual-table layout — kedua table ke Table component, skeleton loading ke Skeleton.

    Tambah import yang diperlukan di setiap file.
  </action>
  <verify>pnpm --filter frontend build</verify>
  <done>AC-2, AC-3, AC-4, AC-5 satisfied untuk 9 halaman master; build pass</done>
</task>

<task type="auto">
  <name>Component Upgrade: Laporan + Pengaturan + Login (9 halaman)</name>
  <files>apps/frontend/src/pages/laporan/**/index.tsx, apps/frontend/src/pages/pengaturan/**/index.tsx, apps/frontend/src/pages/login/index.tsx</files>
  <action>
    SAMA pattern untuk:
    - laporan/harian, bulanan, tahunan, tunggakan, rekap-pos (5 halaman)
    - pengaturan/profil, users, roles (3 halaman)
    - login (1 halaman)

    LAPORAN pages:
    - Stat/summary cards → Card variant="glass"
    - Table → Table component
    - Skeleton → Skeleton component
    - Status badge → Badge component

    PENGATURAN pages:
    - Table → Table component
    - Skeleton → Skeleton component  
    - Status badge → Badge component
    - Raw select → shadcn Select (users page yang pakai raw select untuk isActive)
    - profil: tidak ada tabel — hanya form. Ganti card wrapper ke Card.

    LOGIN page:
    - Sudah pakai token (bg-accent, from-primary) — paling bersih
    - Tambah `animate-fade-in-up` di card login
    - Update `rounded-2xl` → `rounded-[var(--radius-card)]`

    Tambah import yang diperlukan di setiap file.
  </action>
  <verify>pnpm --filter frontend build</verify>
  <done>AC-2 through AC-7 satisfied untuk 9 halaman tersisa; build pass</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Logic bisnis, state management, API calls, data fetching
- Struktur JSX (hanya ganti className dan component type)
- Props dan event handlers
- Sidebar, AppLayout, routing
- Jenis dan struktur dialog/modal
- Panduan teks (gray/red italic)
- Format tanggal dan rupiah

## SCOPE LIMITS
- HANYA files yang disebutkan di `<files>`
- HANYA ganti: classNames (warna, spacing), table→Table, skeleton div→Skeleton, badge span→Badge, card div→Card
- Jika ada component yang TIDAK kompatibel (misal butuh restructure besar), skip halaman itu dan catat di SUMMARY
- JANGAN ubah form input (tetap pakai Input + Label dari shadcn)
- JANGAN ubah button variants yang sudah benar (outline, ghost, dll)
</boundaries>

<verification>
1. `pnpm --filter frontend build` → sukses setelah setiap task
2. grep `#00A651` di src/pages → 0 hasil
3. grep `animate-pulse bg-gray-200` di src/pages → 0 hasil  
4. grep `<table className` di src/pages → 0 hasil
5. Semua halaman render dengan Card glass, Table, Skeleton, Badge
</verification>
</output>
