---
phase: 28-migrasi-status
plan: 28-02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - apps/frontend/src/pages/master/naik-kelas.tsx
  - apps/frontend/src/pages/master/migrasi-status.tsx
  - apps/frontend/src/pages/master/siswa/index.tsx
  - apps/frontend/src/pages/master/siswa/[id].tsx
  - apps/frontend/src/components/sidebar/index.tsx
  - apps/frontend/src/router.tsx
  - apps/frontend/src/types/master.ts
autonomous: true
---

<objective>
## Goal
Rename modul "Naik Kelas" → "Migrasi Status", tambah UI untuk aksi `keluar`/`pindah`, mode individual siswa, dan hapus semua jejak `isActive` dari UI.

## Purpose
Operator bisa mengelola status siswa (`aktif → lulus/keluar/pindah`) dalam satu halaman — baik secara batch per kelas maupun individual per siswa.

## Output
- Halaman baru `/master/migrasi-status` (rename dari naik-kelas)
- Dua mode: Batch (existing, diperluas) + Individual (search by NIS/Nama)
- 5 aksi: Naik, Tinggal, Lulus, Keluar, Pindah
- Badge `isActive` dihapus dari tabel siswa & halaman detail
- Tombol toggle aktif/nonaktif dihapus dari dropdown menu
- Sidebar: "Naik Kelas" → "Migrasi Status"
- Router: `/master/naik-kelas` → `/master/migrasi-status`
- Types: remove `isActive`, update `PromoteItemDto` action enum
</objective>

<context>
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@apps/frontend/src/pages/master/naik-kelas.tsx
@apps/frontend/src/pages/master/siswa/index.tsx
@apps/frontend/src/pages/master/siswa/[id].tsx
@apps/frontend/src/components/sidebar/index.tsx
@apps/frontend/src/router.tsx
@apps/frontend/src/types/master.ts
</context>

<acceptance_criteria>
## AC-6: Halaman Migrasi Status — Mode Batch
Given operator buka `/master/migrasi-status`
When memilih Tahun Asal + Kelas + Tahun Tujuan → klik "Tampilkan Siswa"
Then muncul tabel preview siswa dengan 5 opsi aksi: Naik, Tinggal, Kelas, Keluar, Pindah

## AC-7: Halaman Migrasi Status — Mode Individual
Given operator buka `/master/migrasi-status`
When pilih tab "Individual" → cari siswa via NIS/Nama → pilih aksi
Then bisa proses 1 siswa dengan aksi keluar/pindah/lulus tanpa parameter kelas

## AC-8: Aksi keluar & pindah di tabel batch
Given tabel preview siswa di mode batch
When operator pilih aksi "Keluar" atau "Pindah"
Then kolom "Kelas Tujuan" disembunyikan (tidak relevan karena tidak ada enrollment baru)

## AC-9: isActive dihapus dari UI
Given halaman Data Siswa atau detail siswa
When tampil
Then tidak ada badge "Aktif/Non-aktif" dan tidak ada tombol toggle di dropdown

## AC-10: Sidebar & routing rename
Given sidebar kiri
When tampil
Then label "Naik Kelas" berubah jadi "Migrasi Status" dengan route `/master/migrasi-status`
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Update types/master.ts: remove isActive, update PromoteItemDto</name>
  <files>apps/frontend/src/types/master.ts</files>
  <action>
  - Hapus `isActive: boolean` dari interface `Student`
  - Hapus `isActive: boolean` dari interface `StudentDetail` jika ada
  - Update `PromotionPreviewStudent` dan `PromoteItemDto` type: action enum tambah `'keluar' | 'pindah'`
  - Update `PromoteResult`: tambah field `keluar: number` dan `pindah: number`
  </action>
  <verify>pnpm --filter frontend typecheck</verify>
  <done>AC-9 prerequisite: types tanpa isActive</done>
</task>

<task type="auto">
  <name>Rename route & sidebar: naik-kelas → migrasi-status</name>
  <files>apps/frontend/src/router.tsx, apps/frontend/src/components/sidebar/index.tsx</files>
  <action>
  Router: ganti import `import NaikKelasPage from '@/pages/master/naik-kelas'` → `import MigrasiStatusPage from '@/pages/master/migrasi-status'`, path `/master/naik-kelas` → `/master/migrasi-status`.
  Sidebar: ganti label NavItem dari "Naik Kelas" ke "Migrasi Status", route dari `/master/naik-kelas` ke `/master/migrasi-status`.
  </action>
  <verify>pnpm --filter frontend typecheck</verify>
  <done>AC-10 satisfied: sidebar & router rename</done>
</task>

<task type="auto">
  <name>Rename file & update halaman: Naik Kelas → Migrasi Status</name>
  <files>apps/frontend/src/pages/master/naik-kelas.tsx</files>
  <action>
  1. COPY `naik-kelas.tsx` → `migrasi-status.tsx` (file baru)
  2. Di migrasi-status.tsx:
     - Rename component `NaikKelasPage` → `MigrasiStatusPage`
     - Ganti judul h1 dari "Naik Kelas" → "Migrasi Status"
     - Update panduan teks: "Proses migrasi status siswa..." (baru)
     - Ganti semua teks "Naik Kelas" → "Migrasi Status" di dialog, button, section hasil
     - Update state type: `studentActions` action dari `'naik' | 'tinggal' | 'lulus'` → tambah `'keluar' | 'pindah'`
     - Di Select Aksi: tambah `<SelectItem value="keluar">Keluar</SelectItem>` dan `<SelectItem value="pindah">Pindah</SelectItem>`
     - Untuk aksi `keluar` dan `pindah`: HIDE kolom "Kelas Tujuan" (tidak butuh toClassId)
     - Update summary counter: tambah `keluar` dan `pindah`
     - Di dialog konfirmasi: tambah stat cards untuk Keluar (merah) dan Pindah (oranye)
     - Di section hasil: tambah stat cards untuk keluar dan pindah
     - Update `handleApplyDefault`: skip `keluar` dan `pindah` (sama seperti `lulus`)
     - Di tabel: untuk `keluar`/`pindah`, tampilkan `—` di kolom Kelas Tujuan (seperti lulus)
  3. HAPUS file `naik-kelas.tsx`
  </action>
  <verify>pnpm --filter frontend typecheck</verify>
  <done>AC-6+AC-8: halaman migrasi status dengan 5 aksi, keluar/pindah tanpa kelas tujuan</done>
</task>

<task type="auto">
  <name>Hapus isActive dari siswa table & detail page</name>
  <files>apps/frontend/src/pages/master/siswa/index.tsx, apps/frontend/src/pages/master/siswa/[id].tsx</files>
  <action>
  siswa/index.tsx:
  - Hapus badge `isActive` (line 337-343) di kolom Status — cukup badge `studentStatus` saja
  - Hapus menu item "Nonaktifkan/Aktifkan" (toggleActive) di dropdown — line 362-368
  - Hapus fungsi `handleToggleActive` dan state `togglingId`
  - Hapus import `Power` jika tidak digunakan di tempat lain
  - Hapus badge isActive di bagian lain (line 570-571)

  siswa/[id].tsx:
  - Hapus section "Status Sistem" yang menampilkan `isActive ? 'Aktif' : 'Non-aktif'`
  - Hanya tampilkan "Status Pendaftaran" dan "Status Akademik"
  </action>
  <verify>pnpm --filter frontend typecheck</verify>
  <done>AC-9 satisfied: tidak ada isActive di UI siswa</done>
</task>

<task type="auto">
  <name>Tambah mode Individual di halaman Migrasi Status</name>
  <files>apps/frontend/src/pages/master/migrasi-status.tsx</files>
  <action>
  Tambah dua tab di atas halaman: "Batch" (default) dan "Individual".
  
  Tab Batch = existing flow (filter kelas+tahun → preview → proses).
  
  Tab Individual:
  - Input search NIS atau Nama (debounce 300ms)
  - Panggil `GET /master/students` untuk mencari
  - Tampilkan hasil: NIS, Nama, Kelas Aktif, Status Saat Ini, dropdown Aksi
  - Aksi: Keluar, Pindah, Lulus (naik dan tinggal tidak relevan untuk individual karena butuh kelas+tahun)
  - Tombol "Proses" kirim POST /academic/promotion dengan 1 item
  - Tampilkan hasil sukses/gagal
  
  Untuk ini perlu endpoint baru atau reuse `/master/students` dengan search query.
  Cukup pakai `GET /master/students?search=xxx` (jika belum ada, tambah query param).
  Jika endpoint search belum ada, fallback: fetch all students lalu filter di frontend.
  </action>
  <verify>pnpm --filter frontend typecheck</verify>
  <done>AC-7 satisfied: mode individual untuk keluar/pindah/lulus per siswa</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Halaman lain selain yang disebut di `files_modified`
- Backend code (sudah di plan 28-01)
- Design system / warna / layout

## SCOPE LIMITS
- File `naik-kelas.tsx` akan DIHAPUS setelah konten dipindah ke `migrasi-status.tsx`
- Mode Individual: jika backend search endpoint belum ada, gunakan filter client-side (fetch all lalu filter) — jangan buat endpoint baru di plan ini
</boundaries>

<verification>
1. `pnpm --filter frontend typecheck` — zero errors
2. `pnpm --filter frontend build` — build sukses
3. Manual test: buka /master/migrasi-status — halaman tampil dengan judul "Migrasi Status"
4. Manual test: mode Batch — pilih kelas, 5 opsi aksi muncul (Naik, Tinggal, Lulus, Keluar, Pindah)
5. Manual test: pilih Keluar/Pindah — kolom Kelas Tujuan hilang
6. Manual test: mode Individual — cari siswa, pilih Keluar, proses
7. Manual test: /master/siswa — hanya 1 badge studentStatus, tidak ada isActive
8. Manual test: sidebar — label "Migrasi Status", bukan "Naik Kelas"
</verification>
</output>
