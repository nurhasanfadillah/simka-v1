---
phase: 24-naik-kelas-frontend
plan: 01
type: execute
wave: 1
depends_on: ["23-01"]
files_modified:
  - apps/frontend/src/types/master.ts
  - apps/frontend/src/router.tsx
  - apps/frontend/src/components/sidebar/index.tsx
  - apps/frontend/src/pages/master/naik-kelas.tsx
autonomous: false
---

<objective>
## Goal
Bangun halaman `/master/naik-kelas` — workflow multi-step untuk proses kenaikan kelas massal: pilih kelas & tahun → preview siswa → tandai action per siswa (naik/tinggal/lulus) → konfirmasi → lihat hasil.

## Purpose
Backend API promotion sudah siap (Phase 23). Frontend ini adalah antarmuka operator untuk menjalankan proses naik kelas tanpa harus enroll satu per satu. Workflow harus jelas dan krusial (ada panduan merah).

## Output
- 5 tipe baru di types/master.ts
- Route /master/naik-kelas di router.tsx
- NavItem "Naik Kelas" di sidebar (Master Data section)
- Halaman naik-kelas.tsx dengan full workflow
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/phases/23-naik-kelas-backend/23-01-SUMMARY.md

## Source Files (pola yang harus diikuti)
@apps/frontend/src/pages/master/kelas/mapping.tsx
@apps/frontend/src/components/sidebar/index.tsx
@apps/frontend/src/router.tsx
@apps/frontend/src/types/master.ts
@apps/frontend/src/lib/api.ts
</context>

<acceptance_criteria>

## AC-1: Route dan sidebar tersedia
```gherkin
Given aplikasi berjalan
When user mengakses /master/naik-kelas
Then halaman tampil tanpa 404, NavItem "Naik Kelas" aktif di sidebar
```

## AC-2: Filter + preview siswa berfungsi
```gherkin
Given user memilih Tahun Asal, Tahun Tujuan, dan Kelas
When user klik "Tampilkan Siswa"
Then tabel siswa muncul dengan kolom NIS, Nama, JK, badge Tunggakan jika hasTunggakan=true
```

## AC-3: Action per siswa bisa dipilih
```gherkin
Given tabel preview sudah muncul
When user mengubah action siswa ke "Tinggal" atau "Lulus"
Then selector action berubah; untuk Naik/Tinggal, selector kelas tujuan tersedia
```

## AC-4: Konfirmasi sebelum proses
```gherkin
Given user klik tombol "Proses Naik Kelas"
When Dialog konfirmasi muncul
Then ringkasan tampil: X Naik, Y Tinggal, Z Lulus; tombol Batal dan Konfirmasi tersedia
```

## AC-5: Hasil proses tampil setelah submit
```gherkin
Given user klik Konfirmasi di dialog
When POST /academic/promotion berhasil
Then bagian Hasil tampil: processed, naik, tinggal, lulus; jika ada errors, daftar siswa gagal tampil
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Types + Route + Sidebar NavItem</name>
  <files>
    apps/frontend/src/types/master.ts,
    apps/frontend/src/router.tsx,
    apps/frontend/src/components/sidebar/index.tsx
  </files>
  <action>
    **types/master.ts — tambahkan di bawah MappingAvailableStudent (akhir file):**
    ```typescript
    export interface PromotionPreviewStudent {
      studentId: number
      nis: string
      name: string
      gender: 'L' | 'P'
      currentClass: { id: number; name: string; level: number }
      hasTunggakan: boolean
    }

    export interface PromotionPreviewResponse {
      fromClass: { id: number; name: string; level: number; schoolYearName: string } | null
      students: PromotionPreviewStudent[]
    }

    export interface PromoteItemDto {
      studentId: number
      action: 'naik' | 'tinggal' | 'lulus'
      toClassId?: number
    }

    export interface PromoteDto {
      fromYearId: number
      toYearId: number
      items: PromoteItemDto[]
    }

    export interface PromoteResult {
      processed: number
      naik: number
      tinggal: number
      lulus: number
      errors: Array<{ studentId: number; message: string }>
    }
    ```

    **router.tsx — tambahkan import dan route:**
    Import: `import NaikKelasPage from '@/pages/master/naik-kelas'`
    Tambahkan di bawah route `/master/kelas/mapping` dan sebelum route `/master/kelas`:
    ```tsx
    <Route
      path="/master/naik-kelas"
      element={
        <ProtectedRoute>
          <AppLayout>
            <NaikKelasPage />
          </AppLayout>
        </ProtectedRoute>
      }
    />
    ```

    **sidebar/index.tsx — tambahkan icon import dan NavItem:**
    Tambahkan `GraduationCap` ke import dari lucide-react (di baris yang sama dengan icon lainnya).
    Tambahkan NavItem setelah Mapping Kelas:
    ```tsx
    <NavItem to="/master/naik-kelas" icon={GraduationCap} label="Naik Kelas" />
    ```
    Letakkan di antara NavItem Mapping Kelas dan Data Siswa.
  </action>
  <verify>
    Jalankan: cd apps/frontend && npx tsc --noEmit
    Tidak ada TypeScript error pada file yang diubah.
  </verify>
  <done>AC-1 partial: route dan sidebar entry tersedia</done>
</task>

<task type="auto">
  <name>Task 2: Halaman naik-kelas.tsx — full workflow</name>
  <files>
    apps/frontend/src/pages/master/naik-kelas.tsx
  </files>
  <action>
    Buat file baru. Ikuti pola dari mapping.tsx (useState lokal, apiClient, useEffect).

    **Import:**
    ```typescript
    import { useState, useEffect } from 'react'
    import { apiClient } from '@/lib/api'
    import type {
      SchoolYear, Class,
      PromotionPreviewResponse, PromotionPreviewStudent,
      PromoteItemDto, PromoteResult
    } from '@/types/master'
    import { Button } from '@/components/ui/button'
    import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
    import { Label } from '@/components/ui/label'
    import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter, DialogDescription } from '@/components/ui/dialog'
    ```

    **State:**
    ```typescript
    const [schoolYears, setSchoolYears] = useState<SchoolYear[]>([])
    const [classes, setClasses] = useState<Class[]>([])
    const [fromYearId, setFromYearId] = useState<number | null>(null)
    const [toYearId, setToYearId] = useState<number | null>(null)
    const [fromClassId, setFromClassId] = useState<number | null>(null)
    const [preview, setPreview] = useState<PromotionPreviewResponse | null>(null)
    // Per-student: action + toClassId
    const [studentActions, setStudentActions] = useState<
      Record<number, { action: 'naik' | 'tinggal' | 'lulus'; toClassId?: number }>
    >({})
    const [defaultToClassId, setDefaultToClassId] = useState<number | null>(null)
    const [loadingPreview, setLoadingPreview] = useState(false)
    const [loadingSubmit, setLoadingSubmit] = useState(false)
    const [error, setError] = useState<string | null>(null)
    const [confirmOpen, setConfirmOpen] = useState(false)
    const [result, setResult] = useState<PromoteResult | null>(null)
    ```

    **useEffect mount — fetch school-years + classes:**
    ```typescript
    useEffect(() => {
      const fetchInitial = async () => {
        try {
          const [yearsRes, classesRes] = await Promise.all([
            apiClient.get<SchoolYear[]>('/master/school-years'),
            apiClient.get<Class[]>('/master/classes'),
          ])
          setSchoolYears(yearsRes.data)
          setClasses(classesRes.data)
          const active = yearsRes.data.find((y) => y.isActive)
          if (active) setFromYearId(active.id)
        } catch {
          setError('Gagal memuat data awal.')
        }
      }
      fetchInitial()
    }, [])
    ```

    **handlePreview:**
    ```typescript
    const handlePreview = async () => {
      if (!fromClassId || !fromYearId) return
      setLoadingPreview(true)
      setError(null)
      setPreview(null)
      setResult(null)
      try {
        const res = await apiClient.get<PromotionPreviewResponse>('/academic/promotion/preview', {
          params: { fromClassId, fromYearId },
        })
        setPreview(res.data)
        // Init default actions: semua 'naik'
        const init: typeof studentActions = {}
        res.data.students.forEach((s) => { init[s.studentId] = { action: 'naik' } })
        setStudentActions(init)
        setDefaultToClassId(null)
      } catch {
        setError('Gagal mengambil data siswa.')
      } finally {
        setLoadingPreview(false)
      }
    }
    ```

    **handleApplyDefault — terapkan defaultToClassId ke semua siswa yang action-nya naik/tinggal:**
    ```typescript
    const handleApplyDefault = () => {
      if (!defaultToClassId) return
      setStudentActions((prev) => {
        const next = { ...prev }
        Object.keys(next).forEach((key) => {
          const id = Number(key)
          if (next[id].action !== 'lulus') {
            next[id] = { ...next[id], toClassId: defaultToClassId }
          }
        })
        return next
      })
    }
    ```

    **handleConfirm — POST promotion:**
    ```typescript
    const handleConfirm = async () => {
      if (!fromYearId || !toYearId || !preview) return
      setLoadingSubmit(true)
      try {
        const items: PromoteItemDto[] = preview.students.map((s) => ({
          studentId: s.studentId,
          action: studentActions[s.studentId]?.action ?? 'naik',
          toClassId: studentActions[s.studentId]?.toClassId,
        }))
        const res = await apiClient.post<PromoteResult>('/academic/promotion', {
          fromYearId,
          toYearId,
          items,
        })
        setResult(res.data)
        setConfirmOpen(false)
        setPreview(null)
      } catch (err: any) {
        setError(err?.response?.data?.message ?? 'Gagal memproses.')
        setConfirmOpen(false)
      } finally {
        setLoadingSubmit(false)
      }
    }
    ```

    **Helper — hitung summary untuk konfirmasi:**
    ```typescript
    const getConfirmSummary = () => {
      const counts = { naik: 0, tinggal: 0, lulus: 0 }
      const tunggakanCount = preview?.students.filter(
        (s) => s.hasTunggakan && studentActions[s.studentId]?.action !== 'lulus'
      ).length ?? 0
      Object.values(studentActions).forEach((a) => counts[a.action]++)
      return { ...counts, tunggakanCount }
    }
    ```

    **JSX Structure:**

    ```tsx
    return (
      <div className="space-y-6">
        {/* Judul */}
        <div>
          <h1 className="text-xl font-semibold text-gray-800">Naik Kelas</h1>
          <p className="text-xs italic text-red-500 mt-1">
            Perhatian: Proses naik kelas bersifat permanen. Pastikan tahun ajaran tujuan,
            kelas, dan data siswa sudah benar sebelum mengkonfirmasi.
          </p>
        </div>

        {/* Filter section */}
        <div className="bg-white rounded-xl border p-5">
          <h2 className="font-medium text-gray-700 mb-4">Parameter Kenaikan Kelas</h2>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
            {/* Tahun Asal */}
            <div className="space-y-1.5">
              <Label>Tahun Ajaran Asal</Label>
              <Select
                value={fromYearId?.toString() ?? ''}
                onValueChange={(v) => { setFromYearId(Number(v)); setPreview(null); setResult(null) }}
              >
                <SelectTrigger><SelectValue placeholder="Pilih tahun asal" /></SelectTrigger>
                <SelectContent>
                  {schoolYears.map((y) => (
                    <SelectItem key={y.id} value={y.id.toString()}>{y.name}</SelectItem>
                  ))}
                </SelectContent>
              </Select>
            </div>
            {/* Kelas */}
            <div className="space-y-1.5">
              <Label>Kelas (dari tahun asal)</Label>
              <Select
                value={fromClassId?.toString() ?? ''}
                onValueChange={(v) => { setFromClassId(Number(v)); setPreview(null); setResult(null) }}
              >
                <SelectTrigger><SelectValue placeholder="Pilih kelas" /></SelectTrigger>
                <SelectContent>
                  {classes.map((c) => (
                    <SelectItem key={c.id} value={c.id.toString()}>
                      {c.name} (Tingkat {c.level})
                    </SelectItem>
                  ))}
                </SelectContent>
              </Select>
            </div>
            {/* Tahun Tujuan */}
            <div className="space-y-1.5">
              <Label>Tahun Ajaran Tujuan</Label>
              <Select
                value={toYearId?.toString() ?? ''}
                onValueChange={(v) => { setToYearId(Number(v)); setPreview(null); setResult(null) }}
              >
                <SelectTrigger><SelectValue placeholder="Pilih tahun tujuan" /></SelectTrigger>
                <SelectContent>
                  {schoolYears.map((y) => (
                    <SelectItem key={y.id} value={y.id.toString()}>{y.name}</SelectItem>
                  ))}
                </SelectContent>
              </Select>
            </div>
          </div>
          <div className="mt-4 flex gap-2">
            <Button
              onClick={handlePreview}
              disabled={!fromClassId || !fromYearId || loadingPreview}
            >
              {loadingPreview ? 'Memuat...' : 'Tampilkan Siswa'}
            </Button>
          </div>
          {error && <p className="text-sm text-red-500 mt-2">{error}</p>}
        </div>

        {/* Preview table */}
        {preview && preview.students.length > 0 && (
          <div className="bg-white rounded-xl border">
            {/* Header */}
            <div className="px-5 py-4 border-b flex items-center justify-between">
              <div>
                <h2 className="font-medium text-gray-800">
                  Siswa di {preview.fromClass?.name} — {preview.fromClass?.schoolYearName}
                </h2>
                <p className="text-xs text-gray-500 mt-0.5">{preview.students.length} siswa</p>
              </div>
            </div>

            {/* Kelas tujuan default */}
            <div className="px-5 py-3 border-b bg-gray-50 flex items-center gap-3">
              <Label className="text-sm text-gray-600 whitespace-nowrap">Kelas Tujuan Default:</Label>
              <div className="w-48">
                <Select
                  value={defaultToClassId?.toString() ?? ''}
                  onValueChange={(v) => setDefaultToClassId(Number(v))}
                >
                  <SelectTrigger className="h-8 text-sm"><SelectValue placeholder="Pilih kelas" /></SelectTrigger>
                  <SelectContent>
                    {classes.map((c) => (
                      <SelectItem key={c.id} value={c.id.toString()}>
                        {c.name} (T{c.level})
                      </SelectItem>
                    ))}
                  </SelectContent>
                </Select>
              </div>
              <Button variant="outline" size="sm" onClick={handleApplyDefault} disabled={!defaultToClassId}>
                Terapkan ke Semua
              </Button>
            </div>

            {/* Table */}
            <div className="overflow-x-auto">
              <table className="w-full text-sm">
                <thead>
                  <tr className="bg-gray-50 border-b">
                    <th className="text-left px-4 py-2 font-medium text-gray-600">NIS</th>
                    <th className="text-left px-4 py-2 font-medium text-gray-600">Nama</th>
                    <th className="text-left px-4 py-2 font-medium text-gray-600">JK</th>
                    <th className="text-left px-4 py-2 font-medium text-gray-600">Tunggakan</th>
                    <th className="text-left px-4 py-2 font-medium text-gray-600">Aksi</th>
                    <th className="text-left px-4 py-2 font-medium text-gray-600">Kelas Tujuan</th>
                  </tr>
                </thead>
                <tbody>
                  {preview.students.map((s) => {
                    const sa = studentActions[s.studentId] ?? { action: 'naik' as const }
                    return (
                      <tr key={s.studentId} className="border-b last:border-0 hover:bg-gray-50">
                        <td className="px-4 py-2 font-mono text-xs">{s.nis}</td>
                        <td className="px-4 py-2">{s.name}</td>
                        <td className="px-4 py-2 text-gray-500">{s.gender === 'L' ? 'L' : 'P'}</td>
                        <td className="px-4 py-2">
                          {s.hasTunggakan ? (
                            <span className="text-xs bg-red-100 text-red-600 px-1.5 py-0.5 rounded">
                              Ada
                            </span>
                          ) : (
                            <span className="text-xs text-gray-400">—</span>
                          )}
                        </td>
                        <td className="px-4 py-2">
                          <Select
                            value={sa.action}
                            onValueChange={(v: 'naik' | 'tinggal' | 'lulus') =>
                              setStudentActions((prev) => ({
                                ...prev,
                                [s.studentId]: { ...prev[s.studentId], action: v },
                              }))
                            }
                          >
                            <SelectTrigger className="h-7 w-28 text-xs"><SelectValue /></SelectTrigger>
                            <SelectContent>
                              <SelectItem value="naik">Naik</SelectItem>
                              <SelectItem value="tinggal">Tinggal</SelectItem>
                              <SelectItem value="lulus">Lulus</SelectItem>
                            </SelectContent>
                          </Select>
                        </td>
                        <td className="px-4 py-2">
                          {sa.action !== 'lulus' ? (
                            <Select
                              value={sa.toClassId?.toString() ?? ''}
                              onValueChange={(v) =>
                                setStudentActions((prev) => ({
                                  ...prev,
                                  [s.studentId]: { ...prev[s.studentId], toClassId: Number(v) },
                                }))
                              }
                            >
                              <SelectTrigger className="h-7 w-36 text-xs">
                                <SelectValue placeholder="Pilih kelas" />
                              </SelectTrigger>
                              <SelectContent>
                                {classes.map((c) => (
                                  <SelectItem key={c.id} value={c.id.toString()}>
                                    {c.name} (T{c.level})
                                  </SelectItem>
                                ))}
                              </SelectContent>
                            </Select>
                          ) : (
                            <span className="text-xs text-gray-400">—</span>
                          )}
                        </td>
                      </tr>
                    )
                  })}
                </tbody>
              </table>
            </div>

            {/* Footer — tombol proses */}
            <div className="px-5 py-4 border-t">
              <Button
                onClick={() => setConfirmOpen(true)}
                disabled={!toYearId}
                className="bg-[#00A651] hover:bg-[#008c44] text-white"
              >
                Proses Naik Kelas
              </Button>
              {!toYearId && (
                <p className="text-xs text-amber-600 mt-1">Pilih Tahun Ajaran Tujuan terlebih dahulu.</p>
              )}
            </div>
          </div>
        )}

        {/* Empty state jika preview kosong */}
        {preview && preview.students.length === 0 && (
          <div className="bg-white rounded-xl border px-5 py-10 text-center text-sm text-gray-400">
            Tidak ada siswa di kelas dan tahun ajaran yang dipilih.
          </div>
        )}

        {/* Dialog konfirmasi */}
        {(() => {
          const summary = preview ? getConfirmSummary() : { naik: 0, tinggal: 0, lulus: 0, tunggakanCount: 0 }
          return (
            <Dialog open={confirmOpen} onOpenChange={setConfirmOpen}>
              <DialogContent>
                <DialogHeader>
                  <DialogTitle>Konfirmasi Proses Naik Kelas</DialogTitle>
                  <DialogDescription asChild>
                    <div className="space-y-3 pt-2">
                      <div className="grid grid-cols-3 gap-3 text-center">
                        <div className="bg-green-50 rounded-lg p-3">
                          <p className="text-2xl font-bold text-green-700">{summary.naik}</p>
                          <p className="text-xs text-green-600">Naik Kelas</p>
                        </div>
                        <div className="bg-amber-50 rounded-lg p-3">
                          <p className="text-2xl font-bold text-amber-700">{summary.tinggal}</p>
                          <p className="text-xs text-amber-600">Tinggal Kelas</p>
                        </div>
                        <div className="bg-blue-50 rounded-lg p-3">
                          <p className="text-2xl font-bold text-blue-700">{summary.lulus}</p>
                          <p className="text-xs text-blue-600">Lulus</p>
                        </div>
                      </div>
                      {summary.tunggakanCount > 0 && (
                        <p className="text-xs text-red-500 bg-red-50 rounded p-2">
                          ⚠ {summary.tunggakanCount} siswa masih memiliki tunggakan.
                          Proses tetap bisa dilanjutkan, namun tagihan lama tidak otomatis terhapus.
                        </p>
                      )}
                      <p className="text-xs text-gray-500">
                        Aksi ini akan membuat enrollment baru di tahun ajaran tujuan. Tidak bisa dibatalkan.
                      </p>
                    </div>
                  </DialogDescription>
                </DialogHeader>
                <DialogFooter>
                  <Button variant="outline" onClick={() => setConfirmOpen(false)}>Batal</Button>
                  <Button
                    onClick={handleConfirm}
                    disabled={loadingSubmit}
                    className="bg-[#00A651] hover:bg-[#008c44] text-white"
                  >
                    {loadingSubmit ? 'Memproses...' : 'Konfirmasi'}
                  </Button>
                </DialogFooter>
              </DialogContent>
            </Dialog>
          )
        })()}

        {/* Hasil proses */}
        {result && (
          <div className="bg-white rounded-xl border p-5 space-y-4">
            <h2 className="font-medium text-gray-800">Hasil Proses Naik Kelas</h2>
            <div className="grid grid-cols-2 md:grid-cols-4 gap-3 text-center">
              <div className="bg-gray-50 rounded-lg p-3">
                <p className="text-xl font-bold text-gray-700">{result.processed}</p>
                <p className="text-xs text-gray-500">Diproses</p>
              </div>
              <div className="bg-green-50 rounded-lg p-3">
                <p className="text-xl font-bold text-green-700">{result.naik}</p>
                <p className="text-xs text-green-600">Naik Kelas</p>
              </div>
              <div className="bg-amber-50 rounded-lg p-3">
                <p className="text-xl font-bold text-amber-700">{result.tinggal}</p>
                <p className="text-xs text-amber-600">Tinggal Kelas</p>
              </div>
              <div className="bg-blue-50 rounded-lg p-3">
                <p className="text-xl font-bold text-blue-700">{result.lulus}</p>
                <p className="text-xs text-blue-600">Lulus</p>
              </div>
            </div>
            {result.errors.length > 0 && (
              <div className="border border-red-200 rounded-lg p-3">
                <p className="text-sm font-medium text-red-600 mb-2">Siswa Gagal Diproses ({result.errors.length})</p>
                <ul className="space-y-1">
                  {result.errors.map((e) => (
                    <li key={e.studentId} className="text-xs text-red-500">
                      ID {e.studentId}: {e.message}
                    </li>
                  ))}
                </ul>
              </div>
            )}
            <Button variant="outline" onClick={() => { setResult(null); setFromClassId(null) }}>
              Proses Kelas Lain
            </Button>
          </div>
        )}
      </div>
    )
    ```

    Wrap semua JSX dalam komponen `export default function NaikKelasPage()`.
  </action>
  <verify>
    cd apps/frontend && npx tsc --noEmit
    Tidak ada TypeScript error.
    File ada di apps/frontend/src/pages/master/naik-kelas.tsx
  </verify>
  <done>AC-1 penuh + AC-2 + AC-3 + AC-4 + AC-5 terpenuhi dalam implementasi</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Halaman /master/naik-kelas dengan:
    - NavItem "Naik Kelas" (icon GraduationCap) di sidebar Master Data section
    - Filter section: Tahun Asal, Kelas, Tahun Tujuan + tombol "Tampilkan Siswa"
    - Preview table: NIS, Nama, JK, badge Tunggakan, selector Aksi (Naik/Tinggal/Lulus), selector Kelas Tujuan
    - "Kelas Tujuan Default" apply-to-all di atas tabel
    - Dialog konfirmasi dengan stat cards (Naik/Tinggal/Lulus) + warning tunggakan
    - Hasil section setelah submit
    - Panduan merah italic di judul halaman
  </what-built>
  <how-to-verify>
    1. Jalankan: docker start simka-postgres simka-redis (jika belum jalan)
    2. Jalankan: pnpm dev (dari root simka-v1)
    3. Buka http://localhost:5173, login sebagai Super Admin
    4. Cek sidebar Master Data — muncul "Naik Kelas" setelah "Mapping Kelas"
    5. Klik "Naik Kelas" → halaman terbuka, panduan merah italic terlihat
    6. Pilih Tahun Asal dan Kelas → klik "Tampilkan Siswa" → tabel siswa muncul
    7. Ubah action salah satu siswa ke "Lulus" → kolom Kelas Tujuan berubah jadi "—"
    8. Pilih Tahun Tujuan → tombol "Proses Naik Kelas" aktif
    9. Klik "Proses Naik Kelas" → Dialog konfirmasi muncul dengan stat cards
    10. Klik Batal → dialog tutup, data tetap ada
    11. (Opsional) Klik Konfirmasi → hasil proses tampil
  </how-to-verify>
  <resume-signal>Ketik "approved" untuk lanjut, atau deskripsikan issue yang ditemukan</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- apps/backend/ — tidak ada perubahan backend di plan ini
- apps/frontend/src/pages/ selain naik-kelas.tsx yang baru dibuat
- apps/frontend/src/components/ selain sidebar/index.tsx
- apps/frontend/src/stores/ — auth store tidak disentuh
- Komponen DeleteErrorModal dan shadcn components lainnya

## SCOPE LIMITS
- Tidak ada validasi bahwa toClassId yang dipilih sesuai level (backend tidak enforce, frontend juga tidak)
- Tidak ada filter kelas berdasarkan school unit (simplicity)
- Tidak ada RBAC visibility check — semua menu tampil, permission check di backend
- Tidak ada fitur edit/cancel setelah proses selesai

</boundaries>

<verification>
Sebelum menyatakan plan complete:
- [ ] npx tsc --noEmit di apps/frontend tanpa error
- [ ] Route /master/naik-kelas tersedia (tidak 404)
- [ ] NavItem "Naik Kelas" muncul di sidebar
- [ ] Filter + preview berfungsi
- [ ] Dialog konfirmasi muncul dengan stat cards
- [ ] Hasil proses tampil setelah submit
</verification>

<success_criteria>
- Route, sidebar, types semua terpasang
- Halaman naik-kelas.tsx berfungsi end-to-end
- TypeScript strict pass
- User verification approved
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/24-naik-kelas-frontend/24-01-SUMMARY.md`
</output>
