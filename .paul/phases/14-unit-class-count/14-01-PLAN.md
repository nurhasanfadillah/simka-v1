---
phase: 14-unit-class-count
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/master/school-units/school-units.service.ts
  - apps/frontend/src/types/master.ts
  - apps/frontend/src/pages/master/kelas/index.tsx
autonomous: true
---

<objective>
## Goal
Tampilkan jumlah kelas di tabel Unit Sekolah dengan menambah field `classCount` pada response GET /master/school-units.

## Purpose
Operator perlu tahu berapa kelas yang dimiliki setiap unit sekolah langsung dari daftar — tanpa perlu berpindah tab.

## Output
- Backend: `findAll()` school-units return field `classCount`
- Frontend: kolom "Jumlah Kelas" di tabel tab Unit Sekolah
</objective>

<context>
@apps/backend/src/master/school-units/school-units.service.ts
@apps/frontend/src/types/master.ts
@apps/frontend/src/pages/master/kelas/index.tsx
</context>

<acceptance_criteria>
## AC-1: Jumlah kelas tampil di tabel
```gherkin
Given tab Unit Sekolah dibuka
When data unit sekolah ditampilkan
Then setiap baris menampilkan jumlah kelas yang dimiliki unit tersebut
  AND unit tanpa kelas menampilkan 0
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Tambah classCount ke backend + type + kolom tabel</name>
  <files>
    apps/backend/src/master/school-units/school-units.service.ts,
    apps/frontend/src/types/master.ts,
    apps/frontend/src/pages/master/kelas/index.tsx
  </files>
  <action>
    **1. Backend — school-units.service.ts:**
    Ubah `findAll()` menggunakan LEFT JOIN + count ke tabel classes:
    ```ts
    findAll() {
      return this.db
        .select({
          id: schoolUnits.id,
          name: schoolUnits.name,
          code: schoolUnits.code,
          createdAt: schoolUnits.createdAt,
          updatedAt: schoolUnits.updatedAt,
          classCount: count(classes.id),
        })
        .from(schoolUnits)
        .leftJoin(classes, eq(classes.schoolUnitId, schoolUnits.id))
        .groupBy(schoolUnits.id, schoolUnits.name, schoolUnits.code, schoolUnits.createdAt, schoolUnits.updatedAt)
        .orderBy(asc(schoolUnits.name));
    }
    ```
    Import yang dibutuhkan: `count` sudah ada, `classes` sudah ada, tambah `leftJoin` — sudah tersedia di drizzle-orm, tidak perlu import tambahan karena leftJoin adalah method dari query builder.

    **2. Frontend — types/master.ts:**
    Tambah `classCount: number` ke interface SchoolUnit:
    ```ts
    export interface SchoolUnit {
      id: number
      name: string
      code: string
      classCount: number
      createdAt: string
      updatedAt: string
    }
    ```

    **3. Frontend — kelas/index.tsx:**
    Di tabel Unit Sekolah:
    - Tambah header "Jumlah Kelas" setelah "Kode"
    - Tambah cell `{row.classCount}` di setiap baris
    - Update `colSpan` skeleton loader dari 3 ke 4
  </action>
  <verify>
    - `cd apps/backend && npx tsc --noEmit` — 0 error
    - `cd apps/frontend && npx tsc --noEmit` — 0 error
  </verify>
  <done>AC-1 satisfied: kolom Jumlah Kelas tampil di tabel Unit Sekolah</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Endpoint lain school-units (GET/:id, POST, PATCH, DELETE)
- Halaman lain selain kelas/index.tsx

## SCOPE LIMITS
- Hanya kolom display — tidak ada filter atau sorting berdasarkan classCount
</boundaries>

<verification>
- [ ] `npx tsc --noEmit` backend + frontend 0 error
- [ ] response GET /master/school-units punya field classCount
- [ ] Tabel Unit Sekolah punya kolom "Jumlah Kelas"
</verification>

<success_criteria>
- TypeScript 0 error di backend dan frontend
- AC-1 terpenuhi
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/14-unit-class-count/14-01-SUMMARY.md`
</output>
