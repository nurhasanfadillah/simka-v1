---
phase: 02-billing-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/db/schema/financial.schema.ts
  - apps/backend/drizzle/ (generated migration file)
autonomous: true
---

<objective>
## Goal
Tambah tabel `payment_templates`, `bills`, `bill_months` ke financial schema + generate dan apply migrasi Drizzle.

## Purpose
Billing Engine tidak bisa dibangun tanpa tabel-tabel ini di database. Schema ini adalah fondasi Plan 02-02 (PaymentTemplates API) dan 02-03 (Billing Engine).

## Output
- `financial.schema.ts` diperluas dengan 3 tabel baru + 2 enum baru + relations
- Migration SQL baru di `apps/backend/drizzle/`
- Migrasi ter-apply ke PostgreSQL
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@apps/backend/src/db/schema/financial.schema.ts
@apps/backend/src/db/schema/academic.schema.ts
@apps/backend/src/db/schema/index.ts
@apps/backend/drizzle.config.ts
</context>

<acceptance_criteria>

## AC-1: Tabel baru ada di schema TypeScript
```gherkin
Given financial.schema.ts di-import
When TypeScript di-compile
Then tidak ada error, semua tabel exported: paymentTemplates, bills, billMonths
```

## AC-2: Migration ter-generate
```gherkin
Given schema sudah diupdate
When pnpm --filter backend db:generate dijalankan
Then file SQL baru muncul di apps/backend/drizzle/ dengan CREATE TABLE untuk ketiga tabel
```

## AC-3: Migration ter-apply
```gherkin
Given Docker PostgreSQL running dan migration file ada
When pnpm --filter backend db:migrate dijalankan
Then tabel payment_templates, bills, bill_months muncul di database tanpa error
```

## AC-4: Unique constraints benar
```gherkin
Given tabel bills dan payment_templates sudah ada di DB
When INSERT dua baris dengan kombinasi (student_id, payment_template_id) yang sama ke bills
Then PostgreSQL reject dengan unique violation 23505
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extend financial.schema.ts</name>
  <files>apps/backend/src/db/schema/financial.schema.ts</files>
  <action>
    Tambah ke financial.schema.ts setelah definisi `paymentPosts`. Import tambahan yang dibutuhkan:
    `integer, smallint, bigint, unique, numeric` dari drizzle-orm/pg-core.

    **1. Import baru:**
    Tambah `integer, smallint, bigint, unique` ke import yang sudah ada (cek duplikasi — `varchar`, `text`, `timestamp`, `pgEnum`, `bigserial` sudah ada).

    **2. Enum baru:**
    ```typescript
    export const billStatusEnum = pgEnum('bill_status_enum', [
      'belum_bayar',
      'cicilan',
      'lunas',
    ]);

    export const billMonthStatusEnum = pgEnum('bill_month_status_enum', [
      'belum_bayar',
      'lunas',
    ]);
    ```

    **3. Tabel `paymentTemplates`:**
    ```typescript
    export const paymentTemplates = pgTable(
      'payment_templates',
      {
        id: bigserial('id', { mode: 'number' }).primaryKey(),
        classId: bigint('class_id', { mode: 'number' })
          .notNull()
          .references(() => classes.id),
        paymentPostId: bigint('payment_post_id', { mode: 'number' })
          .notNull()
          .references(() => paymentPosts.id),
        schoolYearId: bigint('school_year_id', { mode: 'number' })
          .notNull()
          .references(() => schoolYears.id),
        amount: integer('amount').notNull(), // nominal dalam rupiah (integer, tanpa desimal)
        createdAt: timestamp('created_at').defaultNow().notNull(),
        updatedAt: timestamp('updated_at').defaultNow().notNull(),
      },
      (table) => [
        unique('uq_payment_template').on(
          table.classId,
          table.paymentPostId,
          table.schoolYearId,
        ),
      ],
    );
    ```

    **4. Tabel `bills`:**
    ```typescript
    export const bills = pgTable(
      'bills',
      {
        id: bigserial('id', { mode: 'number' }).primaryKey(),
        invoiceNumber: varchar('invoice_number', { length: 30 }).notNull().unique(),
        studentId: bigint('student_id', { mode: 'number' })
          .notNull()
          .references(() => students.id),
        paymentTemplateId: bigint('payment_template_id', { mode: 'number' })
          .notNull()
          .references(() => paymentTemplates.id),
        schoolYearId: bigint('school_year_id', { mode: 'number' })
          .notNull()
          .references(() => schoolYears.id),
        totalAmount: integer('total_amount').notNull(),
        status: billStatusEnum('status').default('belum_bayar').notNull(),
        createdAt: timestamp('created_at').defaultNow().notNull(),
        updatedAt: timestamp('updated_at').defaultNow().notNull(),
      },
      (table) => [
        unique('uq_bill').on(table.studentId, table.paymentTemplateId),
      ],
    );
    ```

    **5. Tabel `billMonths`:**
    ```typescript
    export const billMonths = pgTable(
      'bill_months',
      {
        id: bigserial('id', { mode: 'number' }).primaryKey(),
        billId: bigint('bill_id', { mode: 'number' })
          .notNull()
          .references(() => bills.id, { onDelete: 'cascade' }),
        month: smallint('month').notNull(),   // 1–12
        year: smallint('year').notNull(),     // e.g. 2026
        amount: integer('amount').notNull(),  // nominal bulan ini (bisa beda per bulan jika cicilan)
        status: billMonthStatusEnum('status').default('belum_bayar').notNull(),
        paidAt: timestamp('paid_at'),
        createdAt: timestamp('created_at').defaultNow().notNull(),
      },
      (table) => [
        unique('uq_bill_month').on(table.billId, table.month, table.year),
      ],
    );
    ```

    **6. Relations (tambah di bawah semua tabel):**
    ```typescript
    export const paymentTemplatesRelations = relations(paymentTemplates, ({ one, many }) => ({
      class: one(classes, { fields: [paymentTemplates.classId], references: [classes.id] }),
      paymentPost: one(paymentPosts, { fields: [paymentTemplates.paymentPostId], references: [paymentPosts.id] }),
      schoolYear: one(schoolYears, { fields: [paymentTemplates.schoolYearId], references: [schoolYears.id] }),
      bills: many(bills),
    }));

    export const billsRelations = relations(bills, ({ one, many }) => ({
      student: one(students, { fields: [bills.studentId], references: [students.id] }),
      paymentTemplate: one(paymentTemplates, { fields: [bills.paymentTemplateId], references: [paymentTemplates.id] }),
      schoolYear: one(schoolYears, { fields: [bills.schoolYearId], references: [schoolYears.id] }),
      billMonths: many(billMonths),
    }));

    export const billMonthsRelations = relations(billMonths, ({ one }) => ({
      bill: one(bills, { fields: [billMonths.billId], references: [bills.id] }),
    }));
    ```

    **PENTING:**
    - `classes`, `schoolYears`, `students` di-import dari academic.schema — gunakan cross-file reference
    - financial.schema sudah import dari drizzle-orm/pg-core — JANGAN duplikasi import
    - amount pakai `integer` bukan `numeric` — nominal SPP selalu bilangan bulat (rupiah)
    - `billMonths.billId` pakai `onDelete: 'cascade'` — hapus bill → hapus semua bulannya
    - JANGAN tambah `onDelete: cascade` ke bills.studentId atau bills.paymentTemplateId — protect data

    **Cross-file imports yang dibutuhkan di financial.schema.ts:**
    ```typescript
    import { classes, schoolYears, students } from './academic.schema';
    ```
  </action>
  <verify>
    ```bash
    pnpm --filter backend build
    # → TypeScript compile tanpa error
    # → "Successfully compiled" atau "Build complete"
    ```
  </verify>
  <done>AC-1 satisfied: paymentTemplates, bills, billMonths exported, build bersih</done>
</task>

<task type="auto">
  <name>Task 2: Generate dan Apply Migration</name>
  <files>apps/backend/drizzle/ (file SQL baru)</files>
  <action>
    **1. Generate migration:**
    ```bash
    # Dari root atau dengan filter:
    pnpm --filter backend db:generate
    ```
    Ini akan membuat file SQL baru di `apps/backend/drizzle/` (contoh: `0001_*.sql`).

    **2. Verifikasi isi migration:**
    File SQL harus mengandung:
    - `CREATE TYPE "public"."bill_status_enum"`
    - `CREATE TYPE "public"."bill_month_status_enum"`
    - `CREATE TABLE "payment_templates"`
    - `CREATE TABLE "bills"`
    - `CREATE TABLE "bill_months"`
    - `UNIQUE` constraints untuk ketiga tabel

    **3. Apply migration:**
    ```bash
    # Pastikan Docker PostgreSQL running: docker start simka-postgres
    pnpm --filter backend db:migrate
    ```

    Avoid:
    - Jangan edit file SQL yang sudah di-generate — biarkan drizzle-kit generate otomatis
    - Jangan jalankan db:push (pakai migrate untuk production-safe workflow)
  </action>
  <verify>
    ```bash
    # Cek file migration baru muncul:
    ls apps/backend/drizzle/

    # Verifikasi tabel ada di DB:
    docker exec -it simka-postgres psql -U postgres -d simka -c "\dt"
    # → Harus tampil: payment_templates, bills, bill_months

    # Verifikasi struktur bills:
    docker exec -it simka-postgres psql -U postgres -d simka -c "\d bills"
    # → Harus ada kolom: invoice_number (unique), status (enum), total_amount
    ```
  </verify>
  <done>AC-2 dan AC-3 satisfied: migration file ada, tabel muncul di database</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `apps/backend/src/db/schema/auth.schema.ts` — auth system stable
- `apps/backend/src/db/schema/academic.schema.ts` — academic schema stable
- `apps/backend/src/db/schema/audit.schema.ts` — audit schema stable
- `apps/backend/drizzle/0000_cute_talos.sql` — existing migration tidak diubah
- `apps/backend/src/db/seed.ts` — seed tidak diubah di plan ini

## SCOPE LIMITS
- Plan ini hanya schema + migration — tidak ada service, controller, atau module NestJS
- Tidak ada API endpoint di plan ini
- Tidak ada perubahan frontend
- Tidak ada seed data untuk tabel baru (seed billing akan dilakukan di plan terpisah)

</boundaries>

<verification>
Sebelum declare plan complete:
- [ ] `pnpm --filter backend build` tanpa TypeScript error
- [ ] `pnpm --filter backend db:generate` menghasilkan file SQL baru
- [ ] File SQL mengandung CREATE TABLE untuk payment_templates, bills, bill_months
- [ ] `pnpm --filter backend db:migrate` sukses tanpa error
- [ ] `\dt` di psql menampilkan ketiga tabel baru
</verification>

<success_criteria>
- Task 1 dan Task 2 completed
- Build TypeScript bersih
- Tiga tabel baru ada di database dengan struktur dan constraints yang benar
- Pola consistent dengan schema existing (bigserial PK, timestamps, unique constraints via array)
</success_criteria>

<output>
Setelah completion, buat `.paul/phases/02-billing-engine/02-01-SUMMARY.md`
</output>
