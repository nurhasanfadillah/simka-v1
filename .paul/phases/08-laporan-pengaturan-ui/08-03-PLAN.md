---
phase: 08-laporan-pengaturan-ui
plan: 03
type: execute
wave: 1
depends_on: ["08-02"]
files_modified:
  - apps/frontend/src/pages/pengaturan/profil/index.tsx
  - apps/frontend/src/pages/pengaturan/users/index.tsx
  - apps/frontend/src/pages/pengaturan/roles/index.tsx
  - apps/frontend/src/router.tsx
  - apps/frontend/src/components/sidebar/index.tsx
autonomous: true
---

<objective>
## Goal
Bangun 3 halaman Pengaturan: Profil Pengguna (read-only via GET /auth/me), dan 2 placeholder halaman Users + Roles yang menjelaskan fitur belum tersedia. Daftarkan ke router.

## Purpose
Sidebar sudah punya link /pengaturan/users dan /pengaturan/roles — tanpa route, user mendapat 404. Profil memberi nilai langsung (lihat lastLogin, permissions). Placeholder mencegah 404 sambil memberi konteks "coming soon".

## Output
- 3 halaman pengaturan live (profil + users placeholder + roles placeholder)
- Sidebar dilengkapi link "Profil Saya"
- Router terdaftar untuk semua 3 route baru
- Phase 08 selesai (plan terakhir)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/phases/08-laporan-pengaturan-ui/08-02-SUMMARY.md

## Source Files (referensi pola)
@apps/frontend/src/stores/auth.store.ts
@apps/frontend/src/components/sidebar/index.tsx
@apps/frontend/src/router.tsx
@apps/frontend/src/pages/dashboard/index.tsx
</context>

<acceptance_criteria>

## AC-1: Profil Pengguna
```gherkin
Given user membuka /pengaturan/profil
When halaman dimuat
Then tampil info: Nama, Email, Role, Status Akun, Terakhir Login
And tampil daftar permission codes yang dimiliki user
And data diambil dari GET /auth/me (bukan hanya dari store)
```

## AC-2: Placeholder Users
```gherkin
Given user membuka /pengaturan/users (dari sidebar "Pengguna")
When halaman dimuat
Then tampil pesan informatif bahwa fitur manajemen pengguna belum tersedia
And tidak ada 404 error
```

## AC-3: Placeholder Roles
```gherkin
Given user membuka /pengaturan/roles (dari sidebar "Role & Akses")
When halaman dimuat
Then tampil pesan informatif bahwa fitur manajemen role belum tersedia
And tidak ada 404 error
```

## AC-4: Routes + Sidebar
```gherkin
Given semua route terdaftar di router.tsx
When user navigasi via sidebar
Then /pengaturan/profil, /pengaturan/users, /pengaturan/roles semuanya render
And sidebar memiliki link "Profil Saya" menuju /pengaturan/profil
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Buat 3 halaman Pengaturan + tambah AuthProfile type</name>
  <files>
    apps/frontend/src/types/master.ts,
    apps/frontend/src/pages/pengaturan/profil/index.tsx,
    apps/frontend/src/pages/pengaturan/users/index.tsx,
    apps/frontend/src/pages/pengaturan/roles/index.tsx
  </files>
  <action>
    1. Di `types/master.ts`, tambah type di akhir file:
       ```ts
       export interface AuthProfile {
         id: number
         name: string
         email: string
         role: string
         isActive: boolean
         lastLogin: string | null
         permissions: string[]
       }
       ```

    2. Buat `apps/frontend/src/pages/pengaturan/profil/index.tsx`:
       - Fetch GET `/auth/me` → state data (AuthProfile | null), loading, error
       - Layout:
         * Header: "Profil Saya" + subtitle
         * Card info: grid 2 kolom, tampil Nama, Email, Role, Status Akun (badge hijau/abu), Terakhir Login
         * Section Permissions: daftar badge kecil untuk setiap permission code (gray-100)
         * Loading skeleton + error banner
       - Format tanggal lastLogin: `new Date(x).toLocaleString('id-ID')`, atau "Belum pernah login" jika null
       - Gunakan pola p-6, rounded-xl, shadow-sm, border-gray-100

    3. Buat `apps/frontend/src/pages/pengaturan/users/index.tsx`:
       - Halaman placeholder sederhana, tanpa fetch
       - Layout: p-6, header "Pengguna", info card dengan ikon (Construction atau ShieldAlert dari lucide-react)
       - Pesan: "Fitur manajemen pengguna sedang dalam pengembangan. Untuk saat ini, pengelolaan akun dilakukan langsung melalui Super Admin."
       - Tidak ada tabel, tidak ada form

    4. Buat `apps/frontend/src/pages/pengaturan/roles/index.tsx`:
       - Sama seperti users placeholder
       - Pesan: "Fitur manajemen role dan hak akses sedang dalam pengembangan."

    Avoid: jangan buat form edit profil, jangan buat komponen baru.
    Avoid: jangan import dari library baru selain lucide-react (sudah ada).
  </action>
  <verify>
    - `pnpm --filter frontend tsc --noEmit` → 0 errors
    - 3 file halaman terbuat di direktori yang benar
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: ketiga halaman terbuat dengan konten yang sesuai</done>
</task>

<task type="auto">
  <name>Task 2: Router + Sidebar update</name>
  <files>
    apps/frontend/src/router.tsx,
    apps/frontend/src/components/sidebar/index.tsx
  </files>
  <action>
    1. Di `apps/frontend/src/router.tsx`:
       - Tambah imports:
         ```ts
         import ProfilPage from '@/pages/pengaturan/profil'
         import PengaturanUsersPage from '@/pages/pengaturan/users'
         import PengaturanRolesPage from '@/pages/pengaturan/roles'
         ```
       - Tambah 3 routes setelah /laporan/rekap-pos (sebelum catch-all):
         ```tsx
         <Route path="/pengaturan/profil" element={<ProtectedRoute><AppLayout><ProfilPage /></AppLayout></ProtectedRoute>} />
         <Route path="/pengaturan/users" element={<ProtectedRoute><AppLayout><PengaturanUsersPage /></AppLayout></ProtectedRoute>} />
         <Route path="/pengaturan/roles" element={<ProtectedRoute><AppLayout><PengaturanRolesPage /></AppLayout></ProtectedRoute>} />
         ```

    2. Di `apps/frontend/src/components/sidebar/index.tsx`:
       - Tambah NavItem untuk Profil Saya sebelum NavItem Users:
         ```tsx
         <NavItem to="/pengaturan/profil" icon={User} label="Profil Saya" />
         ```
       - Pastikan icon `User` sudah diimport dari lucide-react (tambah ke import jika belum ada)

    Avoid: jangan ubah NavItem Users/Roles yang sudah ada, hanya tambah NavItem profil.
    Avoid: jangan ubah routing yang sudah ada, hanya append di bawah /laporan/rekap-pos.
  </action>
  <verify>
    - `pnpm --filter frontend tsc --noEmit` → 0 errors
    - grep "pengaturan/profil\|pengaturan/users\|pengaturan/roles" router.tsx → 3 matches
  </verify>
  <done>AC-4 satisfied: 3 routes terdaftar, sidebar punya link Profil Saya</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- apps/backend/ (tidak ada endpoint baru — scope B adalah frontend-only)
- apps/frontend/src/stores/auth.store.ts (auth store sudah final)
- apps/frontend/src/pages/laporan/ (laporan sudah selesai di 08-01, 08-02)
- apps/frontend/src/components/sidebar/index.tsx selain menambah 1 NavItem profil

## SCOPE LIMITS
- Tidak ada form edit profil (baca saja — scope B)
- Tidak ada ganti password form (butuh endpoint baru — out of scope)
- Tidak ada tabel user/role management (backend belum ada — placeholder saja)
- Tidak ada komponen baru — inline pattern saja

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter frontend tsc --noEmit` → 0 errors
- [ ] /pengaturan/profil render dengan data dari /auth/me
- [ ] /pengaturan/users render placeholder tanpa 404
- [ ] /pengaturan/roles render placeholder tanpa 404
- [ ] Sidebar "Profil Saya" link tersedia
</verification>

<success_criteria>
- 3 halaman pengaturan live: /pengaturan/profil + /pengaturan/users + /pengaturan/roles
- TypeScript: 0 errors
- Sidebar tidak 404 untuk semua link Pengaturan
- Phase 08 siap untuk UNIFY → phase complete
</success_criteria>

<output>
After completion, create `.paul/phases/08-laporan-pengaturan-ui/08-03-SUMMARY.md`
</output>
