---
phase: 19-panduan-master-dashboard
plan: 02
type: execute
wave: 1
depends_on: ["19-01"]
files_modified:
  - apps/frontend/src/pages/master/siswa/index.tsx
  - apps/frontend/src/pages/master/pos/index.tsx
  - apps/frontend/src/pages/master/template/index.tsx
autonomous: true
---

<objective>
## Goal
Tambah teks panduan penggunaan di tiga halaman Master Data — Siswa, POS Keuangan, dan Template Pembayaran — menggunakan pola yang sama dengan Plan 19-01 (gray italic, non-teknis, mudah dipahami operator).

## Purpose
Melengkapi Phase 19 sehingga seluruh halaman Master Data dan Dashboard memiliki panduan. Ketiga halaman ini bersifat biasa (bukan krusial), sehingga menggunakan warna abu-abu.

## Output
Tiga file frontend diubah: masing-masing ditambah satu elemen `<p>` panduan gray italic di bawah header halaman.
</objective>

<context>
@.paul/PROJECT.md
@.paul/phases/19-panduan-master-dashboard/19-01-SUMMARY.md
@apps/frontend/src/pages/master/siswa/index.tsx
@apps/frontend/src/pages/master/pos/index.tsx
@apps/frontend/src/pages/master/template/index.tsx
</context>

<acceptance_criteria>

## AC-1: Panduan tampil di Data Siswa
```gherkin
Given halaman Data Siswa dibuka
When halaman tampil
Then teks panduan muncul di bawah judul "Data Siswa", font kecil italic abu-abu
```

## AC-2: Panduan tampil di POS Keuangan
```gherkin
Given halaman POS Keuangan dibuka
When halaman tampil
Then teks panduan muncul di bawah judul "POS Keuangan", font kecil italic abu-abu
```

## AC-3: Panduan tampil di Template Pembayaran
```gherkin
Given halaman Template Pembayaran dibuka
When halaman tampil
Then teks panduan muncul di bawah judul "Template Pembayaran", font kecil italic abu-abu
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Panduan Data Siswa dan POS Keuangan</name>
  <files>apps/frontend/src/pages/master/siswa/index.tsx, apps/frontend/src/pages/master/pos/index.tsx</files>
  <action>
    **siswa/index.tsx (line 215):**
    Cari:
    ```tsx
    <div className="flex items-center justify-between mb-6">
      <div>
        <h1 className="text-2xl font-bold text-gray-900">Data Siswa</h1>
        <p className="text-gray-500 mt-1">Kelola data siswa per unit sekolah</p>
      </div>
    ```
    Ganti `mb-6` menjadi `mb-2` pada div tersebut, lalu setelah `</div>` penutup div header (yaitu setelah `</div>` yang menutup seluruh blok header termasuk tombol filter di kanan), tambahkan:
    ```tsx
    <p className="text-xs italic text-gray-400 mb-4">
      Daftar seluruh siswa yang terdaftar di sekolah. Gunakan filter Unit Sekolah untuk melihat data per jenjang. Klik ikon kelas untuk melihat riwayat kelas siswa.
    </p>
    ```

    **pos/index.tsx (line 107):**
    Cari:
    ```tsx
    <div className="flex items-center justify-between mb-6">
      <div>
        <h1 className="text-2xl font-bold text-gray-900">POS Keuangan</h1>
        <p className="text-gray-500 mt-1">Kelola pos-pos pembayaran</p>
      </div>
      <Button className="bg-[#00A651] hover:bg-[#008C44]" onClick={openCreate}>
        <Plus className="w-4 h-4 mr-2" />
        Tambah POS
      </Button>
    </div>
    ```
    Ganti `mb-6` menjadi `mb-2`, lalu setelah `</div>` penutup div header tersebut tambahkan:
    ```tsx
    <p className="text-xs italic text-gray-400 mb-4">
      Kelola pos-pos penerimaan keuangan sekolah (contoh: SPP, Uang Gedung, Seragam). POS yang sudah digunakan dalam tagihan tidak dapat dihapus — nonaktifkan saja jika tidak lagi dibutuhkan.
    </p>
    ```

    Jangan ubah logika fetchData, form handler, atau tampilan tabel/modal di kedua file.
  </action>
  <verify>
    Baca ulang kedua file setelah edit. Pastikan:
    - `<p>` panduan ada di posisi yang benar (setelah header div, sebelum konten tabel)
    - `mb-6` sudah diganti `mb-2` pada outer header div
    - Tidak ada perubahan pada handler atau state
    - `npx tsc --noEmit` dari apps/frontend clean
  </verify>
  <done>AC-1 dan AC-2 satisfied: panduan gray italic tampil di Data Siswa dan POS Keuangan</done>
</task>

<task type="auto">
  <name>Task 2: Panduan Template Pembayaran</name>
  <files>apps/frontend/src/pages/master/template/index.tsx</files>
  <action>
    Di dalam blok return, cari (line 155):
    ```tsx
    <div className="flex items-center justify-between mb-6">
      <div>
        <h1 className="text-2xl font-bold text-gray-900">Template Pembayaran</h1>
        <p className="text-gray-500 mt-1">Atur nominal tagihan per kelas per pos</p>
      </div>
    ```
    Ganti `mb-6` menjadi `mb-2` pada div tersebut, lalu setelah `</div>` penutup div header (termasuk area filter di kanan), tambahkan:
    ```tsx
    <p className="text-xs italic text-gray-400 mb-4">
      Atur nominal tagihan yang berlaku untuk kelas tertentu di tahun pelajaran tertentu. Template ini menjadi dasar ketika tagihan digenerate secara massal untuk seluruh siswa di kelas tersebut.
    </p>
    ```

    Jangan ubah logika fetchData, form handler, atau tampilan tabel/modal.
  </action>
  <verify>
    Baca ulang file setelah edit. Pastikan:
    - `<p>` panduan ada setelah header, sebelum konten tabel
    - `mb-6` sudah diganti `mb-2`
    - Tidak ada perubahan pada handler atau state
    - `npx tsc --noEmit` dari apps/frontend clean
  </verify>
  <done>AC-3 satisfied: panduan gray italic tampil di Template Pembayaran</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Logika state, handler, dan fetch functions di semua file
- Dialog, modal, form, dan tabel yang sudah ada
- Routing dan struktur komponen
- Style apapun selain penambahan elemen `<p>` panduan dan perubahan `mb-6` → `mb-2`

## SCOPE LIMITS
- Hanya menambah satu elemen `<p>` per halaman di bawah header
- Tidak membuat komponen baru atau shared component untuk panduan
- Tidak menyentuh halaman Keuangan, Laporan, atau Pengaturan (masuk Phase 20 dan 21)
- Semua panduan gray italic — tidak ada merah di plan ini

</boundaries>

<verification>
- [ ] Siswa: `<p className="text-xs italic text-gray-400 mb-4">` ada setelah header
- [ ] POS: `<p className="text-xs italic text-gray-400 mb-4">` ada setelah header
- [ ] Template: `<p className="text-xs italic text-gray-400 mb-4">` ada setelah header
- [ ] Ketiga file: `mb-6` → `mb-2` diubah pada header div
- [ ] `npx tsc --noEmit` dari apps/frontend: 0 error
- [ ] Semua AC terpenuhi
</verification>

<success_criteria>
- 3 halaman memiliki teks panduan di bawah judul
- Bahasa awam, mudah dipahami operator non-teknis
- Semua teks abu-abu italic (bukan merah)
- Tidak ada regresi pada fungsi yang sudah ada
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/19-panduan-master-dashboard/19-02-SUMMARY.md`
</output>
