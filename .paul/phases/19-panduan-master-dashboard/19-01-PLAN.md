---
phase: 19-panduan-master-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/pages/dashboard/index.tsx
  - apps/frontend/src/pages/master/tahun-pelajaran/index.tsx
  - apps/frontend/src/pages/master/kelas/index.tsx
  - apps/frontend/src/pages/master/kelas/mapping.tsx
autonomous: true
---

<objective>
## Goal
Tambah teks panduan penggunaan di empat halaman: Dashboard, Tahun Pelajaran, Manajemen Kelas, dan Mapping Kelas — bahasa awam (non-teknis), mudah dipahami operator. Mapping Kelas berwarna merah karena termasuk modul krusial.

## Purpose
Membantu operator sekolah memahami fungsi setiap halaman tanpa perlu bertanya — panduan tampil langsung di bawah judul halaman dengan font kecil italic.

## Output
Empat file frontend diubah: masing-masing ditambah satu elemen `<p>` panduan di bawah header halaman.
</objective>

<context>
@.paul/PROJECT.md
@apps/frontend/src/pages/dashboard/index.tsx
@apps/frontend/src/pages/master/tahun-pelajaran/index.tsx
@apps/frontend/src/pages/master/kelas/index.tsx
@apps/frontend/src/pages/master/kelas/mapping.tsx
</context>

<acceptance_criteria>
## AC-1: Panduan tampil di Dashboard
Given halaman Dashboard dibuka
When konten selesai dimuat
Then teks panduan muncul di bawah judul "Dashboard", font kecil italic abu-abu

## AC-2: Panduan tampil di Tahun Pelajaran
Given halaman Tahun Pelajaran dibuka
When halaman tampil
Then teks panduan muncul di bawah judul "Tahun Pelajaran", font kecil italic abu-abu

## AC-3: Panduan tampil di Manajemen Kelas
Given halaman Manajemen Kelas dibuka (tab Kelas maupun tab Unit Sekolah)
When halaman tampil
Then teks panduan muncul di bawah judul "Manajemen Kelas", font kecil italic abu-abu (panduan tampil di atas tab bar)

## AC-4: Panduan tampil di Mapping Kelas — warna merah
Given halaman Mapping Kelas dibuka
When halaman tampil
Then teks panduan muncul di bawah judul "Mapping Kelas", font kecil italic **merah** (text-red-500) sebagai penanda perhatian
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Panduan Dashboard dan Tahun Pelajaran</name>
  <files>apps/frontend/src/pages/dashboard/index.tsx, apps/frontend/src/pages/master/tahun-pelajaran/index.tsx</files>
  <action>
    **dashboard/index.tsx:**
    Di dalam blok return, cari `{/* Header */}` diikuti `<div className="flex items-start justify-between">`.
    Setelah `</div>` penutup div header tersebut, tambahkan:
    ```tsx
    <p className="text-xs italic text-gray-400 -mt-4">
      Halaman ini menampilkan ringkasan keuangan sekolah — jumlah siswa aktif, total penerimaan bulan ini, dan transaksi terakhir yang masuk.
    </p>
    ```
    Karena dashboard menggunakan `space-y-6` pada wrapper, cukup tambahkan `-mt-4` agar teks lebih dekat ke header dan tidak terlalu jauh dari stat cards.

    **master/tahun-pelajaran/index.tsx:**
    Di dalam blok return, cari:
    ```tsx
    <div className="flex items-center justify-between mb-6">
    ```
    Ganti `mb-6` menjadi `mb-2` pada div tersebut, lalu setelah `</div>` penutupnya tambahkan:
    ```tsx
    <p className="text-xs italic text-gray-400 mb-4">
      Gunakan halaman ini untuk mengatur tahun pelajaran. Pastikan hanya satu tahun yang ditandai Aktif — tahun aktif digunakan sebagai acuan data kelas dan tagihan siswa.
    </p>
    ```

    Jangan ubah logika fetchData, form handler, atau tampilan tabel/modal.
  </action>
  <verify>
    Baca ulang kedua file setelah edit. Pastikan:
    - Elemen `<p>` panduan ada di posisi yang benar (setelah header, sebelum konten utama)
    - Tidak ada perubahan pada handler atau state
    - `npx tsc --noEmit` dari apps/frontend clean
  </verify>
  <done>AC-1 dan AC-2 satisfied: panduan muncul di Dashboard dan Tahun Pelajaran</done>
</task>

<task type="auto">
  <name>Task 2: Panduan Manajemen Kelas (kelas/index.tsx)</name>
  <files>apps/frontend/src/pages/master/kelas/index.tsx</files>
  <action>
    Di dalam blok return, cari:
    ```tsx
    <div className="mb-6">
      <h1 className="text-2xl font-bold text-gray-900">Manajemen Kelas</h1>
      <p className="text-gray-500 mt-1">Kelola data kelas dan unit sekolah</p>
    </div>
    ```
    Ganti `mb-6` menjadi `mb-2`, lalu setelah `</div>` penutup div header tersebut tambahkan:
    ```tsx
    <p className="text-xs italic text-gray-400 mb-4">
      Kelola daftar kelas dan unit sekolah. Pada tab Kelas, klik baris kelas untuk melihat daftar siswanya. Pada tab Unit Sekolah, kelola jenjang atau satuan pendidikan yang tersedia.
    </p>
    ```
    Panduan ini tampil di atas tab bar, berlaku untuk kedua tab.

    Jangan ubah logika tab, handler, atau tampilan tabel/modal.
  </action>
  <verify>
    Baca ulang file setelah edit. Pastikan:
    - `<p>` panduan muncul setelah header, sebelum tab bar
    - Tab bar dan konten tabel tidak berubah
    - `npx tsc --noEmit` clean
  </verify>
  <done>AC-3 satisfied: panduan tampil di atas tab bar Manajemen Kelas</done>
</task>

<task type="auto">
  <name>Task 3: Panduan Mapping Kelas — merah (krusial)</name>
  <files>apps/frontend/src/pages/master/kelas/mapping.tsx</files>
  <action>
    Di dalam blok return, cari blok header:
    ```tsx
    {/* Header */}
    <div>
      <h1 className="text-2xl font-bold text-gray-900">Mapping Kelas</h1>
      <p className="text-sm text-gray-500 mt-1">Assign siswa ke kelas berdasarkan tahun pelajaran</p>
    </div>
    ```
    Setelah `</div>` penutup div header tersebut, tambahkan (menggunakan `text-red-500` karena ini modul krusial):
    ```tsx
    <p className="text-xs italic text-red-500 -mt-4">
      Gunakan halaman ini untuk memasukkan siswa ke dalam kelas. Pilih tahun pelajaran, unit sekolah, dan kelas terlebih dahulu. Periksa daftar siswa sebelum mengklik Proses — tindakan ini langsung mengubah data kelas siswa.
    </p>
    ```
    Karena wrapper menggunakan `space-y-6`, tambahkan `-mt-4` agar teks lebih dekat ke header.

    Jangan ubah logika filter, handler staging, atau Dialog konfirmasi.
  </action>
  <verify>
    Baca ulang file setelah edit. Pastikan:
    - `<p>` berwarna merah ada setelah header, sebelum error block dan filter bar
    - Tidak ada perubahan pada state atau handler
    - `npx tsc --noEmit` clean
  </verify>
  <done>AC-4 satisfied: panduan merah tampil di Mapping Kelas</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Logika state, handler, dan fetch functions di semua file
- Dialog, modal, form, dan tabel yang sudah ada
- Routing dan struktur komponen
- Style apapun selain penambahan elemen `<p>` panduan

## SCOPE LIMITS
- Hanya menambah satu elemen `<p>` per halaman di bawah header
- Tidak membuat komponen baru atau shared component untuk panduan
- Tidak mengubah warna, font, atau style elemen yang sudah ada
- Tidak menyentuh halaman Siswa, POS, Template (masuk Plan 19-02)
</boundaries>

<verification>
- [ ] Dashboard: `<p className="text-xs italic text-gray-400 ...">` ada setelah header
- [ ] Tahun Pelajaran: panduan gray italic tampil, mb-6 → mb-2 diubah
- [ ] Kelas/index.tsx: panduan gray italic di atas tab bar, mb-6 → mb-2 diubah
- [ ] Mapping Kelas: panduan `text-red-500` ada setelah header
- [ ] `npx tsc --noEmit` dari apps/frontend: 0 error
- [ ] Semua AC terpenuhi
</verification>

<success_criteria>
- 4 halaman memiliki teks panduan di bawah judul
- Bahasa awam, mudah dipahami operator non-teknis
- Mapping Kelas: teks merah sebagai penanda krusial
- Tiga halaman lain: teks abu-abu italic
- Tidak ada regresi pada fungsi yang sudah ada
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/19-panduan-master-dashboard/19-01-SUMMARY.md`
</output>
