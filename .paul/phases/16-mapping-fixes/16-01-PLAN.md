---
phase: 16-mapping-fixes
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Perbaiki dua bug UI di halaman Mapping Kelas: (1) sidebar menghighlight dua item sekaligus saat di /master/kelas/mapping, (2) filter siswa tersedia memiliki 3 input terpisah — gabung NIS+Nama jadi 1 input teks, ubah Kelas Saat Ini jadi dropdown.
</objective>

<context>
@.paul/PROJECT.md
@apps/frontend/src/components/sidebar/NavItem.tsx
@apps/frontend/src/pages/master/kelas/mapping.tsx
</context>

<acceptance_criteria>
## AC-1: Sidebar hanya highlight satu item aktif
Given user berada di halaman /master/kelas/mapping
When sidebar dirender
Then hanya "Mapping Kelas" yang aktif (bg-accent), "Manajemen Kelas" tidak aktif

## AC-2: Filter siswa tersedia menggunakan 1 input teks
Given halaman mapping dengan kelas dipilih
When user mengetik di input filter
Then daftar siswa tersedia difilter berdasarkan NIS ATAU Nama yang mengandung teks tersebut (case-insensitive)

## AC-3: Filter kelas saat ini berupa dropdown
Given halaman mapping dengan siswa tersedia ada data
When user membuka dropdown filter kelas
Then opsi dropdown berisi nilai unik currentClassName dari daftar siswa tersedia + opsi "Semua Kelas"
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Fix NavLink end prop untuk /master/kelas</name>
  <files>apps/frontend/src/components/sidebar/NavItem.tsx, apps/frontend/src/components/sidebar/index.tsx</files>
  <action>
    Tambahkan prop `end` pada NavLink di NavItem, namun `end` hanya dibutuhkan untuk route /master/kelas saja (bukan semua NavItem).
    
    Opsi terbaik: tambahkan optional prop `end?: boolean` ke NavItemProps dan teruskan ke NavLink.
    Lalu di sidebar/index.tsx, tambahkan `end` prop pada NavItem untuk `/master/kelas`:
    `<NavItem to="/master/kelas" icon={Users} label="Manajemen Kelas" end />`
    
    Tanpa `end`, React Router NavLink menganggap /master/kelas aktif jika URL dimulai dengan /master/kelas (termasuk /master/kelas/mapping).
  </action>
  <verify>Tidak ada cara automated — verifikasi visual: buka /master/kelas/mapping, pastikan hanya "Mapping Kelas" yang berwarna hijau di sidebar.</verify>
  <done>AC-1 satisfied: hanya satu item aktif saat di /master/kelas/mapping</done>
</task>

<task type="auto">
  <name>Task 2: Gabung filter NIS+Nama dan buat dropdown Kelas Saat Ini</name>
  <files>apps/frontend/src/pages/master/kelas/mapping.tsx</files>
  <action>
    1. Hapus state `filterNis` dan `filterName`, ganti dengan satu state `filterSearch: string`.
    
    2. Update logic filter di `filteredAvailable`:
       ```
       if (filterSearch) {
         const q = filterSearch.toLowerCase()
         if (!s.nis.toLowerCase().includes(q) && !s.name.toLowerCase().includes(q)) return false
       }
       ```
    
    3. Hapus state `filterClass: string`, ganti dengan `filterClassId: string` (value = currentClassName atau '' untuk semua).
    
    4. Hitung opsi dropdown dari availableStudents (bukan filteredAvailable karena belum difilter):
       ```
       const classOptions = Array.from(
         new Set(availableStudents.map((s) => s.currentClassName).filter(Boolean))
       ).sort() as string[]
       ```
    
    5. Update filter logic untuk kelas:
       ```
       if (filterClassId && (s.currentClassName ?? '') !== filterClassId) return false
       ```
    
    6. Di JSX, ganti 3 input menjadi:
       - 1 `<Input>` dengan placeholder "Cari NIS atau Nama..." untuk filterSearch
       - 1 `<Select>` dengan opsi "Semua Kelas" (value '') + map classOptions untuk filterClassId
    
    7. Reset `filterSearch` dan `filterClassId` ke '' saat `selectedClassId` berubah (di useEffect yang sudah ada atau tambah reset di sana).
    
    Jangan ubah logika fetchClassData, handler Lepas/Staging/Proses, atau bagian lain halaman.
  </action>
  <verify>
    - Pilih kelas, ketik sebagian NIS di input → daftar siswa terfilter
    - Ketik sebagian nama → daftar siswa terfilter
    - Buka dropdown Kelas → tampil opsi unik dari data, pilih satu → tersaring
    - Pilih "Semua Kelas" → semua siswa muncul kembali
  </verify>
  <done>AC-2 dan AC-3 satisfied: 1 input teks + 1 dropdown kelas berfungsi dengan benar</done>
</task>
</tasks>

<output>
Setelah selesai, buat `.paul/phases/16-mapping-fixes/16-01-SUMMARY.md`
</output>
