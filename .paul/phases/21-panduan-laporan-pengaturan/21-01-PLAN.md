---
phase: 21-panduan-laporan-pengaturan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/pages/laporan/harian/index.tsx
  - apps/frontend/src/pages/laporan/bulanan/index.tsx
  - apps/frontend/src/pages/laporan/tahunan/index.tsx
  - apps/frontend/src/pages/laporan/tunggakan/index.tsx
  - apps/frontend/src/pages/laporan/rekap-pos/index.tsx
  - apps/frontend/src/pages/pengaturan/profil/index.tsx
  - apps/frontend/src/pages/pengaturan/roles/index.tsx
  - apps/frontend/src/pages/pengaturan/users/index.tsx
autonomous: true
---

<objective>
## Goal
Tambah teks panduan gray italic di 8 halaman terakhir — 5 halaman Laporan (Harian, Bulanan, Tahunan, Tunggakan, Rekap POS) dan 3 halaman Pengaturan (Profil, Role & Akses, Pengguna). Ini adalah plan terakhir Milestone v1.3.

## Purpose
Melengkapi Milestone v1.3 — setelah plan ini selesai, seluruh halaman SIMKA memiliki panduan penggunaan bahasa awam.

## Output
Delapan file frontend diubah: masing-masing ditambah satu `<p>` panduan gray italic di bawah header.
</objective>

<context>
@.paul/PROJECT.md
@.paul/phases/19-panduan-master-dashboard/19-01-SUMMARY.md
@apps/frontend/src/pages/laporan/harian/index.tsx
@apps/frontend/src/pages/laporan/bulanan/index.tsx
@apps/frontend/src/pages/laporan/tahunan/index.tsx
@apps/frontend/src/pages/laporan/tunggakan/index.tsx
@apps/frontend/src/pages/laporan/rekap-pos/index.tsx
@apps/frontend/src/pages/pengaturan/profil/index.tsx
@apps/frontend/src/pages/pengaturan/roles/index.tsx
@apps/frontend/src/pages/pengaturan/users/index.tsx
</context>

<acceptance_criteria>

## AC-1: Panduan tampil di semua halaman Laporan
```gherkin
Given halaman Laporan mana pun dibuka (Harian/Bulanan/Tahunan/Tunggakan/Rekap POS)
When halaman tampil
Then teks panduan muncul di bawah header, font kecil italic abu-abu
```

## AC-2: Panduan tampil di semua halaman Pengaturan
```gherkin
Given halaman Pengaturan mana pun dibuka (Profil/Role & Akses/Pengguna)
When halaman tampil
Then teks panduan muncul di bawah header, font kecil italic abu-abu
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Panduan gray — 5 halaman Laporan</name>
  <files>
    apps/frontend/src/pages/laporan/harian/index.tsx,
    apps/frontend/src/pages/laporan/bulanan/index.tsx,
    apps/frontend/src/pages/laporan/tahunan/index.tsx,
    apps/frontend/src/pages/laporan/tunggakan/index.tsx,
    apps/frontend/src/pages/laporan/rekap-pos/index.tsx
  </files>
  <action>
    Semua 5 file menggunakan pola yang sama: `<div className="flex items-center justify-between mb-6">` sebagai header, diikuti konten filter di sebelah kanan.
    Untuk setiap file: ganti `mb-6` menjadi `mb-2` pada div header tersebut, lalu tambahkan `<p>` panduan gray setelah `</div>` penutup header div.

    **harian/index.tsx** (header div menutup di line 48):
    ```tsx
    <p className="text-xs italic text-gray-400 mb-4">
      Rekap transaksi pembayaran untuk tanggal tertentu. Pilih tanggal lalu lihat total penerimaan dan daftar transaksi hari itu. Gunakan tombol Export untuk mencetak atau menyimpan laporan.
    </p>
    ```

    **bulanan/index.tsx** (header div menutup di line 55):
    ```tsx
    <p className="text-xs italic text-gray-400 mb-4">
      Rekap transaksi per hari dalam satu bulan. Pilih bulan dan tahun untuk melihat total penerimaan dan rinciannya. Gunakan tombol Export untuk mencetak atau menyimpan laporan.
    </p>
    ```

    **tahunan/index.tsx** (header div menutup di line 61):
    ```tsx
    <p className="text-xs italic text-gray-400 mb-4">
      Rekap transaksi per bulan dalam satu tahun ajaran. Pilih tahun ajaran untuk melihat tren penerimaan bulanan. Gunakan tombol Export untuk mencetak atau menyimpan laporan.
    </p>
    ```

    **tunggakan/index.tsx** (header div menutup di line 88):
    ```tsx
    <p className="text-xs italic text-gray-400 mb-4">
      Daftar siswa yang masih memiliki tagihan belum lunas. Gunakan filter tahun pelajaran dan kelas untuk menyaring data. Gunakan tombol Export untuk mencetak laporan tunggakan.
    </p>
    ```

    **rekap-pos/index.tsx** (header div menutup di line 79):
    ```tsx
    <p className="text-xs italic text-gray-400 mb-4">
      Rekap penerimaan yang dipecah per pos pembayaran (SPP, Uang Gedung, dll). Gunakan filter tanggal dan tahun ajaran untuk menyesuaikan rentang data, lalu Export untuk mencetak rekap.
    </p>
    ```

    Jangan ubah logika state, handler, filter, atau tombol Export di semua file.
  </action>
  <verify>
    Grep `text-xs italic text-gray-400 mb-4` di semua 5 file — harus ada 1 hasil per file.
    Grep `justify-between mb-2` di semua 5 file — harus ada 1 hasil per file.
    `npx tsc --noEmit` dari apps/frontend: 0 error.
  </verify>
  <done>AC-1 satisfied: panduan gray italic tampil di semua 5 halaman Laporan</done>
</task>

<task type="auto">
  <name>Task 2: Panduan gray — 3 halaman Pengaturan</name>
  <files>
    apps/frontend/src/pages/pengaturan/profil/index.tsx,
    apps/frontend/src/pages/pengaturan/roles/index.tsx,
    apps/frontend/src/pages/pengaturan/users/index.tsx
  </files>
  <action>
    **profil/index.tsx:**
    Header berupa `<div className="mb-6">` (line 72) tanpa flex justify-between.
    Ganti `mb-6` menjadi `mb-2`, lalu setelah `</div>` penutupnya (line 75) tambahkan:
    ```tsx
    <p className="text-xs italic text-gray-400 mb-4">
      Halaman ini menampilkan informasi akun Anda yang sedang login. Gunakan form Ganti Password di bawah untuk memperbarui kata sandi Anda.
    </p>
    ```

    **roles/index.tsx:**
    Header berupa `<div className="flex items-center justify-between mb-6">` (line 109).
    Header div menutup di `</div>` pertama di level yang sama (line 118 berdasarkan grep).
    Ganti `mb-6` menjadi `mb-2`, lalu setelah `</div>` penutup header tambahkan:
    ```tsx
    <p className="text-xs italic text-gray-400 mb-4">
      Kelola peran dan hak akses pengguna. Setiap role memiliki kumpulan izin yang menentukan fitur apa yang bisa diakses oleh pengguna dengan role tersebut.
    </p>
    ```

    **users/index.tsx:**
    Header berupa `<div className="flex items-center justify-between mb-6">` (line 126).
    Header div menutup di `</div>` pertama di level yang sama (line 135 berdasarkan grep).
    Ganti `mb-6` menjadi `mb-2`, lalu setelah `</div>` penutup header tambahkan:
    ```tsx
    <p className="text-xs italic text-gray-400 mb-4">
      Kelola akun pengguna sistem SIMKA. Setiap pengguna memiliki satu role yang menentukan hak aksesnya. Nonaktifkan akun yang sudah tidak digunakan — jangan hapus.
    </p>
    ```

    Jangan ubah logika state, handler, modal, atau tabel di semua file.
  </action>
  <verify>
    Grep `text-xs italic text-gray-400 mb-4` di semua 3 file — harus ada 1 hasil per file.
    `npx tsc --noEmit` dari apps/frontend: 0 error.
  </verify>
  <done>AC-2 satisfied: panduan gray italic tampil di Profil, Role & Akses, dan Pengguna</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Logika state, handler, fetch functions, modal, form, tabel di semua file
- Tombol Export, filter, dan routing yang sudah ada
- Style apapun selain penambahan `<p>` panduan dan perubahan `mb-6` → `mb-2`

## SCOPE LIMITS
- Hanya menambah satu `<p>` per halaman di bawah header
- Tidak membuat komponen baru
- Semua panduan gray — tidak ada merah di plan ini

</boundaries>

<verification>
- [ ] Semua 5 laporan: panduan gray ada + mb-2 di header
- [ ] Profil: panduan gray ada + mb-2 di header div
- [ ] Roles + Users: panduan gray ada + mb-2 di header
- [ ] `npx tsc --noEmit`: 0 error
- [ ] AC-1 dan AC-2 terpenuhi
</verification>

<success_criteria>
- 8 halaman memiliki teks panduan di bawah judul
- Semua gray italic — konsisten dengan pola Phase 19 dan 20
- Milestone v1.3 complete setelah plan ini selesai
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/21-panduan-laporan-pengaturan/21-01-SUMMARY.md`
</output>
