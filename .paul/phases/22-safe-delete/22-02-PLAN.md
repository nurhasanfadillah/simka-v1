---
phase: 22-safe-delete
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - apps/frontend/src/components/ui/delete-error-modal.tsx
  - apps/frontend/src/pages/master/tahun-pelajaran/index.tsx
  - apps/frontend/src/pages/master/kelas/index.tsx
  - apps/frontend/src/pages/master/siswa/index.tsx
  - apps/frontend/src/pages/master/pos/index.tsx
  - apps/frontend/src/pages/master/template/index.tsx
  - apps/frontend/src/pages/pengaturan/roles/index.tsx
  - apps/frontend/src/pages/pengaturan/users/index.tsx
  - apps/frontend/src/pages/keuangan/riwayat/index.tsx
autonomous: false
---

<objective>
## Goal
Tambah komponen `DeleteErrorModal` yang menampilkan data terkait dari response 409, dan integrasikan ke semua 8 halaman yang punya atau membutuhkan tombol hapus — termasuk tombol Hapus di Riwayat khusus transaksi void.

## Purpose
Setelah backend (Plan 22-01) siap mengembalikan 409 + relatedData, frontend perlu menangkap error itu dan menampilkan modal informatif yang memberitahu user mengapa record tidak bisa dihapus.

## Output
1 komponen baru + 8 halaman diupdate.
</objective>

<context>
@.paul/PROJECT.md
@.paul/phases/22-safe-delete/CONTEXT.md
@apps/frontend/src/pages/master/tahun-pelajaran/index.tsx
@apps/frontend/src/pages/master/kelas/index.tsx
@apps/frontend/src/pages/master/siswa/index.tsx
@apps/frontend/src/pages/master/pos/index.tsx
@apps/frontend/src/pages/master/template/index.tsx
@apps/frontend/src/pages/pengaturan/roles/index.tsx
@apps/frontend/src/pages/pengaturan/users/index.tsx
@apps/frontend/src/pages/keuangan/riwayat/index.tsx
</context>

<acceptance_criteria>

## AC-1: Modal error 409 tampil dengan data terkait
```gherkin
Given user menekan tombol Hapus pada record yang masih punya dependensi
When API mengembalikan 409 dengan relatedData
Then modal "Tidak Dapat Dihapus" muncul
And menampilkan daftar data terkait dalam format: "[nama]: [jumlah] data"
And hanya ada tombol "Tutup" (tidak ada force delete)
```

## AC-2: Delete berhasil tanpa modal error
```gherkin
Given user menekan tombol Hapus pada record tanpa dependensi
When API mengembalikan 200
Then record hilang dari tabel
And tidak ada modal error yang muncul
```

## AC-3: Tombol Hapus di Riwayat hanya tampil untuk transaksi void
```gherkin
Given halaman Riwayat Transaksi ditampilkan
When tabel ter-render
Then baris dengan status 'void' menampilkan tombol Hapus (Trash2)
And baris dengan status lain tidak menampilkan tombol Hapus
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Buat komponen DeleteErrorModal</name>
  <files>
    apps/frontend/src/components/ui/delete-error-modal.tsx
  </files>
  <action>
    Buat file baru `apps/frontend/src/components/ui/delete-error-modal.tsx`:

    ```tsx
    import {
      Dialog,
      DialogContent,
      DialogHeader,
      DialogTitle,
      DialogDescription,
    } from '@/components/ui/dialog'
    import { Button } from '@/components/ui/button'
    import { AlertTriangle } from 'lucide-react'

    interface DeleteErrorModalProps {
      open: boolean
      onClose: () => void
      relatedData: Record<string, number>
    }

    const LABEL_MAP: Record<string, string> = {
      tagihan: 'Tagihan',
      template: 'Template Pembayaran',
      kelas: 'Kelas',
      siswa: 'Siswa',
      pengguna: 'Pengguna',
    }

    export function DeleteErrorModal({ open, onClose, relatedData }: DeleteErrorModalProps) {
      return (
        <Dialog open={open} onOpenChange={onClose}>
          <DialogContent className="sm:max-w-md">
            <DialogHeader>
              <DialogTitle className="flex items-center gap-2 text-red-600">
                <AlertTriangle className="w-5 h-5" />
                Tidak Dapat Dihapus
              </DialogTitle>
              <DialogDescription asChild>
                <div>
                  <p className="text-sm text-gray-600 mt-1">
                    Data ini masih memiliki data terkait yang harus dihapus terlebih dahulu:
                  </p>
                  <ul className="mt-3 space-y-1">
                    {Object.entries(relatedData).map(([key, val]) => (
                      <li key={key} className="text-sm text-gray-800 flex items-center gap-2">
                        <span className="w-2 h-2 rounded-full bg-red-400 inline-block" />
                        {LABEL_MAP[key] ?? key}: <span className="font-semibold">{val} data</span>
                      </li>
                    ))}
                  </ul>
                </div>
              </DialogDescription>
            </DialogHeader>
            <div className="flex justify-end pt-2">
              <Button variant="outline" onClick={onClose}>Tutup</Button>
            </div>
          </DialogContent>
        </Dialog>
      )
    }
    ```

    Tidak ada logika lain di file ini — hanya presentasi.
  </action>
  <verify>
    `npx tsc --noEmit` dari apps/frontend: 0 error.
    File exists di `apps/frontend/src/components/ui/delete-error-modal.tsx`.
  </verify>
  <done>Komponen DeleteErrorModal tersedia untuk dipakai semua halaman</done>
</task>

<task type="auto">
  <name>Task 2: Integrasikan DeleteErrorModal ke 5 halaman Master</name>
  <files>
    apps/frontend/src/pages/master/tahun-pelajaran/index.tsx,
    apps/frontend/src/pages/master/kelas/index.tsx,
    apps/frontend/src/pages/master/siswa/index.tsx,
    apps/frontend/src/pages/master/pos/index.tsx,
    apps/frontend/src/pages/master/template/index.tsx
  </files>
  <action>
    Untuk setiap halaman, lakukan pola yang sama:

    **1. Tambah import:**
    ```tsx
    import axios from 'axios'
    import { DeleteErrorModal } from '@/components/ui/delete-error-modal'
    ```

    **2. Tambah state:**
    ```tsx
    const [deleteErrorData, setDeleteErrorData] = useState<Record<string, number> | null>(null)
    ```

    **3. Ubah handler delete** — tangkap 409 dan set deleteErrorData:
    ```tsx
    const handleDelete = async (id: number) => {
      try {
        setDeleting(true)
        await apiClient.delete(`/[endpoint]/${id}`)
        await fetchData()
        setDeleteTarget(null)
      } catch (err) {
        if (axios.isAxiosError(err) && err.response?.status === 409) {
          setDeleteTarget(null)
          setDeleteErrorData(err.response.data.relatedData ?? {})
        } else {
          setError('Gagal menghapus data.')
        }
      } finally {
        setDeleting(false)
      }
    }
    ```

    **4. Tambah DeleteErrorModal** di JSX (setelah Dialog lain):
    ```tsx
    <DeleteErrorModal
      open={deleteErrorData !== null}
      onClose={() => setDeleteErrorData(null)}
      relatedData={deleteErrorData ?? {}}
    />
    ```

    **Endpoint per halaman:**
    - tahun-pelajaran: `/master/school-years/:id`
    - kelas: `/master/classes/:id`
    - siswa: `/master/students/:id`
    - pos: `/master/payment-posts/:id`
    - template: `/billing/payment-templates/:id`

    **Catatan tahun-pelajaran dan kelas:** Kedua halaman ini sudah punya logika delete dan state `deleteTarget` — cukup tambah penanganan 409 di catch block yang sudah ada.

    **Catatan siswa, pos, template:** Cek apakah sudah punya tombol Hapus. Jika belum, tambah tombol `<Trash2>` di kolom Aksi pada tabel dengan handler yang sesuai (pola sama dengan tahun-pelajaran).

    Jangan ubah logika create, edit, filter, atau tabel selain kolom Aksi.
  </action>
  <verify>
    Grep `DeleteErrorModal` di kelima file — harus ada.
    Grep `deleteErrorData` di kelima file — harus ada.
    `npx tsc --noEmit` dari apps/frontend: 0 error.
  </verify>
  <done>AC-1 dan AC-2 terpenuhi untuk 5 halaman Master</done>
</task>

<task type="auto">
  <name>Task 3: Integrasikan ke roles, users, dan tombol Hapus void di Riwayat</name>
  <files>
    apps/frontend/src/pages/pengaturan/roles/index.tsx,
    apps/frontend/src/pages/pengaturan/users/index.tsx,
    apps/frontend/src/pages/keuangan/riwayat/index.tsx
  </files>
  <action>
    **roles/index.tsx dan users/index.tsx:**
    Pola sama seperti Task 2:
    - Import `axios` dan `DeleteErrorModal`
    - Tambah state `deleteErrorData`
    - Tambah tombol Hapus `<Trash2>` di kolom Aksi tabel (di samping tombol yang sudah ada)
    - Handler delete hit `DELETE /roles/:id` atau `DELETE /users/:id`
    - Tangkap 409 → set deleteErrorData
    - Tambah `<DeleteErrorModal>` di JSX

    Untuk **users**: delete adalah hard delete — tidak ada dependensi, tapi tetap pakai pola yang sama untuk konsistensi (409 tidak akan terjadi, tapi modal tetap siap).

    **riwayat/index.tsx:**
    - Baca file untuk memahami struktur tabel transaksi yang ada
    - Di kolom Aksi tabel transaksi, tambah tombol Hapus hanya jika `tx.status === 'void'`:
      ```tsx
      {tx.status === 'void' && (
        <Button
          size="sm"
          variant="outline"
          className="text-red-600 border-red-200 hover:bg-red-50"
          onClick={() => handleDeleteTransaction(tx.id)}
        >
          <Trash2 className="w-3.5 h-3.5 mr-1" />
          Hapus
        </Button>
      )}
      ```
    - Tambah state `deletingTx` (boolean) dan `deleteErrorData`
    - Handler `handleDeleteTransaction`:
      ```tsx
      const handleDeleteTransaction = async (id: number) => {
        try {
          setDeletingTx(true)
          await apiClient.delete(`/transactions/${id}`)
          await fetchData()
        } catch (err) {
          if (axios.isAxiosError(err) && err.response?.status === 409) {
            setDeleteErrorData(err.response.data.relatedData ?? { status: 1 })
          } else {
            setError('Gagal menghapus transaksi.')
          }
        } finally {
          setDeletingTx(false)
        }
      }
      ```
    - Import `Trash2` dari lucide-react (mungkin sudah ada)
    - Tambah `<DeleteErrorModal>` di JSX

    Jangan ubah tombol Void, tampilan detail transaksi, atau logika filter yang sudah ada.
  </action>
  <verify>
    Grep `DeleteErrorModal` di ketiga file — harus ada.
    Grep `handleDeleteTransaction` di riwayat — harus ada.
    Grep `tx.status === 'void'` di riwayat — tombol hapus conditional.
    `npx tsc --noEmit` dari apps/frontend: 0 error.
  </verify>
  <done>AC-1, AC-2, AC-3 terpenuhi untuk roles, users, dan riwayat transaksi</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Komponen DeleteErrorModal yang menampilkan relatedData dari 409
    - Tombol Hapus + modal error di: Tahun Pelajaran, Kelas, Siswa, POS, Template, Role, Pengguna
    - Tombol Hapus di Riwayat hanya untuk baris status 'void'
  </what-built>
  <how-to-verify>
    1. Jalankan: docker start simka-postgres simka-redis && pnpm dev
    2. **Test 409 modal:** Coba hapus Tahun Pelajaran yang sudah punya Kelas/Tagihan → modal "Tidak Dapat Dihapus" muncul dengan daftar data terkait
    3. **Test delete berhasil:** Buat record baru tanpa dependensi → hapus → hilang dari tabel
    4. **Test Riwayat:** Buka halaman Riwayat → baris void menampilkan tombol Hapus, baris lain tidak
    5. **Test hapus transaksi void:** Klik Hapus pada transaksi void → transaksi hilang dari tabel
    6. Cek tidak ada console error
  </how-to-verify>
  <resume-signal>Ketik "approved" atau jelaskan isu yang ditemukan</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Logika create, edit, filter, sort, pagination di semua halaman
- Tombol Void di halaman Riwayat
- Logika fetch, state management selain delete-related
- Style/tema yang sudah ada

## SCOPE LIMITS
- Tidak membuat halaman baru
- Modal hanya untuk menampilkan info error 409 — tidak ada "force delete"
- Tombol Hapus di Riwayat hanya muncul untuk transaksi void — tidak untuk status lain

</boundaries>

<verification>
- [ ] `DeleteErrorModal` component berfungsi menampilkan relatedData
- [ ] Semua 8 halaman: DeleteErrorModal terintegrasi
- [ ] Riwayat: tombol Hapus hanya di baris void
- [ ] Hapus berhasil (tanpa dependensi): record hilang, tidak ada error
- [ ] Hapus gagal (ada dependensi): modal muncul dengan info yang benar
- [ ] `npx tsc --noEmit` dari apps/frontend: 0 error
- [ ] Checkpoint human-verify: approved
</verification>

<success_criteria>
- Semua 8 halaman memiliki safe delete yang berfungsi
- DeleteErrorModal tampil informatif dengan data terkait
- Transaksi void dapat dihapus dari halaman Riwayat
- TypeScript 0 error
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/22-safe-delete/22-02-SUMMARY.md`
</output>
