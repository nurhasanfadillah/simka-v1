---
phase: 29-spacing-fix
plan: 29-01
type: execute
autonomous: true
---

<objective>
## Goal
Perbaiki header halaman Migrasi Status agar konsisten dengan halaman lain — tambah deskripsi gray + perbaiki spacing panduan merah.

## Purpose
Header halaman saat ini tidak punya deskripsi singkat (tidak seperti halaman lain) dan panduan merah mepet ke judul karena negative margin `-mt-1`. Standar pattern: judul → deskripsi gray → panduan merah dengan `mt-2`.
</objective>

<context>
@apps/frontend/src/pages/master/migrasi-status.tsx
</context>

<acceptance_criteria>
## AC-1: Ada deskripsi halaman
Given halaman Migrasi Status dirender
When header tampil
Then ada teks deskripsi singkat gray di bawah judul (seperti halaman lain: Tahun Pelajaran, Generate Tagihan, dll)

## AC-2: Panduan tidak mepet ke judul
Given halaman Migrasi Status dirender
When judul dan panduan tampil
Then panduan merah berada di bawah deskripsi dengan spacing `mt-2`, bukan menempel ke judul
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Fix header: tambah deskripsi + perbaiki spacing panduan</name>
  <files>apps/frontend/src/pages/master/migrasi-status.tsx</files>
  <action>
  Ubah header div (line 142-148) dari:
  ```
  <div>
    <h1>Migrasi Status</h1>
    <p class="text-xs italic text-red-500 -mt-1">panduan...</p>
  </div>
  ```
  Menjadi:
  ```
  <div>
    <h1 class="text-2xl font-bold text-gray-900">Migrasi Status</h1>
    <p class="text-gray-500 mt-1">Kelola status akademik siswa — naik kelas, lulus, keluar, atau pindah</p>
  </div>
  <p class="text-xs italic text-red-500 mt-2">
    Perhatian: Proses migrasi status bersifat permanen. Pastikan tahun ajaran, kelas, dan data siswa sudah benar sebelum mengkonfirmasi.
  </p>
  ```
  Panduan merah dipisah dari wrapper div judul — ikuti pattern Generate Tagihan (header div → panduan p di luar).
  </action>
  <verify>pnpm --filter frontend build</verify>
  <done>AC-1 + AC-2 satisfied: deskripsi gray tampil, panduan merah dengan mt-2</done>
</task>
</tasks>

<output>
After completion, SUMMARY.md opsional — cukup konfirmasi build pass.
</output>
