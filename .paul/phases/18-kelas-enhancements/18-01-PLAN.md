---
phase: 18-kelas-enhancements
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Dua enhancement halaman Manajemen Kelas: (1) tambah kolom jumlah siswa di tab Kelas berdasarkan tahun pelajaran aktif + modal daftar siswa saat baris diklik dengan filter tahun, (2) tambah konfirmasi Dialog sebelum aksi "Lepas" siswa di mapping.tsx.
</objective>

<context>
@.paul/PROJECT.md
@apps/backend/src/master/classes/classes.service.ts
@apps/backend/src/master/classes/classes.controller.ts
@apps/frontend/src/pages/master/kelas/index.tsx
@apps/frontend/src/pages/master/kelas/mapping.tsx
@apps/frontend/src/types/master.ts
</context>

<acceptance_criteria>
## AC-1: Kolom Jumlah Siswa tampil di tab Kelas
Given tab Kelas dibuka
When data kelas diload
Then setiap baris menampilkan jumlah siswa terdaftar di kelas tersebut berdasarkan tahun pelajaran aktif

## AC-2: Klik baris kelas membuka daftar siswa
Given tab Kelas dengan data
When user mengklik baris kelas
Then Dialog terbuka menampilkan daftar siswa di kelas tersebut dengan kolom NIS, Nama, JK
Dan ada filter Select tahun pelajaran untuk melihat tahun lain

## AC-3: Konfirmasi sebelum Lepas siswa di mapping
Given halaman Mapping Kelas dengan siswa di kelas
When user mengklik tombol "Lepas" pada baris siswa
Then Dialog konfirmasi muncul dengan nama siswa
Ketika dikonfirmasi baru request DELETE dikirim
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Backend — tambah studentCount ke GET /master/classes</name>
  <files>apps/backend/src/master/classes/classes.service.ts, apps/backend/src/master/classes/classes.controller.ts</files>
  <action>
    Di `classes.service.ts`, modifikasi `findAll(schoolUnitId?: number)` menjadi `findAll(schoolUnitId?: number, schoolYearId?: number)`.

    Tambah field `studentCount` ke select menggunakan sql template:
    ```ts
    import { sql } from 'drizzle-orm'

    studentCount: sql<number>`(
      SELECT COUNT(*)::int FROM student_classes sc
      WHERE sc.class_id = ${classes.id}
      ${schoolYearId ? sql`AND sc.school_year_id = ${schoolYearId}` : sql``}
    )`,
    ```

    Di `classes.controller.ts`, tambah `@Query('schoolYearId') schoolYearId?: string` pada method `findAll`, parse ke number, teruskan ke service:
    ```ts
    @Get()
    findAll(
      @Query('school_unit_id') schoolUnitId?: string,
      @Query('schoolYearId') schoolYearId?: string,
    ) {
      return this.service.findAll(
        schoolUnitId ? +schoolUnitId : undefined,
        schoolYearId ? +schoolYearId : undefined,
      )
    }
    ```

    Jangan ubah endpoint path, method lain, atau response shape lainnya.
  </action>
  <verify>
    Test manual: GET /master/classes?schoolYearId=1 → setiap item ada field studentCount (integer).
    GET /master/classes (tanpa param) → studentCount ada tapi hitung semua tahun.
  </verify>
  <done>AC-1 backend satisfied: endpoint mengembalikan studentCount per kelas</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — kolom jumlah siswa + modal daftar siswa</name>
  <files>apps/frontend/src/types/master.ts, apps/frontend/src/pages/master/kelas/index.tsx</files>
  <action>
    **types/master.ts:**
    Tambah `studentCount?: number` ke interface `Class`.

    **kelas/index.tsx:**

    1. Tambah import DialogDescription, DialogFooter dari dialog (sudah ada Dialog, DialogContent, DialogHeader, DialogTitle).

    2. Tambah state baru:
       ```ts
       const [schoolYears, setSchoolYears] = useState<SchoolYear[]>([])
       const [activeYearId, setActiveYearId] = useState<number | null>(null)
       const [studentListClass, setStudentListClass] = useState<Class | null>(null)
       const [studentListYearId, setStudentListYearId] = useState<number | null>(null)
       const [classMembers, setClassMembers] = useState<ClassMember[]>([])
       const [membersLoading, setMembersLoading] = useState(false)
       ```

    3. Tambah import type: `SchoolYear`, `ClassMember` dari '@/types/master'.

    4. Fetch school years on mount (di useEffect fetchUnits):
       ```ts
       const yearsRes = await apiClient.get<SchoolYear[]>('/master/school-years')
       setSchoolYears(yearsRes.data)
       const active = yearsRes.data.find((y) => y.isActive)
       if (active) setActiveYearId(active.id)
       ```
       Jalankan parallel dengan fetchUnits menggunakan Promise.all.

    5. Update `fetchClasses` agar pass `schoolYearId=activeYearId` ke params jika ada:
       ```ts
       const params: Record<string, string | number> = {}
       if (selectedUnitId) params.school_unit_id = selectedUnitId
       if (activeYearId) params.schoolYearId = activeYearId
       ```
       Tambah `activeYearId` ke dependency array useCallback.

    6. Di tabel kelas, ubah header: `['Nama Kelas', 'Tingkat', 'Unit Sekolah', 'Jumlah Siswa', 'Aksi']`.

    7. Buat baris kelas bisa diklik (tambah `cursor-pointer` dan `onClick`):
       ```tsx
       <tr
         key={row.id}
         className="border-t border-gray-100 hover:bg-gray-50 cursor-pointer"
         onClick={() => handleOpenStudentList(row)}
       >
       ```
       Tapi tombol Edit/Hapus jangan trigger row click: tambah `e.stopPropagation()` di onClick tombol-tombol aksi.

    8. Tambah cell jumlah siswa setelah unitName:
       ```tsx
       <td className="px-6 py-4 text-sm text-gray-600">{row.studentCount ?? 0}</td>
       ```

    9. Handler `handleOpenStudentList`:
       ```ts
       const handleOpenStudentList = async (cls: Class) => {
         setStudentListClass(cls)
         setStudentListYearId(activeYearId)
         await fetchClassMembers(cls.id, activeYearId)
       }
       ```

    10. Handler `fetchClassMembers`:
        ```ts
        const fetchClassMembers = async (classId: number, yearId: number | null) => {
          if (!yearId) return
          setMembersLoading(true)
          try {
            const res = await apiClient.get<ClassMember[]>('/master/student-classes', {
              params: { classId, schoolYearId: yearId },
            })
            setClassMembers(res.data)
          } catch {
            setClassMembers([])
          } finally {
            setMembersLoading(false)
          }
        }
        ```

    11. Tambah Dialog daftar siswa di JSX (setelah Dialog Konfirmasi Hapus Kelas):
        ```tsx
        <Dialog open={!!studentListClass} onOpenChange={(open) => { if (!open) setStudentListClass(null) }}>
          <DialogContent className="sm:max-w-lg">
            <DialogHeader>
              <DialogTitle>Siswa Kelas {studentListClass?.name}</DialogTitle>
              <DialogDescription>
                <Select
                  value={studentListYearId?.toString() ?? '__none__'}
                  onValueChange={(v) => {
                    const id = Number(v)
                    setStudentListYearId(id)
                    if (studentListClass) fetchClassMembers(studentListClass.id, id)
                  }}
                >
                  <SelectTrigger className="mt-2 h-8 text-sm">
                    <SelectValue placeholder="Pilih tahun pelajaran" />
                  </SelectTrigger>
                  <SelectContent>
                    {schoolYears.map((y) => (
                      <SelectItem key={y.id} value={y.id.toString()}>
                        {y.name}{y.isActive ? ' (Aktif)' : ''}
                      </SelectItem>
                    ))}
                  </SelectContent>
                </Select>
              </DialogDescription>
            </DialogHeader>
            <div className="max-h-80 overflow-auto">
              {membersLoading ? (
                <p className="text-sm text-gray-400 py-4 text-center">Memuat...</p>
              ) : classMembers.length === 0 ? (
                <p className="text-sm text-gray-400 py-4 text-center">Tidak ada siswa di kelas ini</p>
              ) : (
                <table className="w-full text-sm">
                  <thead>
                    <tr className="bg-gray-50 border-b">
                      <th className="text-left px-4 py-2 font-medium text-gray-600">NIS</th>
                      <th className="text-left px-4 py-2 font-medium text-gray-600">Nama</th>
                      <th className="text-left px-4 py-2 font-medium text-gray-600">JK</th>
                    </tr>
                  </thead>
                  <tbody>
                    {classMembers.map((m) => (
                      <tr key={m.enrollmentId} className="border-b last:border-0">
                        <td className="px-4 py-2 font-mono text-xs">{m.nis}</td>
                        <td className="px-4 py-2">{m.name}</td>
                        <td className="px-4 py-2">{m.gender}</td>
                      </tr>
                    ))}
                  </tbody>
                </table>
              )}
            </div>
          </DialogContent>
        </Dialog>
        ```

    Jangan ubah logika hapus/edit kelas, tab unit, atau form handlers.
  </action>
  <verify>
    - Tab Kelas: kolom "Jumlah Siswa" muncul dengan angka
    - Klik baris → Dialog terbuka dengan daftar siswa
    - Select tahun → daftar siswa berubah
    - Tombol Edit/Hapus tidak trigger row click
  </verify>
  <done>AC-1 dan AC-2 satisfied: jumlah siswa tampil, modal daftar siswa berfungsi</done>
</task>

<task type="auto">
  <name>Task 3: Konfirmasi Lepas siswa di mapping.tsx</name>
  <files>apps/frontend/src/pages/master/kelas/mapping.tsx</files>
  <action>
    1. Tambah state `lepasTarget`:
       ```ts
       const [lepasTarget, setLepasTarget] = useState<ClassMember | null>(null)
       ```

    2. Ubah handler — jangan langsung delete, simpan target:
       Rename `handleLepas` menjadi `executeLepas` (tetap async, tetap sama isinya).
       ```ts
       const executeLepas = async (member: ClassMember) => {
         if (!selectedClassId || !selectedYearId) return
         try {
           await apiClient.delete(`/master/student-classes/${member.enrollmentId}`)
           await fetchClassData(selectedClassId, selectedYearId)
         } catch {
           setError('Gagal melepas siswa dari kelas.')
         } finally {
           setLepasTarget(null)
         }
       }
       ```

    3. Ubah tombol Lepas di tabel currentMembers:
       ```tsx
       onClick={() => setLepasTarget(m)}
       ```

    4. Tambah Dialog konfirmasi di JSX (sebelum Dialog confirmOpen):
       ```tsx
       <Dialog open={!!lepasTarget} onOpenChange={(open) => { if (!open) setLepasTarget(null) }}>
         <DialogContent>
           <DialogHeader>
             <DialogTitle>Lepas Siswa dari Kelas</DialogTitle>
             <DialogDescription>
               Lepas <strong>{lepasTarget?.name}</strong> dari kelas <strong>{selectedClassName}</strong>?
               Siswa dapat di-assign kembali melalui Mapping Kelas.
             </DialogDescription>
           </DialogHeader>
           <DialogFooter>
             <Button variant="outline" onClick={() => setLepasTarget(null)}>Batal</Button>
             <Button
               className="bg-red-600 hover:bg-red-700 text-white"
               onClick={() => lepasTarget && executeLepas(lepasTarget)}
             >
               Lepas
             </Button>
           </DialogFooter>
         </DialogContent>
       </Dialog>
       ```

    DialogDescription sudah diimport di Phase 17. Verifikasi import cukup.
    Jangan ubah handleProses, staging, atau filter.
  </action>
  <verify>
    - Klik "Lepas" pada baris siswa → Dialog konfirmasi muncul dengan nama siswa + nama kelas
    - Klik Batal → Dialog tutup, siswa tidak dilepas
    - Klik Lepas di modal → siswa dilepas, tabel refresh, modal tutup
  </verify>
  <done>AC-3 satisfied: konfirmasi Lepas berfungsi sebelum delete enrollment</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Logic hapus/edit kelas dan unit di kelas/index.tsx
- handleProses, staging, filter di mapping.tsx
- Schema database / migration

## SCOPE LIMITS
- Tidak perlu pagination di daftar siswa (max 50 siswa per kelas wajar)
- Tidak perlu konfirmasi untuk aksi Edit/Tambah (form modal sudah cukup)
</boundaries>

<verification>
- [ ] GET /master/classes?schoolYearId=X mengembalikan studentCount
- [ ] Kolom Jumlah Siswa tampil di tab Kelas
- [ ] Klik baris → Dialog siswa terbuka dengan filter tahun
- [ ] Konfirmasi Lepas tampil sebelum delete di mapping.tsx
- [ ] Tombol Edit/Hapus di tabel kelas tidak trigger row click
</verification>

<success_criteria>
- Semua 3 tasks completed dan verified
- studentCount akurat sesuai tahun pelajaran aktif
- Tidak ada regresi di tab Unit Sekolah atau fitur mapping lainnya
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/18-kelas-enhancements/18-01-SUMMARY.md`
</output>
