---
phase: 35-sidebar-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/components/sidebar/index.tsx
  - apps/frontend/src/layouts/AppLayout.tsx
autonomous: true
---

<objective>
## Goal
Perbaiki 4 issue di sidebar SIMKA: (1) logo diganti hamburger saat collapse, (2) toggle button clean tanpa kesan dropdown, (3) logout icon sejajar dengan info profil, (4) header sticky + footer ikut scroll.

## Purpose
Sidebar saat ini punya UX issues yang perlu dirapikan sebelum dianggap production-ready.

## Output
Sidebar yang lebih polished — hamburger di collapsed state, toggle subtle, logout inline, scroll behavior benar.
</objective>

<context>
@.paul/PROJECT.md
@.paul/STATE.md
@.paul/phases/34-ui-modernization/34-02-SUMMARY.md
@apps/frontend/src/components/sidebar/index.tsx
@apps/frontend/src/components/sidebar/NavItem.tsx
@apps/frontend/src/layouts/AppLayout.tsx
@apps/frontend/src/stores/ui.store.ts
</context>

<acceptance_criteria>
## AC-1: Hamburger di Collapsed State
Given sidebar dalam state collapsed (w-16)
When user melihat header sidebar
Then yang tampil adalah button hamburger (Menu icon) — BUKAN logo. Klik hamburger → sidebar expand.

## AC-2: Toggle Clean Tanpa Floating Dropdown
Given sidebar dalam state expanded (w-64)
When user melihat header sidebar
Then toggle button (chevron) terintegrasi bersih di dalam header area — tidak floating di luar / tidak memberi kesan dropdown. Posisi inline dengan elemen lain.

## AC-3: Logout Sejajar dengan Info Profil
Given sidebar expanded
When user melihat footer sidebar
Then icon logout (hanya icon, tanpa teks "Logout") berada di samping kanan avatar + nama user dalam satu baris horizontal — bukan di bawah.

## AC-4: Footer Ikut Scroll, Header Sticky
Given sidebar memiliki banyak nav items
When user scroll sidebar
Then hanya header (logo area) yang sticky di atas. Footer (user info + logout) ikut scroll bersama nav content — tidak terpisah fixed di bawah.

## AC-5: Build Pass
Given semua perubahan
When `pnpm --filter frontend build`
Then sukses
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Rewrite Sidebar: hamburger collapsed + header sticky + footer scroll</name>
  <files>apps/frontend/src/components/sidebar/index.tsx</files>
  <action>
    Rewrite sidebar dengan perubahan berikut:

    1. **Hamburger di collapsed state** (AC-1):
       - Saat `collapsed={true}`, ganti `<img src="/logo.png">` dengan button hamburger:
       ```
       {collapsed ? (
         <button onClick={onToggle} className="p-2 rounded-lg hover:bg-white/10 text-white/60 hover:text-white transition-colors" title="Buka sidebar">
           <Menu className="w-5 h-5" />
         </button>
       ) : (
         <img src="/logo.png" alt="Logo" className="w-9 h-9 object-contain shrink-0" />
       )}
       ```
       - Import `Menu` dari lucide-react

    2. **Toggle clean** (AC-2):
       - Hapus toggle button yang floating absolute di luar header area
       - Pindahkan toggle (ChevronLeft) ke DALAM header — di ujung kanan, inline dengan logo + teks
       - Saat collapsed: hamburger button sudah berfungsi sebagai toggle → tidak perlu chevron terpisah
       - Struktur header expanded:
       ```
       <div className="flex items-center gap-3 px-4 py-5 border-b border-white/10">
         <img src="/logo.png" ... />
         <div className="flex flex-col flex-1 min-w-0">...</div>
         <button onClick={onToggle} className="p-1.5 rounded-lg hover:bg-white/10 text-white/50 hover:text-white transition-colors shrink-0">
           <ChevronLeft className="w-4 h-4" />
         </button>
       </div>
       ```
       - Struktur header collapsed:
       ```
       <div className="flex items-center justify-center py-5 border-b border-white/10">
         <button onClick={onToggle} ...>
           <Menu className="w-5 h-5" />
         </button>
       </div>
       ```

    3. **Logout inline dengan profil** (AC-3):
       - Ganti layout footer: user info + logout icon dalam SATU baris horizontal
       - Logout hanya icon (tanpa teks) + title tooltip
       ```
       <div className="flex items-center gap-2 px-3 py-3">
         <div className="w-8 h-8 rounded-full bg-accent ...">{initials}</div>
         <div className="flex-1 min-w-0">
           <p className="...">{user?.name}</p>
           <p className="...">{user?.role}</p>
         </div>
         <button onClick={handleLogout} title="Keluar" className="p-1.5 rounded-lg hover:bg-white/10 text-white/40 hover:text-white transition-colors shrink-0">
           <LogOut className="w-4 h-4" />
         </button>
       </div>
       ```
       - Saat collapsed: tidak tampil sama sekali (seperti sekarang — user info hidden)

    4. **Header sticky, footer scroll** (AC-4):
       - Bungkus header (logo area) dengan `sticky top-0 z-10` — tetap di atas saat scroll
       - Footer (user info + logout) TIDAK lagi di `border-t` fixed di bawah
       - Footer dipindahkan ke DALAM `<nav>` (sebelum `</nav>`), sehingga ikut scroll bersama nav items
       - Struktur:
       ```
       <aside>
         <div className="sticky top-0 z-10 bg-gradient-to-b from-primary via-[#1a4630] to-primary-light">
           <!-- Logo area / hamburger -->
         </div>
         <nav className="flex-1 overflow-y-auto px-3 py-4 space-y-0.5">
           <!-- Nav items -->
           <!-- Footer di sini — ikut scroll -->
           <div className="pt-4 mt-4 border-t border-white/10">
             <!-- User info + logout row -->
           </div>
         </nav>
       </aside>
       ```

    DO NOT ubah:
    - Daftar nav items, SectionLabel, urutan menu
    - Props interface (collapsed, mobileOpen, onMobileClose, onToggle)
    - Mobile drawer behavior
    - Gradasi background
  </action>
  <verify>pnpm --filter frontend build</verify>
  <done>AC-1, AC-2, AC-3, AC-4 satisfied: hamburger, toggle clean, logout inline, header sticky footer scroll; build pass</done>
</task>

<task type="auto">
  <name>Hapus hamburger mobile di AppLayout (pindah ke Sidebar)</name>
  <files>apps/frontend/src/layouts/AppLayout.tsx</files>
  <action>
    Saat ini AppLayout punya hamburger button sendiri untuk mobile (`lg:hidden fixed top-4 left-4`). 
    Karena Sidebar sekarang punya hamburger di collapsed state, pastikan tidak ada duplikasi.

    Verifikasi: AppLayout tetap punya hamburger mobile — sidebar sendiri tidak render di mobile (translate-x-full). 
    Jadi hamburger mobile di AppLayout tetap diperlukan untuk MEMBUKA drawer.

    TIDAK ADA PERUBAHAN di AppLayout. Hanya verifikasi bahwa tidak ada konflik.
    
    Jika ada — pastikan hamburger mobile AppLayout dan hamburger collapsed sidebar tidak bentrok.
    AppLayout punya: `lg:hidden` (only mobile)
    Sidebar collapsed punya: `hidden lg:flex` (only desktop)
    → Tidak bentrok.
  </action>
  <verify>pnpm --filter frontend build</verify>
  <done>No conflict: AppLayout hamburger (mobile) dan Sidebar toggle (desktop) tidak bentrok; build pass</done>
</task>

<task type="auto">
  <name>Clean up: hapus import X + ChevronRight dari sidebar jika tidak dipakai</name>
  <files>apps/frontend/src/components/sidebar/index.tsx</files>
  <action>
    Setelah restruktur:
    - `X` icon — hanya dipakai di mobile close button (masih ada)
    - `ChevronRight` — hanya dipakai di toggle collapsed state. Sekarang toggle collapsed = hamburger Menu, bukan ChevronRight. Hapus import ChevronRight.
    - `ChevronLeft` — tetap dipakai di toggle expanded state
    - Tambah `Menu` — dipakai di hamburger collapsed state
    - `LogOut` — tetap dipakai
    
    Sesuaikan import dari lucide-react.
  </action>
  <verify>pnpm --filter frontend build</verify>
  <done>AC-5 satisfied: unused imports cleaned, build pass</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Daftar dan urutan nav items
- SectionLabel component
- NavItem component
- Gradasi background sidebar
- Mobile drawer behavior (overlay + close)
- Props interface SidebarProps
- useUIStore / useAuthStore — tidak ubah store

## SCOPE LIMITS
- HANYA sidebar/index.tsx (rewrite) + AppLayout.tsx (verify only)
- Tidak ubah NavItem.tsx
- Tidak ubah halaman pages/*
- Tidak tambah dependency baru
</boundaries>

<verification>
1. `pnpm --filter frontend build` → sukses
2. Desktop: collapse sidebar → tampil hamburger (bukan logo). Klik → expand. Klik chevron → collapse.
3. Desktop expanded: toggle chevron di dalam header (inline), bukan floating
4. Desktop expanded: logout icon sejajar dengan avatar + nama — dalam satu baris
5. Scroll sidebar: header tetap di atas (sticky), footer ikut scroll
6. Mobile: hamburger tetap muncul, overlay drawer berfungsi
</verification>
</output>
