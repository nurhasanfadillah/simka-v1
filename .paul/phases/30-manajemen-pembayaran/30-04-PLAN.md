---
phase: 30-manajemen-pembayaran
plan: 04
type: execute
wave: 4
depends_on: [30-03]
files_modified:
  - apps/frontend/src/pages/master/manajemen-pembayaran/index.tsx
  - apps/frontend/src/pages/keuangan/generate/index.tsx
  - apps/frontend/src/components/sidebar/index.tsx
  - apps/frontend/src/router.tsx
  - apps/backend/src/billing/payment-management/payment-management.service.ts
autonomous: true
---

<objective>
## Goal
Perbaiki penempatan halaman dan flow Generate Pembayaran.

## Purpose
- **Manajemen Pembayaran** (Master Data) = CRUD buku (POS + Tahun + Nominal) — tanpa kelas, tanpa dual-table
- **Generate Pembayaran** (Keuangan) = pilih buku → dual-table (siswa dengan/s/tanpa tagihan) → buat tagihan

## Output
- Manajemen Pembayaran: halaman CRUD simple (mirip template lama, tanpa kolom kelas)
- Generate Pembayaran: pilih buku dari dropdown (bukan POS+Tahun terpisah), filter unit/kelas, dual-table
- Sidebar: kedua menu muncul di section yang tepat
- Router: kedua route aktif
</objective>

<context>
@.paul/ROADMAP.md
</context>

<acceptance_criteria>
## AC-1: Manajemen Pembayaran = CRUD Buku
Given user buka `/master/manajemen-pembayaran`
When halaman dirender
Then tampil tabel dengan kolom: POS, Tahun Ajaran, Nominal, Aksi — tanpa kolom Kelas. Create/Edit modal: pilih POS + Tahun + input Nominal.

## AC-2: Generate Pembayaran = Pilih Buku, bukan POS+Tahun
Given user buka `/keuangan/generate`
When halaman dirender
Then satu dropdown "Buku Pembayaran" menampilkan daftar buku dari Manajemen Pembayaran (format: "POS Name — Tahun Name"). Setelah pilih buku, tampil info buku + filter unit/kelas/search + dual-table.

## AC-3: Satu siswa hanya satu buku per tahun
Given POST `/billing/payment-management/bills`
When studentId sudah ada di paymentTemplateId yang sama
Then record dilewati (skip), tidak error — unique constraint uq_bill mencegah duplikat

## AC-4: Sidebar benar
Given sidebar dirender
When lihat section Master Data → "Manajemen Pembayaran". Section Keuangan → "Generate Pembayaran", "Data Tagihan", "Transaksi Baru", "Riwayat", "Tunggakan"

## AC-5: Build sukses
Given `pnpm --filter frontend build`
When semua perubahan selesai
Then TypeScript + Vite build tanpa error
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Rewrite Manajemen Pembayaran — simple CRUD</name>
  <files>apps/frontend/src/pages/master/manajemen-pembayaran/index.tsx</files>
  <action>
    Halaman CRUD sederhana:
    - Header: "Manajemen Pembayaran" + deskripsi "Kelola buku pembayaran per pos dan tahun ajaran"
    - Filter: dropdown Tahun Ajaran
    - Tombol: "Tambah Buku"
    - Tabel: POS | Tahun Ajaran | Nominal | Aksi (Edit/Hapus)
    - Modal Create/Edit: Select POS, Select Tahun Ajaran, Input Nominal
    - Delete: safe delete dengan 409 handling
    - Panduan: teks gray italic tentang konsep buku pembayaran
    
    JANGAN ada dual-table, JANGAN ada kelas, JANGAN ada filter unit/kelas.
    
    File SUDAH dibuat — verifikasi isinya sesuai spec di atas.
  </action>
  <verify>grep -c "withBills\|withoutBills\|StudentBillRow\|StudentNoBillRow" apps/frontend/src/pages/master/manajemen-pembayaran/index.tsx</verify>
  <done>AC-1 satisfied: halaman CRUD sederhana tanpa dual-table.</done>
</task>

<task type="auto">
  <name>Rewrite Generate Pembayaran — pilih buku</name>
  <files>apps/frontend/src/pages/keuangan/generate/index.tsx</files>
  <action>
    Halaman generate dengan 1 dropdown "Buku Pembayaran":
    - Fetch `/billing/payment-templates` untuk daftar buku
    - Dropdown menampilkan "POS Name — Tahun Name" dari data buku
    - Setelah pilih buku → fetch template info via `/billing/payment-management/template`
    - Filter tambahan: Unit Sekolah, Kelas, Search NIS/Nama
    - Dual-table seperti sebelumnya (dengan tagihan / tanpa tagihan)
    - Buat tagihan: POST `/billing/payment-management/bills` dengan paymentPostId + schoolYearId dari buku terpilih
    - Jika belum ada buku → arahkan ke Manajemen Pembayaran
    
    JANGAN ada dropdown Tahun terpisah. JANGAN ada dropdown POS terpisah.
    JANGAN ada "Buat Buku Pembayaran" dialog di halaman ini.
    
    File SUDAH dibuat — verifikasi isinya sesuai spec di atas.
  </action>
  <verify>grep "selectedBookId\|selectedBook\|books\[" apps/frontend/src/pages/keuangan/generate/index.tsx | head -5</verify>
  <done>AC-2 satisfied: generate pilih buku, bukan POS+Tahun terpisah.</done>
</task>

<task type="auto">
  <name>Update sidebar — kedua menu aktif</name>
  <files>apps/frontend/src/components/sidebar/index.tsx</files>
  <action>
    Master Data section:
    - "Manajemen Pembayaran" → `/master/manajemen-pembayaran` (icon FileText)
    
    Keuangan section:
    - "Generate Pembayaran" → `/keuangan/generate` (icon Zap)
    - "Data Tagihan" → `/keuangan/tagihan`
    - ...
    
    File SUDAH diupdate — verifikasi.
  </action>
  <verify>grep -n "Manajemen Pembayaran\|Generate Pembayaran" apps/frontend/src/components/sidebar/index.tsx</verify>
  <done>AC-4 satisfied: kedua menu di section yang tepat.</done>
</task>

<task type="auto">
  <name>Update router — kedua route aktif</name>
  <files>apps/frontend/src/router.tsx</files>
  <action>
    - `/master/manajemen-pembayaran` → ManajemenPembayaranPage
    - `/keuangan/generate` → GeneratePage
    
    File SUDAH diupdate — verifikasi.
  </action>
  <verify>grep "manajemen-pembayaran\|keuangan/generate" apps/frontend/src/router.tsx</verify>
  <done>AC-5 satisfied: route lengkap.</done>
</task>

<task type="auto">
  <name>Build & verify</name>
  <files>apps/frontend/src/</files>
  <action>
    Jalankan `pnpm --filter frontend build`. Pastikan TypeScript + Vite build tanpa error.
    Jika ada error, perbaiki.
  </action>
  <verify>pnpm --filter frontend build</verify>
  <done>AC-5 satisfied: build sukses.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Backend (sudah benar)
- Halaman selain manajemen-pembayaran dan generate
- Tipe di types/master.ts

## SCOPE LIMITS
- Hanya perbaikan placement + flow UI
- Konsep "satu siswa satu buku per tahun" sudah dijamin oleh unique constraint DB (uq_bill)
</boundaries>

<verification>
1. `/master/manajemen-pembayaran` → CRUD buku (POS+Tahun+Nominal)
2. `/keuangan/generate` → pilih buku → dual-table
3. `pnpm --filter frontend build` sukses
</verification>
</output>
