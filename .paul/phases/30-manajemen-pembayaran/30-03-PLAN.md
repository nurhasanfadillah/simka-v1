---
phase: 30-manajemen-pembayaran
plan: 03
type: execute
wave: 3
depends_on: [30-01, 30-02]
files_modified:
  - apps/frontend/src/components/sidebar/index.tsx
  - apps/frontend/src/router.tsx
  - apps/frontend/src/pages/master/manajemen-pembayaran/index.tsx (NEW)
  - apps/frontend/src/pages/master/template/** (DELETE)
  - apps/frontend/src/pages/keuangan/generate/** (DELETE)
  - apps/frontend/src/pages/keuangan/tagihan/index.tsx
  - apps/frontend/src/pages/keuangan/tunggakan/index.tsx
  - apps/frontend/src/pages/keuangan/transaksi/baru/index.tsx
  - apps/frontend/src/types/master.ts
  - apps/frontend/src/components/ui/delete-error-modal.tsx
autonomous: true
---

<objective>
## Goal
Rename modul, buat halaman Manajemen Pembayaran baru (dual-table), hapus halaman Generate Tagihan lama, bersihkan referensi invoice di frontend.

## Purpose
Operator sekarang punya satu halaman terpadu untuk mengelola "buku tagihan" — pilih Tahun + POS, langsung lihat siapa yang sudah/belum punya tagihan, edit/hapus/buat tagihan dari satu tempat.

## Output
- Sidebar + route: Manajemen Pembayaran di Master Data
- Halaman baru `/master/manajemen-pembayaran` dengan dual-table
- Halaman Generate Tagihan dihapus total
- Semua referensi `invoiceNumber` dihapus dari UI
- Types terupdate (hapus classId dari PaymentTemplate, invoiceNumber dari Bill, GeneratePreview, GenerateResult)
</objective>

<context>
@.paul/ROADMAP.md (Phase 30 discuss decisions)
@.paul/phases/30-manajemen-pembayaran/30-02-PLAN.md (endpoint references)
</context>

<acceptance_criteria>
## AC-1: Sidebar & Route Rename
Given user buka sidebar
When lihat section Master Data
Then "Template Pembayaran" sudah tidak ada, diganti "Manajemen Pembayaran" → `/master/manajemen-pembayaran`

## AC-2: Generate Tagihan Removed
Given user buka sidebar
When lihat section Keuangan
Then "Generate Tagihan" sudah tidak ada. Route `/keuangan/generate` 404.

## AC-3: Halaman Manajemen Pembayaran
Given user buka `/master/manajemen-pembayaran`
When pilih Tahun + POS
Then:
- Jika template belum ada → tampil form "Buat Buku Pembayaran" (input nominal + tombol Buat)
- Jika template sudah ada → tampil filter bar (Unit, Kelas, Search) + dual table

## AC-4: Tabel 1 — Siswa dengan Tagihan
Given template ada dan filter diterapkan
When tabel 1 dirender
Then tampil: NIS, Nama, Kelas, Nominal, Status (badge), Aksi (Edit, Hapus)
- Edit: modal input nominal baru, validasi < paidAmount ditolak
- Hapus: dialog konfirmasi, jika gagal (409 transaksi) tampil pesan error

## AC-5: Tabel 2 — Siswa tanpa Tagihan
Given template ada dan filter diterapkan
When tabel 2 dirender
Then tampil: ☐ checkbox, NIS, Nama, Kelas, Nominal (editable, default dari template)
- Fitur: cari NIS/Nama, "Pilih Semua" checkbox, "Buat Tagihan" button
- Klik Buat Tagihan → dialog konfirmasi preview (jumlah siswa, daftar nama) → Konfirmasi → proses

## AC-6: No Invoice in UI
Given user lihat halaman Data Tagihan, Tunggakan, Transaksi Baru
When invoice_number sudah dihapus dari backend
Then kolom/tampilan invoice diganti dengan identifikasi lain (kombinasi POS + Tahun + Nama Siswa, atau cukup teks "Tagihan")

## AC-7: Types Clean
Given `pnpm --filter frontend build`
When semua perubahan frontend selesai
Then TypeScript compile tanpa error. Tidak ada referensi ke `classId` di `PaymentTemplate`. Tidak ada `invoiceNumber` di `Bill`. `GeneratePreview` dan `GenerateResult` sudah tidak ada.
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Update Types — hapus classId, invoiceNumber, GeneratePreview/Result</name>
  <files>apps/frontend/src/types/master.ts</files>
  <action>
    1. `PaymentTemplate`: hapus field `classId` dan `className`
    2. `CreatePaymentTemplateDto`: hapus field `classId`
    3. `Bill`: hapus field `invoiceNumber`
    4. Hapus interface `GeneratePreview` seluruhnya
    5. Hapus interface `GenerateResult` seluruhnya
    6. Tambahkan interface baru untuk response endpoint plan 30-02:
       - `PaymentBookInfo` (template info: id, paymentPostId, schoolYearId, amount, paymentPostName, schoolYearName)
       - `StudentBillRow` (siswa dengan tagihan: studentId, studentName, nis, className, classId, billId, amount, paidAmount, status)
       - `StudentNoBillRow` (siswa tanpa tagihan: studentId, studentName, nis, className, classId)
       - `CreateBillsDto` (request: bills array, paymentPostId, schoolYearId)
       - `CreateBillsResponse` (response: created count, bills array)
    
    JANGAN hapus `Bill` atau `BillDetail` — tetap digunakan halaman tagihan/tunggakan.
  </action>
  <verify>grep -r "import.*from.*types/master" apps/frontend/src --include="*.tsx" | head -20</verify>
  <done>AC-7 satisfied: types bersih, interface baru siap digunakan.</done>
</task>

<task type="auto">
  <name>Rename Sidebar & Route</name>
  <files>apps/frontend/src/components/sidebar/index.tsx, apps/frontend/src/router.tsx</files>
  <action>
    Sidebar:
    - Ubah label "Template Pembayaran" → "Manajemen Pembayaran"
    - Ubah route `/master/template` → `/master/manajemen-pembayaran`
    - Icon tetap `FileText`
    
    Router:
    - Ubah import: `import TemplatePage` → `import ManajemenPembayaranPage from '@/pages/master/manajemen-pembayaran'`
    - Ubah path: `/master/template` → `/master/manajemen-pembayaran`
    - Hapus import `GeneratePage` dan route `/keuangan/generate`
    - Hapus NavItem "Generate Tagihan" dari sidebar
    
    JANGAN hapus file lama dulu — page baru dibuat di task berikutnya, file lama dihapus di task cleanup.
  </action>
  <verify>grep -n "template\|Template" apps/frontend/src/components/sidebar/index.tsx apps/frontend/src/router.tsx</verify>
  <done>AC-1 + AC-2 satisfied: sidebar dan route terupdate.</done>
</task>

<task type="auto">
  <name>Buat Halaman Manajemen Pembayaran Baru</name>
  <files>apps/frontend/src/pages/master/manajemen-pembayaran/index.tsx</files>
  <action>
    Buat halaman baru dengan komponen-komponen berikut:
    
    **State Management:**
    - `selectedYearId`, `selectedPostId` — filter utama
    - `selectedUnitId`, `selectedClassId`, `search` — filter tambahan
    - `template` — PaymentBookInfo | null
    - `withBills` — StudentBillRow[]
    - `withoutBills` — StudentNoBillRow[]
    - `checkedIds` — Set<studentId>
    - `editingBill` — StudentBillRow | null (untuk modal edit)
    - `deleteTarget` — StudentBillRow | null (untuk dialog hapus)
    - `createPreviewOpen` — boolean (dialog konfirmasi buat tagihan)
    
    **Dropdown Cascade:**
    - fetch schoolYears → pilih tahun (default: yang isActive=true)
    - fetch paymentPosts (tidak perlu filter khusus, tapi tampilkan yang ada di tahun itu via data)
    - fetch schoolUnits → filter by tahun? atau semua
    - fetch classes → filter by unitId + schoolYearId
    
    **Data Fetching:**
    - Saat selectedYearId + selectedPostId berubah → fetch template
    - Jika template ada → fetch withBills + withoutBills
    - Refetch saat filter classId/unitId/search berubah
    
    **Template Not Found:**
    - Tampilkan card: "Belum ada buku pembayaran untuk POS ini di tahun ini."
    - Tombol "Buat Buku Pembayaran" → dialog input nominal (useForm dengan zod: amount min 1000)
    - Submit → POST `/billing/payment-templates` (endpoint existing, tanpa classId) → refetch template
    
    **Tabel 1 — Dengan Tagihan:**
    - Kolom: NIS, Nama, Kelas, Nominal (formatRupiah), Status (badge: lunas=hijau, cicilan=kuning, belum_bayar=abu), Aksi
    - Aksi Edit: modal dengan input nominal. Validasi: amount >= row.paidAmount. Tampilkan "Sudah dibayar: Rp X" di modal.
    - Aksi Hapus: dialog konfirmasi "Hapus tagihan {nama} — {POS}?". Handle 409: tampilkan error "Tidak dapat dihapus karena sudah memiliki transaksi".
    - Search bar di atas tabel (search by NIS/Nama, filter lokal atau query param)
    
    **Tabel 2 — Tanpa Tagihan:**
    - Kolom: ☐ (checkbox), NIS, Nama, Kelas, Nominal (input number editable, default dari template.amount)
    - Header checkbox "Pilih Semua"
    - Search bar
    - Tombol "Buat Tagihan" di bawah tabel — disabled jika tidak ada yang dicentang
    - Klik Buat Tagihan → dialog preview: "Buat tagihan untuk {N} siswa?", daftar nama siswa yang dicentang
    - Konfirmasi → POST `/billing/payment-management/bills` → hapus centang, refetch both tables
    
    **Edit nominal di Tabel 2:**
    - Gunakan local state `amounts: Record<studentId, number>` untuk tracking perubahan nominal
    - Default value dari template.amount
    
    **Styling:**
    - Sama dengan halaman lain: `p-6`, header flex justify-between, white card table
    - 2 tabel dipisahkan dengan section header "Siswa dengan Tagihan" dan "Siswa tanpa Tagihan"
    - Loading state: skeleton
    - Error state: banner merah
    
    **Panduan:**
    - Teks panduan `text-xs italic text-gray-400 mb-4` di bawah judul
    - Isi: "Kelola buku tagihan per pos pembayaran dan tahun ajaran. Pilih POS dan tahun untuk melihat daftar siswa yang sudah atau belum memiliki tagihan."
    
    JANGAN gunakan komponen dari generate page lama — tulis dari awal.
  </action>
  <verify>pnpm --filter frontend build</verify>
  <done>AC-3 + AC-4 + AC-5 satisfied: halaman Manajemen Pembayaran berfungsi penuh dengan dual-table.</done>
</task>

<task type="auto">
  <name>Cleanup Invoice References di Halaman Existing</name>
  <files>
    apps/frontend/src/pages/keuangan/tagihan/index.tsx,
    apps/frontend/src/pages/keuangan/tunggakan/index.tsx,
    apps/frontend/src/pages/keuangan/transaksi/baru/index.tsx
  </files>
  <action>
    Di semua 3 halaman:
    - Hapus kolom/tampilan `row.invoiceNumber` atau `bill.invoiceNumber`
    - Ganti dengan identifikasi alternatif:
      - Tagihan page: tampilkan `{bill.paymentPostName} — {bill.studentName}` sebagai header row, bukan invoice
      - Tunggakan: hapus kolom invoice, ganti dengan POS name atau cukup hapus kolomnya
      - Transaksi Baru: label bill selection gunakan `{bill.paymentPostName} — {bill.studentName} ({formatRupiah(bill.totalAmount)})`
    
    JANGAN ubah struktur table selain kolom invoice. JANGAN ubah logic fetch/filter.
  </action>
  <verify>grep -rn "invoiceNumber\|invoice_number\|invoice" apps/frontend/src/pages/ --include="*.tsx"</verify>
  <done>AC-6 satisfied: tidak ada referensi invoice di UI.</done>
</task>

<task type="auto">
  <name>Hapus File Lama & Cleanup</name>
  <files>
    apps/frontend/src/pages/master/template/** (DELETE),
    apps/frontend/src/pages/keuangan/generate/** (DELETE),
    apps/frontend/src/components/ui/delete-error-modal.tsx
  </files>
  <action>
    1. Hapus folder `apps/frontend/src/pages/master/template/` seluruhnya
    2. Hapus folder `apps/frontend/src/pages/keuangan/generate/` seluruhnya
    3. Di `delete-error-modal.tsx`: hapus entry `template: 'Template Pembayaran'` dari LABEL_MAP (jika ada)
    
    Pastikan tidak ada file import yang mengarah ke path yang sudah dihapus.
  </action>
  <verify>pnpm --filter frontend build</verify>
  <done>Semua file lama dihapus, build frontend sukses tanpa broken import.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Halaman keuangan lain (Tagihan, Tunggakan, Transaksi, Riwayat) — hanya bersihkan invoice references
- Halaman laporan
- Halaman pengaturan
- Auth flow, layout, dashboard

## SCOPE LIMITS
- Mengganti halaman Generate Tagihan → Manajemen Pembayaran (flow baru)
- Halaman Tagihan (Data Tagihan) TETAP ADA sebagai list semua bills
- Tidak mengubah backend
</boundaries>

<verification>
1. `pnpm --filter frontend build` — TypeScript + Vite build tanpa error
2. Buka `/master/manajemen-pembayaran` — halaman baru tampil
3. Pilih Tahun + POS → dual table muncul
4. Edit/hapus/buat tagihan dari halaman berfungsi
5. `/keuangan/generate` 404, `/master/template` 404
6. Tidak ada referensi invoiceNumber di UI manapun
</verification>
</output>
