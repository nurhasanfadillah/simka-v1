---
phase: 30-manajemen-pembayaran
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/db/schema/financial.schema.ts
  - apps/backend/src/billing/payment-templates/**
  - apps/backend/src/billing/billing-engine/**
  - apps/backend/src/billing/bills/bills.service.ts
  - apps/backend/src/billing/billing.module.ts
  - apps/backend/src/app.module.ts
  - apps/backend/src/reports/reports.service.ts
  - apps/backend/drizzle/**
autonomous: true
---

<objective>
## Goal
Hapus `classId` dari `payment_templates`, hapus `invoice_number` dari `bills`, hapus modul `billing-engine` (generate tagihan lama), dan update semua file terdampak.

## Purpose
Template pembayaran tidak lagi terikat kelas. Sistem no. invoice dihapus — bills adalah "buku tagihan" yang cukup diidentifikasi oleh POS + Tahun + Siswa. Modul generate tagihan massal lama digantikan oleh modul manajemen pembayaran baru (plan 30-02).

## Output
- DB migration baru
- Schema file terupdate
- DTO, service, controller payment-templates terupdate (tanpa classId)
- Modul billing-engine dihapus total
- BillsService tidak lagi select invoiceNumber
- ReportsService terupdate
- Seed permissions bersih
</objective>

<context>
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
</context>

<acceptance_criteria>
## AC-1: Schema Migration
Given migration dijalankan (drizzle-kit generate + migrate)
When query `payment_templates` tanpa `class_id`
Then tabel `payment_templates` hanya punya kolom: id, payment_post_id, school_year_id, amount, created_at, updated_at — dengan unique constraint pada (payment_post_id, school_year_id)

## AC-2: No Invoice Column
Given migration dijalankan
When query `bills`
Then kolom `invoice_number` sudah tidak ada

## AC-3: Payment Templates API Clean
Given backend berjalan (pnpm dev)
When GET/POST/PATCH/DELETE `/billing/payment-templates`
Then semua endpoint berfungsi tanpa `classId` — create hanya butuh paymentPostId, schoolYearId, amount

## AC-4: Billing Engine Removed
Given backend berjalan
When akses POST `/billing/generate/preview` atau POST `/billing/generate`
Then 404 Not Found

## AC-5: Build & TypeScript Pass
Given `pnpm build` dijalankan
When semua perubahan selesai
Then TypeScript compile tanpa error, tidak ada broken import, tidak ada referensi ke classId di payment-templates
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>DB Migration — drop class_id + invoice_number</name>
  <files>apps/backend/src/db/schema/financial.schema.ts, apps/backend/drizzle/</files>
  <action>
    1. Hapus `classId` dari definisi `paymentTemplates` di financial.schema.ts
    2. Update unique constraint jadi: `unique('uq_payment_template').on(table.paymentPostId, table.schoolYearId)`
    3. Hapus `invoiceNumber` dari `bills` di financial.schema.ts (termasuk `.unique()`)
    4. Hapus relasi `class` dari `paymentTemplatesRelations`
    5. Jalankan `pnpm --filter backend drizzle-kit generate` untuk auto-generate migration SQL
    6. Jalankan `pnpm --filter backend drizzle-kit migrate` untuk apply migration
    JANGAN hapus kolom `paymentTemplateId` dari bills — tetap dibutuhkan untuk FK ke template.
  </action>
  <verify>pnpm --filter backend drizzle-kit generate && pnpm --filter backend build</verify>
  <done>AC-1 + AC-2 satisfied: kolom class_id dan invoice_number tidak ada lagi di schema. Unique constraint payment_templates = (payment_post_id, school_year_id).</done>
</task>

<task type="auto">
  <name>Update PaymentTemplates DTOs</name>
  <files>apps/backend/src/billing/payment-templates/dto/create-payment-template.dto.ts</files>
  <action>
    Hapus field `classId` dari `CreatePaymentTemplateDto`.
    File menjadi hanya: paymentPostId, schoolYearId, amount.
    JANGAN ubah UpdatePaymentTemplateDto (tetap hanya amount opsional).
  </action>
  <verify>pnpm --filter backend build</verify>
  <done>AC-3 satisfied: DTO create tidak lagi butuh classId.</done>
</task>

<task type="auto">
  <name>Update PaymentTemplates Service & Controller</name>
  <files>apps/backend/src/billing/payment-templates/payment-templates.service.ts, apps/backend/src/billing/payment-templates/payment-templates.controller.ts</files>
  <action>
    Service:
    - `findAll()`: hapus parameter `classId`, hapus kondisi `eq(paymentTemplates.classId, classId)`
    - `findAll()` select: hapus `classId: paymentTemplates.classId`
    - `findOne()` select: hapus `classId`
    - `create()`: hapus `classId: dto.classId` dari values
    - Update unique error message
    
    Controller:
    - Hapus `@Query('classId')` dari endpoint GET findAll
    
    JANGAN hapus import classes dari service — mungkin masih dibutuhkan untuk join (className tetap ada di response).
    Update LEFT JOIN tetap menggunakan `classes` tapi via bills context nanti, bukan dari template langsung. Setelah classId dihapus dari templates, className tidak bisa di-join dari template. Tapi ini akan dihandle oleh query baru di plan 30-02.
    
    Untuk saat ini: hapus className dari select pada findAll/findOne. Response template tidak lagi menyertakan className.
  </action>
  <verify>curl -X POST http://localhost:3000/api/billing/payment-templates -H "Authorization: Bearer TOKEN" -H "Content-Type: application/json" -d '{"paymentPostId":1,"schoolYearId":1,"amount":150000}'</verify>
  <done>AC-3 satisfied: findAll dan findOne tidak lagi return classId/className. Create/PATCH berfungsi tanpa classId.</done>
</task>

<task type="auto">
  <name>Hapus Modul Billing Engine</name>
  <files>apps/backend/src/billing/billing-engine/**, apps/backend/src/billing/billing.module.ts</files>
  <action>
    1. Hapus folder `apps/backend/src/billing/billing-engine/` seluruhnya
    2. Di `billing.module.ts`: hapus `import { BillingEngineModule }` dan hapus dari `imports` array
    JANGAN hapus BillsModule atau PaymentTemplatesModule.
  </action>
  <verify>pnpm --filter backend build</verify>
  <done>AC-4 satisfied: POST /billing/generate/preview dan /billing/generate tidak ada lagi (404).</done>
</task>

<task type="auto">
  <name>Update BillsService — hapus invoiceNumber</name>
  <files>apps/backend/src/billing/bills/bills.service.ts</files>
  <action>
    - Di `buildBaseQuery()` select: hapus `invoiceNumber: bills.invoiceNumber`
    - Hapus filter `filters.paymentTemplateId` jika ada (atau biarkan — tapi pastikan tidak error)
    - Hapus join condition yang mereferensi `classId` dari paymentTemplates jika ada
    
    JANGAN ubah controller bills — endpoint findAll, findTunggakan, findOne tetap ada.
  </action>
  <verify>pnpm --filter backend build</verify>
  <done>AC-5 satisfied: BillsService tidak lagi select invoiceNumber, build sukses.</done>
</task>

<task type="auto">
  <name>Update ReportsService — hapus referensi invoiceNumber/classId</name>
  <files>apps/backend/src/reports/reports.service.ts</files>
  <action>
    Cari semua referensi ke `bills.invoiceNumber` dan `paymentTemplates.classId` di ReportsService, hapus dari select.
    Jika ada .innerJoin yang mereferensi paymentTemplates.classId dalam filter, sesuaikan.
  </action>
  <verify>pnpm --filter backend build</verify>
  <done>AC-5 satisfied: ReportsService tidak ada referensi classId/invoiceNumber lagi.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- bills.paymentTemplateId — FK ke template tetap ada
- bills.studentId, bills.totalAmount, bills.status — struktur bills inti tidak berubah
- transaction_items, transactions — transaksi tidak disentuh
- Schema tabel lain (school_units, classes, students, dll)

## SCOPE LIMITS
- Plan ini hanya backend — 0 perubahan frontend
- Tidak menambah endpoint baru (itu plan 30-02)
- Tidak mengubah UI (itu plan 30-03)
</boundaries>

<verification>
1. `pnpm --filter backend build` — TypeScript compile tanpa error
2. Migration berhasil di-generate oleh drizzle-kit
3. Tidak ada file import dari `billing-engine` yang tersisa
</verification>
</output>
